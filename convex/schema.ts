import { defineSchema } from "convex/server";

/**
 * Convex schema placeholder.
 *
 * Tables for products, orders, carts, customers, conversations will be added
 * as MVP features land. Design rule: every order/inventory entity carries a
 * `channel` field so future marketplace connectors (Shopee, Lazada, TikTok
 * Shop, StoreHub) slot in without schema rewrites.
 */
export default defineSchema({});
