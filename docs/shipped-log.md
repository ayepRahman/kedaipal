# Shipped log (post-MVP)

Chronological record of everything shipped after the April 2026 MVP — newest first. Each entry says **what shipped, why it was built that way, what was deliberately rejected, and which trap was found**, and links to the feature doc that carries the full detail.

This log lives here, not in [`CLAUDE.md`](../CLAUDE.md), because it is reference material read on demand — not an instruction that needs to be in context for every task. It was moved out verbatim (ClickUp `86eyqh89n`) when the section had grown to **91% of CLAUDE.md**, loading ~40k tokens into every session while carrying none of the rules.

## How to add an entry

- Write the entry **in the same PR as the feature**, alongside its code, tests and docs (the standing "code + tests + docs together" rule).
- New entries go at the **top** of the list below.
- Link the feature doc that holds the detail — this log is the summary and the reasoning, the doc is the reference.
- Keep the *why*: the rejected alternative and the trap found are the parts that stop the next person repeating the mistake.
- **Do not add it to CLAUDE.md.** CLAUDE.md carries rules and pointers only.

> Seller-facing release notes are a **different artifact** — benefit statements shown in-app, authored in `src/content/releases.ts` (ClickUp `86eyqgxv9`). This log is the engineering record; that list is what a seller reads. Neither replaces the other.

---

- **Annual billing — sold in Settings → Billing to proven payers, and the hidden public toggle was wrong all along** ✅ (dev) — annual was ~80% built and 0% sellable: `ANNUAL_MONTHS_CHARGED = 10`, `billingCycle` on both tables, a 365-day period, an admin cycle picker — and **no seller-facing surface anywhere**, so the only way to buy a year was for the operator to think of it. The blocker on record ("no recurring rails") turned out to apply to *auto-renewing monthly*, not to annual: **a year is a single bank transfer**, which is precisely what manual billing already does well, and it collapses twelve issue-chase-mark-paid cycles per seller into one. So annual ships **in-app, not on `/pricing`** — `SHOW_ANNUAL_TOGGLE` stays `false` because a public annual price with no checkout is a dead-end CTA. **The dead code behind that flag was broken in three ways**, none of which anyone had seen: the billed-annual total was `floor(monthly × 10 / 12) × 10` — a year priced at **8.33 months** — so Starter advertised **RM650/yr against an RM790 invoice**, under-quoting every tier by RM140–500 / S$50–200; `pricingpage_billed_annual` read **"Billed RM{total}/yr"** in all three locales, slipping past the currency guard because `/\bRM\s?\d/` doesn't match `RM{` (now `[\d{]` — a symbol glued to a *placeholder* is exactly as wrong as one glued to `79`, and this was the **third** currency spelled into `pricing_*`); and a **`-17%`** badge sat four lines below the comment forbidding percentages. All three would have shipped the day the flag flipped. **`annualQuote()` is now the single author** of every annual number on every surface — `annualTotal` *is* `planPrice(plan,"annual",…)`, pinned by a test, and `effectiveMonthly` rounds **up** (`Math.round` gave MYR Starter 6,583 × 12 = 78,996 against a 79,000 charge; a seller multiplying by twelve must never land under the bill). Eligibility is a pure ladder (`src/lib/annual-billing.ts`, 32 tests) whose **order is load-bearing**: a pending *annual* invoice short-circuits first, because `issueInvoice` never touches the subscription so an accepted seller still reads `monthly` for 14 days and the card would keep selling what they just bought — then `issueInvoice` throws at the operator; `onAnnual` sits **above** the plan gate, because hiding a true fact about someone's own billing due to an off-list tier is a lie by omission. **Pro only** — Starter because a year upfront contradicts start-when-you-sell (and the Starter → Pro nudge now says *why not*, since a constraint is surfaced, never enforced silently), **Scale because `issueInvoice` throws on it**, which would have reproduced in-app the exact dead-end CTA we refuse to ship publicly. **Two paid invoices required**: there is no proration anywhere, so a year taken from someone who churns in month two is a manual refund argument. An open invoice is treated as **the moment, not the blocker** — nothing has been collected, so it is the only point in a cycle when switching is free to the seller, and a monthly seller has one pending for half of every cycle so hiding it there would make the card blink in and out — **except** inside `ANNUAL_SWAP_MIN_DAYS = 4`, where the void-then-reissue window could let the daily cron flip them to `past_due` and soft-lock the dashboard of the seller most willing to pay us (the cron locks on an overdue pending invoice **and** on a lapsed period with nothing pending). Two operator bugs found and fixed on the way: `billingEmail` sourced plan+cycle from the **subscription**, so the first annual invoice would have emailed **"Pro · Monthly" beside a ten-times amount** while its own PDF said "Annual Subscription" (now `invoice.x ?? sub.x`, matching `markPaid`); and `listPending` omitted `billingCycle`, so the admin could mark an invoice paid — granting 365 days instead of 30 — **without being able to see which it was**. Deliberately rejected: any percentage anywhere; promotional treatment (this tab's other cards are neutral, and a glowing panel in Settings makes a small seller suspicious, so emerald appears on exactly one word — the saving); leading with the flattering effective-monthly instead of the amount actually transferred; a "switch back to monthly" button; paraglide keys (billing-tab is 100% hardcoded English and translating one dashboard surface would make it the only one); and an allowlist entry in `currency-literals.test.ts` — `DEFAULT_BILLING_CURRENCY` moved to `convex/lib/plans.ts` instead, because an allowlist licenses every *future* literal in that file. The `onAnnual` card states the period end as a fact and **promises no reminder**, because the annual renewal chase is currently a `console.info` — named as the follow-up that matters most before the first annual invoice is paid. See [`docs/pricing.md`](./pricing.md#annual-billing) + [`docs/manual-subscription.md`](./manual-subscription.md).

- **Delyva courier booking — backend (nationwide parcel + cold chain, 2nd provider behind the `deliveryJobs` seam)** ✅ (ClickUp `86eyjpv6z`, dev, PR1 of 2) — Lalamove covers intra-city riders only; the frozen ICP (Walid, Haziq, WaDaFish) ships outstation and chilled. Delyva chosen over EasyParcel (frozen prohibited there; `86eyexp7p`); commercials = **Delyva-funded 1% per completed order**, which only tracks via **`source: "kedaipal"` on every `POST /order`** — hardcoded in `buildCreateOrderBody` and pinned by test, because dropping it is invisible breakage (bookings keep working, revenue silently stops). **Connect deviates from the ticket's two-field AC on purpose**: Delyva auths every call with ONE key (`X-Delyvax-Access-Token`), and `GET /user` (webhook HMAC `apiSecret`) + `GET /customer` (integer `customerId`) supply the rest — so the seller pastes one key, `delyva.connect` validates it, encrypts both secrets **in the action** (no plaintext ever lands in the DB or mutation log, unlike the Lalamove/HitPay save-then-encrypt path) and **auto-subscribes our webhook URL** (`POST /webhook` × 3 events; failure doesn't fail the connect — `webhooksSubscribedAt` unset is the honest retry signal). **No sandbox toggle, by design**: Delyva has one API host and no key prefix — a "sandbox" is a separate account at `demo.delyva.app`, so the ticket's "infer env from key prefix" AC is void. **Wire shapes probed live, not assumed** (demo account, 27 Aug): waypoint address rides INSIDE `contact` (top-level `address1` is rejected), `inventory` is required on BOTH waypoints, `POST /order/{id}/cancel` works (returns 900), and the real zero-credit refusal ("You don't have enough credit balance. Bal: 0, Price: 5.00") is the classifier's test fixture. Dispatch differs from Lalamove where Delyva differs: `instantQuote` returns a **service LIST** (courier + price each, indicative, no quotation id) → `prepareBooking` hands the card a price-sorted picker and an **empty list is a normal answer** (demo account returns zero CHILLED services — a real state, rendered as an empty state with the manual-courier handoff); `confirmBooking` = the same **reserve → external → commit/release** invariant, with the reservation **counting jobs from EITHER provider** (never a rider AND a courier on one order), draft create under `idempotency-key: kp-<jobId>` (24h response cache = retries can't double-create), process failure **cancels the draft best-effort** (no orphans in the seller's Delyva dashboard) then releases into the amber card with a classified reason (`credit`/`not_activated`/`no_service`/`unknown` — message-phrase matching because Delyva has no stable error ids, and per the 86eypncfy lesson "unknown" beats a wrong story). Parcel weight = `summarizeCartWeight` over `parcelWeightG` (the 86eyeea1n field, blocker merged), with a seller-typed `weightKgOverride` for custom/weightless carts (the seller knows the packed weight best). Webhook `POST /webhook/delyva`: base64 HMAC-SHA256 of the raw body with the account's apiSecret, fail-closed on a matching job with no secret, 200+ignore on foreign traffic, plus a `customerId` cross-check; status codes verified against **Delyva's own maintained WooCommerce plugin** (100/110 assigning, 200/400 ongoing, 475 rejected, 500/600 picked_up, 700/1000 completed, 900 canceled) with **650 (failed delivery attempt) deliberately NOT terminal** — the parcel is still with the courier (retry/return follows), so it stamps `failureReason` + emails the seller and leaves the job picked_up. Collected → **shipped**, completed → **delivered** via `applyStatusTransition` behind the same never-regress guards; the AWB (`consignmentNo`, from the order.created webhook or the post-process fetch) mirrors **fill-if-unset** into the existing `orders.courierName`/`trackingNo`/`carrierTrackingUrl` (86eyehvk4) so the buyer's track page + shipped WhatsApp carry tracking with zero new plumbing and a seller's manual entry is never overwritten. Structural moves: provider-agnostic job-status machinery extracted to **`convex/lib/deliveryJobs.ts`** (lib/lalamove.ts re-exports, zero importer churn); `deliveryJobs.provider` widened to a union; **`by_provider_order` index widened to `[provider, providerOrderId]`** (two opaque id namespaces + `.unique()` = a cross-provider collision would throw inside a webhook handler); `retailers.delyva` gets a **structured `pickupAddress`** (its own field, NOT a parse of `businessAddress` — that's one free-text label + coords, useless for postcode/state zone pricing, and the pickup address is the unit Delyva's cold-chain activation applies to); the manual-advance rider gate + the failure email (all 3 locales) are now provider-aware. Pro-gated via the existing `PLAN_FEATURES.delivery`, MY-only via its own `delyvaBookingAllowed` table (booking capability per provider, never derived from pricing modes), disconnect/disable never gated. **Seller UI (PR2):** the feature is **vendor-side only** — the buyer never sees Delyva, keeps paying the store's existing flat/weight-zone charge at checkout, and gets the tracking number through the manual-courier pipeline that already existed. That matches where the market landed (Shopee **removed** buyer courier choice in 2020 and assigns it; Lazada/TikTok show a tier not a brand; Shopify has the buyer pick a merchant-configured *rate*) and it protects the seller's margin, since the booking spends **their** credit — so the dispatch card shows what the buyer paid beside each courier's real price. **Settings → Fulfilment → Delyva courier** sits between the delivery *charge* card and the despatch *label* card (what the buyer pays → how the parcel leaves → the paper on it; booking is orthogonal to pricing, so it's its own card, never a charge mode) and — unlike its HitPay sibling — owns its own reads/actions rather than taking a summary + `onSave`, because Delyva has its own Convex namespace and threading four actions through the tab would be five props for nothing. It carries the one-key connect (with the "one key is all we need" line, since every other integration asks for two), the **account name** as proof the right account's key was pasted, the structured pickup address with the 5-digit-postcode rule mirrored from the server, the parcel-type default tiles, a **cold-chain activation note** shown only for a chilled/frozen default (the likeliest cause of a frozen seller's first failed booking), and a **webhook-retry warning** when subscription failed — otherwise a silent failure surfaces only as orders that mysteriously stop updating. **Order detail → Delyva Courier** sits right after the Lalamove card (both answer "how is this going out?"; each hides itself when its provider isn't set up, so nobody sees two) with the picker **inline rather than in a modal** — the deliberate divergence from `BookDeliveryCard`: Lalamove has one price bound to a 5-minute quotation so it needs a modal with a countdown, Delyva has a list that never expires and the task is a *comparison*, and a scrollable courier list inside a scrollable dialog is the worse phone experience. Flow = weight (seeded, always editable) → parcel-type pills (store default, per-order override; changing either drops stale prices) → prices → cheapest pre-selected with the CTA repeating the choice → book; a failure renders **in place, not as a toast** (86eypncfy: the seller is back from a top-up and the reason must still be on screen) with the picker intact. The **client-side manual-advance gate now subscribes to both providers** — without it the UI offers a manual *Shipped* the server already refuses — and the gate wording, the by-name card references in cancel/delete warnings, and the failure email follow whichever provider owns the job. Discoverability: an unconnected store gets a one-line hint linking to Settings instead of a disabled button for a feature it's never heard of, plus `public/guides/delyva-setup.html`, the print-ready vendor walkthrough (account → credit → cold-chain activation → key → paste → one real test) on the `hitpay-setup.html` precedent. **Parcel type is a store default + per-order override** (option 1, decided 27 Aug): Delyva's `itemType` applies to the *shipment*, not the line item, so even per-item flags must resolve to one value ("coldest wins") — per-item derivation is backlogged as `86eyrmv1j` for when a genuinely mixed-temperature seller exists. **Settings IA rework rode along (2 Sep, Zaki's test round): a new "Integrations" tab is one home for third-party ACCOUNTS** — Lalamove keys (env badge + webhook row extracted from the fulfilment pricing branch), the Delyva account card, the HitPay card (moved from Payments, checklist deep-link retargeted, pointer left behind) — while Fulfilment keeps the behaviour: the delivery-charge grid untouched and **Courier booking as independent TOGGLES, not a radio** (a store may arm riders AND Delyva and pick per order; the per-order one-active-job reservation arbitrates, both dispatch cards render, and the manual baseline is stated, not an option). The first cut's one-provider-at-a-time mutual exclusion was **built and then reverted the same week** — it solved a money-safety problem that doesn't exist (the buyer's fee is collected at checkout before any booking; provider choice is the seller's margin call, shown as "buyer paid X" beside every price) — the trap worth recording since the rule *sounded* right. One coupling survives: Lalamove live-quote pricing implies rider booking (the quote runs on those credentials), locked-on toggle with the reason; switching pricing away no longer silently disarms booking. `riderOnlyStore` (pricing mode = lalamove) replaces `bookingEnabled` as the driver of the rider-only surfaces (86eyff02p), which stopped being equivalent the day a weight-priced store could arm riders too. Also: SG support (z8r3fdbqmc — 6-digit postal codes via the now-exported `postcodeRule`, no state/city tier, country on the wire) and demo-account detection (`GET /company/{id}` → `code:"demo"`; badge + point-of-spend banners, the 86eypncfy posture — Delyva has no key prefix, so the company record is the only tell). See [`docs/delivery-delyva.md`](./docs/delivery-delyva.md).

- **Pricing reset wave 1 — `/cost` off the retired Founding anchor** ✅ (ClickUp `z8r3fday21`, dev) — first slice of the 30 Aug pricing reset (Scale RM399 at launch, start-when-you-sell billing, Off-Season Hold — all three confirmed by the owner 1 Sep). The `/cost` calculator was the **last public surface still advertising the retired RM104/S$41 Founding rate**: its whole savings model (savings, ratio, the disqualification threshold) anchored on `FOUNDING_PRICE`. The anchor is now the **Pro list price** (`PRO_PRICE`, derived from `PLAN_MONTHLY_PRICES` — same no-restated-literal posture as before, test pins the identity), and `cost_cta_join`/`cost_wa_message` drop the Founding framing in all three locales. **Trap found:** the `landing-redesign.test.ts` founding guard deliberately exempted `cost_*` and matched only `/founding/i` — English-only, so ms "Ahli Pengasas" and zh "创始会员" would have sailed through even on the covered namespaces; the guard now covers `cost_*` and matches `founding|pengasas|创始`. **Deliberately not touched:** existing-member surfaces (storefront badge, "Founding #N" tier pill, billing ribbon — those honour claimed ranks, not an advertised rate) and every rendered tier price, which derives from `PLAN_MONTHLY_PRICES` and moves with the companion backend ticket (`z8r3fday24`, Zaki: RM399/S$149 constants, start-when-you-sell trial rework, hold status). The start-when-you-sell + Off-Season Hold copy is wave 2, gated on that backend so the page never states what billing doesn't yet do. See [`pricing.md`](./pricing.md).

- **Invoices grown up — the invoice face surfaced, seller legal identity, a paid receipt for subscription invoices** ✅ (ClickUp `z8r3fdcrzj`, dev) — three gaps found by a code audit, one theme: *we produced invoice-grade PDFs and then hid or under-dressed them*. (1) **The invoice face was invisible**: `buildOrderReceiptPdf` had printed "Invoice" for unpaid orders since `86ey4fz3w`, but all three download buttons hardcoded "Download receipt", so the one document a seller billing a company needs sat behind a button saying the wrong word. The predicate + nouns now live in **one module** (`convex/lib/orderDocument.ts`) imported by the mapper, the filename, the shared button and the counter Done screen — a call site can no longer reintroduce the wrong noun, and the buyer's tracking page offers "Download invoice" too (their copy is what they forward to whoever pays for them). (2) **Sellers had no legal identity on their own paper** while ours printed "Kedaipal Pte Ltd / UEN 202630712C": new `retailers.businessIdentity` (registered name, SSM/UEN with a country-picked label, multiline billing address, tax no., billing contact) prints under the store name on both faces. **The trap dodged: `businessAddress` was NOT reused** — that field is the private radius-pricing origin, often a home; this block is typed by the seller *specifically to publish*, captured in Settings → Store → Business details with copy saying exactly where it appears, and a test pins it out of the by-slug storefront payload. (3) **A paid subscription invoice still said "Total due" forever**: mark-paid now schedules a **second frozen blob** (`invoices.receiptPdfStorageId`) — the receipt face ("Amount paid", green pill, paid date + method, NO payment instructions) — never overwriting the invoice blob (frozen-at-issue is the point: the bill the seller received stays the bill). The face is explicit opt-in (`asReceipt: true`), never derived from row status, so the legacy on-demand invoice render of an already-paid row still yields an invoice; rows paid before this shipped get their receipt on demand via the same idempotent generator, which refuses anything not `paid`. **Deliberately rejected:** a receipt button on the admin billing console (its invoice list is pending-only — nothing paid to show), and any tax/e-Invoice behaviour (`taxNumber` is a printed string; MyInvois is a separate ticket for when a seller crosses the threshold). All-tier. See [`invoices-receipts.md`](./invoices-receipts.md).


- **Landing video demo — the 30-second explainer in the Mobbin slot** ✅ (dev) — the landing page argued the product in prose and **stylised CSS mockups** (`hero-device.tsx`, `how-it-works-mockups.tsx`) and never once showed the real app moving; a visitor could read the whole page and first see real Kedaipal UI only *after* signing up. The 30 s explainer now sits **directly under the hero, ahead of `RealSellers` and `ProblemStrip`** — the [mobbin.com/mcp](https://mobbin.com/mcp) slot the owner pointed at (autoplay-muted-loop, `playsInline`, no native chrome). It earns that position rather than folding into `HowItWorks` because its burned-in captions walk **the page's own argument in order** — buried chat → store link → storefront → cart → confirmation back in WhatsApp → orders dashboard — so it *is* the pitch, moving; `HowItWorks` stays as the scannable version at a different speed. **The optimisation is the story: 21.5 MB → 928 KB.** The master is 1080p/5.7 Mbps **with no audio stream at all**; ship candidates were measured, not guessed — VP9 crf36 1.94 MB, SVT-AV1 crf36 1.35 MB, x264 crf27 1.58 MB, **VP9 crf46 928 KB** (+ a 1.17 MB H.264 `+faststart` fallback), decided by 2–4× crops of the dashboard rows and WhatsApp bubble text, which flat-shaded motion graphics survive at a CRF that would wreck camera footage. **AV1 was rejected on size, not support** — it lost to VP9 on this source; a phone rendition was rejected because `<source media>` inside `<video>` is dead in modern Chrome and 928 KB doesn't justify JS source-swapping. `preload="none"` + an 8.7 KB WebP poster is load-bearing: **a visitor who bounces at the hero pays 8.7 KB**, an `IntersectionObserver` (0.25) starts playback only on screen and **pauses on exit**, and `aspect-video w-full` reserves the box for zero CLS. Poster is **frame 0, not a prettier mid-clip frame** — playback starts at 0, so anything else visibly jumps. Player rules that are decisions: **no mute control** (there is no audio track — we don't render a control for a thing that doesn't exist), the play/pause toggle is **always visible, never hover-revealed** (a control you must hover to find is a hidden control), **an explicit pause sticks** across scroll (`userPausedRef` — autoplay must never override a deliberate pause), `prefers-reduced-motion` never autoplays and drops `loop`, and a **refused `play()`** (iOS Low Power Mode blocks even muted autoplay) surfaces the same centred play button instead of a dead frame. Captions are burned into the pixels, so `demo_video_transcript` carries them `sr-only` and **translates with the page** — the on-screen captions stay English in all three locales, a stated limitation needing a re-render, not a bug. `/` gains a `VideoObject`; `DEMO_UPLOAD_DATE` is a **pinned constant, not a computed date**, because a `uploadDate` that moves every deploy is the exact signal Google distrusts. See [`docs/landing-video-demo.md`](./landing-video-demo.md).

- **IP-based pricing — the region is detected at the edge, the MY/SG pick rides a cookie the server reads** ✅ (dev) — the SG-lite pricing display shipped complete (both currencies in `PLAN_MONTHLY_PRICES`, `RegionToggle` on the landing teaser and `/pricing`) but the *detection* behind it was a stand-in the code named as one: `Intl.DateTimeFormat().resolvedOptions().timeZone === "Asia/Singapore"`, with a comment saying Cloudflare's country would be better and that plumbing the Worker's `Request` into a loader was left as "a clean follow-up at that seam". This is that follow-up. **A time zone is a device setting, and it fails in the direction that costs money**: an SG visitor whose phone still says `Asia/Kuala_Lumpur` — extremely common on this border, and the default for anyone who bought their phone in Malaysia — was quoted RM79 instead of S$29. `CF-IPCountry` is where the request actually came from. **Read once, at the ROOT route's loader** (`__root.tsx`), because three surfaces need it and the root loader does not re-run on client-side navigation, so a visitor pays for it exactly once; the read is a header lookup with no I/O, so the Clerk-gated `/app` renders that never use it are not measurably slower for carrying it. The mechanism is **`createIsomorphicFn`** (`src/lib/geo-region.ts`), not a `createServerFn`: the server arm reads the cookie then `getRequest().headers`, the client arm returns `null`, and the compiler strips each from the other environment — so there is **no RPC on any navigation** and no server-only import in the browser bundle. That last claim was verified against a real `vite build` rather than assumed: `cf-ipcountry` appears in `dist/server` and in no client chunk. SSR now paints the right currency on the first byte, and because both sides read the same dehydrated loader value, hydration matches — the old shape rendered MY and corrected post-mount, which is a visible RM→S$ flicker for every Singaporean. **Cloudflare's `XX` (unplaceable IP) and `T1` (Tor) read as "no answer", not as a vote for Malaysia**, so the time-zone guess still gets its turn; a *resolvable* non-SG country is a real answer and the device is not allowed to argue with it. The time-zone heuristic survives only as the fallback where the header is absent — `vite dev`, `wrangler dev`, any non-Cloudflare origin — which is also what keeps the SG path exercisable locally. **The `RegionToggle` stays, on all three surfaces** — briefly removed mid-build and restored after a look at how comparable platforms handle this: geo default plus a visible override is the near-universal pattern, because geo-IP is a guess about a *person* and gets VPNs, corporate proxies, carrier NAT egressing in another country, and the JB commuter on a Singapore network wrong. Without the switcher a wrong guess is a dead end. **The pick moved from `localStorage` to a cookie** (`kp_landing_region`, `SameSite=Lax`, a year, `Secure` only where the page already is), which is the load-bearing part: the server can read a cookie, so a returning visitor who overrode the guess gets their currency in the SSR render — with `localStorage` they got the *geo* currency and a correction after mount, a visible price flicker on every visit, which is the exact defect the SSR read existed to remove. Precedence is now **stored pick → geo-IP → time zone → MY**, identical on both sides, and verified end-to-end against a running server (cookie `MY` + header `SG` renders RM on the first byte; a junk cookie falls back to the header rather than becoming a third state). Deliberate asymmetry in the two parsers: the **header** is normalized case-insensitively (it is Cloudflare's value), the **cookie** is case-sensitive (we write it, so a lowercase value means something else did). The cookie is a functional preference the visitor asked for by clicking — first-party, no PII — so no consent banner; not `HttpOnly`, since `setRegion` is its only writer. **`/cost` joined the two pricing surfaces**, which is where the real work was, because the calculator was Malaysia-hardcoded in three layers and only the third was cosmetic: it carried its own `FOUNDING_PRICE_RM = 104` (a second copy of the Pro founding price, now derived from `FOUNDING_MONTHLY_PRICES` with a test pinning the identity), a `LABOR_RATE_RM_PER_HR = 25`, and an AOV slider shaped in ringgit. The SG labour rate is **S$15/hr — deliberately not an FX conversion**: RM25 converts to about S$7, which reads as implausibly cheap labour to a Singaporean and would quietly undercut the chase-cost half of the whole argument. The SG slider is **max S$200, step 2, default S$15**. `computeStatusQuoCost` now takes the currency as a **required** argument and every currency-shaped value is an exhaustive `Record<BillingCurrency, …>`, so a third billing currency is a compile error rather than a silent Malaysian fallback (the `Record<Country, …>` posture from SG-lite). Because the region can resolve *after* a shared `?aov=400` link is opened, the calculator holds only what the visitor actually stated and derives the rest — untouched fields follow the new region's defaults, entered ones are `clampInputs`-fitted into the new slider — which is a pure derivation, no effect and no ref. **Two bugs the pass surfaced that the ticket never mentioned**, both found by rendering the SG page and reading it: the landing anchor line said *"Most WhatsApp order tools charge RM 200–500/month … Starter from RM 79/mo — less than RM 3 a day"* directly above S$29 tier cards, and the Scale card said *"Additional outlets RM49/mo each"* beside S$119. Currency had been spelled into the message catalogs, where no region logic can reach it. Both keys now take a formatted amount as a placeholder (en + ms + zh in lockstep) and the surface derives it; `starterPricePerDay()` rounds **up** so "less than S$1 a day" can never contradict the card beside it. A guard test in `pricing-copy.test.ts` now fails on any currency literal appearing in `pricing_*` / `pricingpage_*` copy, because a one-time sweep does not hold — the same reason `currency-literals.test.ts` exists for source. **Deliberately left alone:** the illustrative RM amounts in the hero chat, the how-it-works mockups and the bento cards. Those are a fictional *Malaysian seller's* storefront in a screenshot, not Kedaipal's price and not the visitor's money; localising them would be worse, not better. The SEO description stays static and names both currencies — Googlebot crawls from the US, so a per-request `<meta>` would just make the indexed copy a coin toss. **Both SG numbers that had no prior decision — the outlet add-on (S$19) and the competitor band (S$80–200) — are marked UNCONFIRMED in `plans.ts`** and follow the tier ratio; Scale is still "Coming soon", so neither is load-bearing yet. See [`pricing.md`](./pricing.md) + [`sg-lite.md`](./sg-lite.md).

- **Order workspace depth — a column registry behind the export, a table view, item thumbnails, pinned orders** ✅ (ClickUp [`86eyrtz74`](https://app.clickup.com/t/86eyrtz74), dev) — four items from a Hermoolah vendor session, shipped together because they are one story: **the inbox wasn't yet a place a seller could live in**, so they exported to Excel. **(1) The export was missing more than reported.** "Categories and address are missing" was true, but the address gap was total — there was **no destination column at all**, for delivery *or* self-collect, so a seller building a despatch manifest was working blind. Auditing turned up an arithmetic hole the file's own comments already admitted: `computeOrderTotals` folds the mockup quote into `total`, and with no quote column `Subtotal + Pickup fee + Delivery fee` **silently failed to reconcile on every made-to-order order** — a bookkeeping export that doesn't balance is the definition of "something left out". `orderCsv.ts` is now a **column registry** (36 entries: key, label, group, width, accessor) rather than a fixed string array, adding the split address, pickup outlet, custom-work quote, payment reference, paid-on, fulfilment time, order type, marketing origin, cancelled reason and fee-pending. `Subtotal · Custom work · Pickup fee · Delivery fee · Total` are kept **adjacent** so the identity checks by eye, and a test pins the adjacency. **`Categories` is frozen per line at checkout** (`orders.items[].categoryNames`) after a first pass shipped it as a live lookup named "Categories (current)". Freezing wins on four counts: the table and CSV stop paying a junction fan-out per row, **search can cover categories at all** (a live lookup needs a per-product read per keystroke), a sales report stays true when the seller reorganises their catalogue, and it matches every other sale-time fact on the document. Per-line rather than a union on the order, because a union can't attribute revenue on an order spanning two categories and "sales by category" is the obvious next ask. Present means recorded, `[]` included ("filed under nothing" is a real answer); **absent** is reserved for orders that predate the field, which is what lets the backfill terminate. That backfill (`migrations:backfillOrderCategoryNames`) shipped only after the owner overrode the original "no backfill" call — correctly: it necessarily stamps **today's** categorisation, the one thing freezing exists to avoid, but a permanently blank column across a store's whole history is worse than an approximate one, and the approximation is bounded to orders placed before the feature existed. It is manual (`npx convex run`, part of the release, never automatic), idempotent, never re-dates a line that already carries a stamp, and never bumps `updatedAt` — recording metadata about a sale is not a change to the sale. Note the original "never freeze" rationale argued for the opposite of what it produced: a live lookup is exactly what lets re-categorising rewrite a past order. **`trackingToken` is excluded and a test enforces it** — it is the buyer's tracking-page capability and exports get emailed to bookkeepers. **(2) A table view**, desktop-only. The rationale is retention, in the vendor's words: they export *"just because they are too used to excel"*, and the inbox already filtered and sorted as well as a spreadsheet — it just didn't look like one. **Two views, not three** (the card list already *is* the list view). It renders from the **same registry** as the CSV, which is what makes "the table replaces the export" true rather than aspirational; needed **zero backend** (`searchOrders` already returned full docs); and the `lg` gate is `useIsDesktop()` in **JS**, not only CSS, because `?view=table` shared from a laptop would otherwise hand a phone a 36-column table with the toggle hidden. Column visibility persists per store in `localStorage` — not the URL (36 toggles would bury the shareable bucket/filter/sort) and not Convex (a display preference shouldn't cost a write or sync to a colleague). Export in table view becomes a **two-item menu** (visible columns / all columns) rather than a **dialog** — a modal on every export taxes a path sellers hit often. **(3) Line-item thumbnails** on order detail (variant image → product image → `AppImage` fallback, batched server-side): a packing aid, so the image is deliberately **not** frozen onto the order and a deleted photo degrades to the fallback box. **(4) Pinned orders** (`orders.pinnedAt`, `seenAt` posture, never bumps `updatedAt`). Three owner calls, all counter-intuitive: **never auto-unpin** (a delivered order can still be worth keeping on top — the system doesn't get to decide the seller is finished); **a pin outranks the filter** while the Pinned chip is on, because that is what sellers pin *for* — park an order on top, filter to something else, compare — implemented as a short-circuit in `buildInboxPredicate` so the export inherits it and the CSV holds exactly what was on screen; and **pinned-first is a partition, not a third sort option** competing with newest/due. **All-tier**: a one-bit annotation is not an inbox search feature, and gating it would leave a Starter watching pinned rows sort to the top of a list they were never allowed to pin into. The design risk was never the marker but **staleness** — pins that never clear turn the top of the inbox into a graveyard, the exact failure the nav-badge fix (`86eyjfazz`) existed to prevent — so the always-visible **Pinned N** chip (tallied over the full window, leading the chip row) is the standing reminder, and clearing is **Unpin** in the existing bulk bar rather than a new "Clear all" control. Known limit: a pin older than the 1,000-row scan window is out of reach. Three UI directions were mocked and **C (chip + boundary hairline) was chosen** over a tinted tray and an inline rail. **Two owner reversals landed on top of the first build:** the table is now available at **every width**, not `lg`-and-up (*"they might need a quick look while on the move"*) — so it scrolls inside its own container rather than the page, its row controls grow to 44px touch targets below `lg` (the checkbox's *visual* box staying 18px inside that target), and the **toolbar was re-laid-out (Option C of three mocked)** once five controls in one row squeezed the search input to its own padding — two of the three faults were bugs, not taste: search was `flex-1 min-w-0` against ~404px of `shrink-0` siblings on a 390px screen, and the column picker was `h-9` in a row of `h-11` controls, misaligned and under the touch floor. The fix is an IA split: the **view switch moved up into the page header** beside Select and Export (things you do *to* the list) while search/sort/filters stay together (things that *narrow* it), and the **column picker moved down into the chip row**, right-aligned outside the scroller so a table-only control can't scroll off a phone — shaped like a `FilterChip` because it now belongs to that row — and the table was then **rebuilt on the shadcn `Table` primitives** (a new `ui/table.tsx`, real `<table>` markup, `<colgroup>` widths) **driven by `@tanstack/react-table`** — the two are complements, not alternatives: shadcn's table is markup with no logic, TanStack is logic with no markup, and shadcn's own "data table" is exactly this pairing. Row navigation follows `customer-list.tsx` — `onClick` on the row plus a real `<Link>` in the Order ID cell, since a `<tr>` can't be an anchor and that link is what keeps middle-click and keyboard nav working. Column layout then gained **drag-to-reorder — by dragging the HEADERS** (@dnd-kit horizontal, the header itself as the handle; the shared sensors' 8px/250ms thresholds mean a plain click still sorts. A first pass put the reorder list inside the Columns dropdown, which is nowhere anyone looks — the panel is show/hide only now. `resolveOrderColumns` honours the caller's order, so the **exported CSV comes out arranged exactly like the table**) and **typed per-column sorting** — columns carry an optional `sortKey`, because a lexical sort puts "125.00" below "86.00" and "3:30 PM" below "9:00 AM"; numeric columns open descending, empties always sink, pins stay on top of any sort AND sort within their own group — by partitioning `getSortedRowModel()` rather than using the table's row pinning, whose `getTopRows()` returns pinned rows in pinned-id order and would freeze the pinned block while the rest re-sorted; and the Sort popover disappears in table view since every header is now its own sort control. **Search was widened to every column** in the same pass (`searchHaystack` over `ORDER_COLUMNS`) — address, courier, tracking number, payment reference, note, pickup outlet, origin, cancel reason — because a narrower search shape than the table renders is how "why can't I find that order by its tracking number?" happens; phone keeps its trailing-digit rule (substring can't match `+60…` from `123456789`) and categories participate too, now that they are frozen onto the order. One predicate serves both views, so cards and table search identically. **No list view was removed** — `origin/staging` has no view mode at all, only the card list; Cards and Table are the first two. A second vendor round then fixed three things the first build got wrong on its own terms, plus one it never got right at all — **the sticky header had never worked**: `sticky top-0` had been on it since day one, but CSS forces `overflow-y` to `auto` as soon as `overflow-x` is, so the scroll wrapper (not the page) was the sticky containing block and, unbounded, never scrolled vertically — the header simply rode away. Caught by probing it in a real browser rather than trusting the comment; fixed with a bounded viewport (`lg:max-h-[70dvh]`) at `lg` and up only, since below that it would mean a nested vertical scroll stacked on the horizontal one, a bad trade on touch. **Columns also became resizable** — drag the right border, 80–640px, double-click to restore the default, widths persisted per store beside visibility and order (own storage key: they change every frame of a drag, the key list changes on a toggle, and a shape mismatch must never drop the seller's arrangement). The **divider IS the handle**: one element rules the header into a grid and puts the grab target where a spreadsheet user aims, and sitting outside the sortable button is what stops a resize from starting a column drag. It is also a real ARIA **window-splitter** — focusable, `aria-valuenow/min/max`, ← → to nudge and Home to reset — which began as a lint error and ended as the right design, since a drag-only handle puts column width out of reach of anyone without a mouse. Headers themselves went from 11px `muted-foreground` to 12px `foreground/75` on a solid `bg-muted` at `h-11`. Then **filtering moved onto the headers**: a funnel on Status, Categories, Order type, Came from, Payment, Payment method (multi-select) and Fulfilment date (single — the due windows overlap, so a set would read as an intersection and behave as a union), each option carrying a **count tallied over the UNFILTERED window** so the picker never shrinks as it is used and a `0` explains an empty list. The generic half (`ui/column-filter-menu.tsx`) knows nothing about orders, so the next table reuses it whole. Header filters **mirror** the panel rather than replacing it — moving them would strip cards view of filters entirely and remove the only surface showing everything applied at once, so the panel gained matching Status and Categories sections and both write the same URL state through the same predicate. Two new server dimensions: `statuses` (exact status, multi — a separate dimension from `bucket`, which is the coarse stage you navigate by, not the precise one you question) and `categories` (ANY line matching, affordable only because categories are now frozen at checkout); `source` widened to `sources`, with the singular still folded in for old bookmarks. **The trap:** `searchOrders`'s Pro gate asked "is any filter set?" via a hand-maintained list of arg names, so adding three filters made it **fail open, silently** — it is now `narrowsTheInbox()` over a `Record` keyed off `InboxFilterArgs`, which stops compiling when that type grows. Same class as the `as InboxFilterArgs` cast on the export path, which would have dropped the legacy `source` without a word: a cast is not a conversion. **The Filters dialog was then rebuilt** on the same option row the header menus use (`ui/filter-option-row.tsx`) — two columns of sectioned rows with a count on every option, active selections as removable tokens above the fold, and a sticky apply bar carrying `Clear all (N)` + the live `Show N orders`, which could previously sit below the fold on a phone. Chips-in-the-dialog vs rows-in-the-header had made one idea look like two and taught the seller the same filter twice. The rewrite briefly dropped `methodChoicesFor`, which keeps a selected-but-unoffered payment rail switch-off-able — a test written for that exact dead end caught it. **The bucket chips then went multi-select too** — "Completed or Cancelled" in one view; "All" became the escape hatch (lit only while the set is empty) rather than a member, the wire keeps the old singular for deep links, and the Pro gate's `bucket !== "all"` special case retired into the same compiler-enforced key set. **A third trap only showed up in use:** every filter click rewrote the query args → new TanStack Query key → `data` briefly `undefined` → the whole list unmounted into a skeleton and back, so the page appeared to reload, the scroll jumped, and an open header dropdown was destroyed mid-selection — you could not pick a second value. `placeholderData: keepPreviousData` keeps the previous page mounted (and retires a `countsRef` hack that had patched the same symptom for the chips alone); filter changes switched from push to `replace`, since a multi-select built one tick at a time otherwise leaves six history entries and, with `scrollRestoration`, scrolls to the top on each one; and a zero-result filter now renders its empty state as a ROW INSIDE the table, because replacing the table removed the header filter the seller needed to undo what they had just done. Then the two reported faults: a **status pill that wrapped** — as a plain inline span, "Ready for Pickup" in a 116px cell broke its rounded background into two ragged fragments, so `StatusBadge` became `inline-block` + `truncate` + `max-w-full` (fixed on the badge, not the table, because every card and the detail page could reach the same defect) and the column widened to 148px; and a **view that wasn't remembered** — opening Orders from the nav dropped a table user back into cards every time, so the layout is now stored per-store beside the column layout (`useInboxView`). That makes `view` the one search param that writes **both** values instead of keeping the default out of the URL: absent now means "the view I was last in", so writing only `table` would leave a table-preferring seller unable to send, or pin, a cards link. and the **product export became a catalogue report too** — 13 report columns after the 11 unchanged import-template ones (`categories`, `storefront`, `stock_policy`, `reserved`, `photos`, order rules…). The import ignores every report column, which would be a silent no-op, so `exportOnlyColumnsPresent()` makes the import screen name them; the two lists live in separate modules and a test asserts they match. Which variants export changed as well: a variant now exports when active **or** when it carries data the seller put there (SKU, price or stock), which finally surfaces manually deactivated variants while still excluding the auto-filled cartesian gaps — exactly `{no sku, price 0, stock 0}` — that would otherwise drag fourteen blank rows into a 5x4 product. See [`order-inbox.md`](./order-inbox.md) + [`invoices-receipts.md`](./invoices-receipts.md) + [`bulk-product-upload-roadmap.md`](./bulk-product-upload-roadmap.md).

- **Booking bundle S7/7 — fixed-length packages + instant book (FS Fitness)** 🚧 (ClickUp `86eyqxb14`, stacked on S6) — the built S1–S6 bundle assumed a campsite: a free check-in/check-out range, per-night × nights pricing, and request-to-book approval. **FS Fitness & Martial Arts** (10th payer, first at full-price RM 149, 23 Aug) is none of those — it sells **month packages**: pick a start date, fixed length, one flat price, no approval. Three product-level presets close the gap with **no new kind and no behaviour fork** (spec decision 5). **`products.booking.packageDays`** (1..366, 0 clears) → the buyer calendar becomes a **single start-date pick**, the end derives, and the line is **`quantity: 1` at the flat price** instead of `quantity: nights`; both shapes ride the same `computeOrderTotals` math so totals/CSV/PDF/Insights/receipts need zero special-casing. **`autoAccept`** ("Instant book" — the spec's named follow-up) → the order lands **`confirmed`** with the confirmation push firing at create through the exact storefront machinery (stamps, retries, webhook correlation) and the activation stamp made, skipping `booking_requested`; env unset ⇒ still confirmed and payable, silently (the S3 approve posture). **`capacityPerNight` became optional — unset = UNLIMITED**, because a gym has no daily member cap; the three former `?? 1` fallbacks (`findFullNights`, `sellerCalendar`, `orders.get.bookingContext`) now pass the absence through, since `?? 1` would have read "unlimited" as "one spot" and refused the second member. **The subtle fix: the capacity scan's look-back bound.** `countBookedPerNight` looked back `MAX_BOOKING_NIGHTS` (30) days — correct only while 30 nights was the longest stay that could exist. A 366-day package starting 60 days before a window genuinely overlaps it, and the indexed scan would have **missed it and reported the night free**; the bound is now `MAX_BOOKING_SPAN_DAYS = max(MAX_BOOKING_NIGHTS, MAX_PACKAGE_DAYS)`, deliberately global rather than per-product (a listing's `packageDays` can be edited or cleared after long bookings were placed against it, so only a ceiling over every shape is safe) — mutation-tested red. **One derivation author**: `resolveBookingRange` serves the calendar, the checkout preview and the mutation, and `requestBooking`'s `checkOut` arg is now optional and **ignored for a package** so a tampered client can't buy 90 days at the 30-day price; the order freezes **`orders.bookingPackageDays`** so a later listing edit never re-describes a placed booking. **Copy:** a package is a validity WINDOW whose last usable day is the night before the exclusive check-out — "Valid 1 Sep – 30 Sep", never "– 1 Oct" — via shared `describeBookingSpan` + `bookingPriceSuffix` across card, product page, checkout receipt, seller order detail and the buyer's "Your package" card; instant-book listings read "Book now" / "Total" and never promise an approval that isn't coming. **`ToggleSwitch` extracted** from `fulfilment-tab.tsx` into `ui/toggle-switch.tsx` rather than copied. **Not in S7:** hourly slots (same primitive, smaller unit — but day-granular MYT midnights make sub-day real work, still unticketed), the "Active" inbox bucket (S8 `86eyqxb2q`), counter sign-up (S9), renewal nudge (S10); packages roll from the start date rather than locking to a calendar month — the ticket's open question for the onboarding call. See [`docs/booking.md`](./booking.md).
- **Booking bundle S6/6 — ICS calendar feed + Settings connect card** 🚧 (ClickUp `86eyn4kf2`, stacked on S5 — the full S1→S6 chain now lives on this branch) — the vendor's Google Calendar view, **one-way ICS subscribe locked 12 Aug** (OAuth push deferred until a seller concretely complains about staleness; two-way out until a real double-booking; the route only READS, so a feed failure can never touch an order). **`retailers.calendarFeedToken`** (+ index) is the whole capability (`/track` posture, counter-QR rotate precedent): `ensureCalendarFeedToken` idempotent (rendering never rotates), `rotateCalendarFeedToken` kills the old URL (card warns first). **`GET /cal/<token>.ics`** (Convex HTTP action) → `feedByToken`: 90d-back/210d-forward window on the availability module's own indexed scans; **approved stays only** (requests deliberately excluded — a 24 h request clock against Google's ~daily refresh renders dead noise; the live Kedaipal calendar is the request-time truth and the card says so) + **blocks as all-day "Blocked" events** (listing-scoped name + note). Titles = guest name — listing, **never a phone number** (feed URLs get forwarded; pinned by test). Pure **`convex/lib/icsFeed.ts`**: RFC 5545 escaping, 75-octet folding, MYT floating `VALUE=DATE` days, **exclusive `bookingCheckOut` ≡ exclusive DTEND** (maps 1:1; inclusive block ends get +1 day), stable UIDs/DTSTAMPs/sort so unchanged data is byte-identical and Google updates in place. **Settings → Bookings** (new tab, Selling group, between Fulfilment and Order status; **filtered from both navs for non-booking stores**, deep-link renders the empty state): auto-provision on first view, copy-URL pill, 3-step Google walkthrough, the in-card freshness caveat ("Google refreshes on its own schedule — usually within a day; your Kedaipal calendar is always live"), rotate behind a consequence-naming confirm. See [`docs/booking.md`](./booking.md).
- **Booking bundle S5/6 — security deposit end-to-end** 🚧 (ClickUp `86eyn4kee`, stacked on S4) — Sengloh's refundable damage hold, collected ON TOP of the stay price **in the one payment at approval** (distinct from the parked partial-payment deposit 86eyhwb03). **Held money, never revenue.** Schema: `products.booking.securityDeposit` (sen, `sanitizeSecurityDeposit` 0..RM10k, 0→unset; the wizard/edit field ships HERE not S1 — an input that does nothing is worse than hidden) → **frozen `orders.securityDeposit`** at request (policy edits never change a placed booking, pinned by test), folded into `total` via the `computeOrderTotals` **extras seam** — so the S3 approve message needs zero change and no new payment states exist. **`revenueExcludingDeposit`** (`convex/lib/order.ts`) is the one author of the revenue figure: CRM `totalSpent` (link/cancel-decrement/move/backfill) and Insights (earned/trend/collected/payment slices) subtract it; recompute-delta sites need no netting (the deposit is frozen, so it cancels out of `total - order.total`). **Stated BEFORE requesting** (product-page line + booking-checkout receipt row + return-named footer) and **lined on every money surface after**: buyer/seller totals (EN/BM), the S3 request card ("incl. RM100 refundable…"), PDF "Security deposit (refundable)" row, CSV "Security deposit" column (0.00 never blank — subtractable). **Return tracking:** `bookings.settleSecurityDeposit` (owner-or-admin, one-shot; **delivered + paid only** — before check-out nothing is returnable, and a cancelled-after-payment booking settles the WHOLE payment in one refund conversation, which both order pages state as context; keep ≤ deposit, reason REQUIRED on keep, quoted verbatim) stamps `securityDepositReturnedAt`/`KeptAmount`/`KeptReason`. Seller `SecurityDepositCard`: amber "RM X to return" at check-out (the card IS the v1 reminder, no cron), Mark-returned / Keep-part dialog, settled outcome note; buyer tracking mirrors it (mint full-return / amber kept w/ reason blockquote + remainder). **Divergence:** the ticket's settle-time WA note joins the S3 Meta-template follow-up — booking guests hold no session window days after check-out (they never message the WABA), so v1 puts the outcome on the page both sides already hold. See [`docs/booking.md`](./booking.md).
- **Booking bundle S4/6 — seller calendar + block days** 🚧 (ClickUp `86eyn4kdb`, stacked on S3; mergeable with the core chain or right after it) — the seller's month view, and the calendar's ONE write. **Blocks join the one availability authority:** new `bookingBlocks` table (`retailerId`, optional `productId` = whole store when unset, `startDate`/`endDate` MYT midnights **end-INCLUSIVE** — a block has no leaving morning, unlike checkOut — note ≤200, `by_retailer_start`), and `findFullNights` now unions **capacity-full OR seller-blocked**, so `requestBooking`'s atomic gate, the public buyer calendar and the seller month view can never disagree — **blocked ≡ full is structural** (binary `unavailable[]`, a buyer can't tell closed from booked-out; pinned by test). Blocks stop **NEW requests only** — existing stays are untouched (handled on the order, where decline/cancel carries reasons + notifications; a block silently killing a paid stay would be indefensible) and overlapping rows are tolerated + unioned at read (merge-at-write would make unblock ambiguous; unblock is idempotent). `convex/bookingBlocks.ts`: `blockDays` (owner-or-admin, soft-locked, act-as audited, scope must be a booking listing), `unblock`, `sellerCalendar` (≤92d windows; per-day `{booked, blocked}` + block rows + listings; **`capacityPerNight` only when listing-scoped** — the all-listings view shows bare counts because summing capacity across products is fake math), `dayBookings` (the tapped night's stays → order links), `hasBookingListings` (gates the toggle). **UI:** `/app/orders/calendar` + `OrdersViewToggle` — an Inbox · Calendar segmented Link control in the Orders header (both breakpoints), only for booking stores, and deliberately **outside the `inboxEnabled` Pro gate** (booking is all-tier); zero-listings deep-link gets a what-this-is empty state. `SellerBookingCalendar`: Monday-start month grid (count pills `N/M` scoped / bare `N` all-listings, striped+`Ban` blocked cells, today ring, past dimmed, −12/+6mo nav, single-listing stores auto-scope so the honest denominator shows), **two-tap blocking** (armed → first tap start → later tap end; the confirm sheet holds scope — whole store vs the filtered listing — a note, and the stops-new-requests-only consequence line), and a day sheet (covering blocks w/ per-listing name + Unblock, that night's bookings → order page, "Block this night" shortcut). Calendar-is-a-lens: block/unblock is the only write on the page. **Not in S4:** buyer-visible "blocked", blocks cancelling bookings, ICS (S6 reads these same rows), drag-select/week view/recurring blocks. See [`docs/booking.md`](./booking.md).
- **Booking bundle S3/6 — approve/decline + buyer request states** 🚧 (ClickUp `86eyn4kcn`, stacked on S2 — the S1→S2→S3 chain now covers the full request→approve→pay→stay flow and merges to staging together) — the seller half. **Two exits, cause-true everywhere:** `bookings.approveBookingRequest` (`booking_requested` → `confirmed` via the one transition path, then the guest's ONE message = the **existing confirm template + push machinery reused wholesale** — stamps/retries/webhook correlation inherited, button lands on the payable order page, total always final for bookings; env unset ⇒ approve works, page payable, no auto message — the storefront's own legacy posture) and `bookings.declineBookingRequest` (reason REQUIRED ≤200 chars, quoted verbatim; stamps **`bookingResolution` + `bookingDeclineReason`**, then the one cancel path releases the hold — the expiry cron stamps `"expired"` the same way). The resolution markers are what the buyer page and the seller's resolution note read. (They were also built to suppress the generic cancelled send — obsolete since **`86eyd63r8` deleted every automatic status send**; the approve path was already riding the one confirmation push that survived.) `assertStillRequested` = hour-25 approve reads "expired — ask the guest to book again". **Seller:** `BookingRequestCard` takes the stepper's slot (server refuses stepper moves): amber card, "Expires in Nh" countdown (red ≤4h — the cron's own clock), ticket-skin range+total, **capacity context line** ("2 of 5 spots already booked those nights" — seller-only `orders.get.bookingContext` off the availability module minus this order's own hold, never on the buyer path), Approve primary / Decline dialog (quick-reason chips + required free text); `BookingResolutionNote` keeps the why on the cancelled order; fulfilment card reads "Booking · 2 nights · 25 → 27 Sep"; **the booking stepper skips "Packed"** (synthesized stages filter the anchor — Confirmed → Checked In → Checked Out; configured stages untouched); `requestBooking` schedules the existing 86eyhw9zy seller-alert template (booking-worded template = Meta follow-up). **Buyer:** amber "Request sent" awaiting card (24h + nothing-charged, EN/BM) with the **payment card suppressed while requested** (an armed Pay button on a request is a lie); declined = reason blockquote + "Try different dates" → storefront (new `retailerSlug` on the payload); expired = the 24h explanation; approved = the normal payment card + a "Your stay" range card; the timeline's first node reads "Awaiting Approval". **Explicit divergence:** the spec's request-ack/declined/expired buyer WA pings need Meta templates that don't exist yet (no session window) — v1 ships the STATES on the page the buyer already holds; templates are the registration follow-up with Arif (the approve message needs no new template). See [`docs/booking.md`](./booking.md).
- **Booking bundle S2/6 — availability module + buyer calendar + request-to-book** 🚧 (ClickUp `86eyn4kbw`, stacked on S1 — the chain merges together) — the buyer half of the Sengloh booking flow. **A booking is an ORDER created by its own public mutation `bookings.requestBooking`** (not a branch in the 600-line storefront create — the two share almost no validation; all shared seams imported from the same libs): lands as **`booking_requested`** (the locked confirm-at-create carve-out; every manual write path guards it — updateStatus/advanceToStage throw "approve or decline", bulk SKIPS), `deliveryMethod: "booking"` (new literal + label preset "Checked In"/"Checked Out" EN/MS/ZH + calendar inbox icon), **`fulfilmentDate = checkIn`** (inbox sort/due-today/urgency free), `items[0].quantity = nights` at the per-night price (every money surface reads "listing × nights" unchanged), phone REQUIRED server-side (the request lifecycle lives on WhatsApp), no stock movement. The spec's `bookingRange` is **flattened** to `bookingCheckIn/bookingCheckOut/bookingProductId` so the capacity scan rides the new `by_booking_product` index (flagged divergence); **checkOut is exclusive** — a 25→27 stay sleeps 25+26 and the 27th takes someone else's check-in (pinned by test). **One availability authority** `convex/lib/bookingAvailability.ts` (holders = every non-cancelled booking order — a request soft-holds from the moment it exists; horizon 180d, max stay 30 nights = the scan bound, TTL 24h) imported by BOTH the atomic in-mutation check (Convex serializes — the last-spot race can't double-book) and public **`bookings.availability`** (BINARY `unavailable[]` — counts never cross the public wire; null for any not-publicly-bookable listing; MYT-aligned ≤92d windows). **Expiry** = 15-min cron cancelling >24h requests via `applyStatusTransition` (one cancel path = hold release + reversals + timeline); the New bucket's 4h/24h age escalation doubles as the seller's countdown (`booking_requested` joins the "new" bucket + risky set). **Buyer surfaces (Variant A, locked 16 Aug):** `/{slug}/checkout?booking=<productSlug>` → `BookingCheckoutForm` (Who's booking → the calendar → note; ticket-skin "Total when approved"; disabled-with-reason **"Request to book"**; "Nothing is charged now" + "confirms within 24 hours" at the commit point; success → `/track/<token>` with NO wa.me/?send). `BookingCalendar` on the themed DayPicker with selection logic PURE in `src/lib/booking-dates.ts`: Monday weeks, struck-grey unavailable (full ≡ blocked), two-tap range, **checkout-only handling** (after check-in, the first full night stays tappable dashed as the leaving morning — drop it and the night before a full weekend is unsellable) + the amber ceiling explainer; availability windows follow month nav on stable MYT anchors. Product card/page: "RM80/night", CTA **"Book"** → page → one door to the booking checkout; **a stay never enters the cart**; `listForCounter` filters booking kind (no date UI at the counter). S3 = approve/decline + tracking states + the WA set (why the chain merges together). See [`docs/booking.md`](./booking.md).
- **Booking bundle S1/6 — product kind + booking listing setup** 🚧 (ClickUp `86eyn4kap`, spec `86eyj70z1`, design `86eym0pjg`; S1→S2→S3 are a stacked chain that merges to staging TOGETHER — S1 alone would ship creatable-but-unbookable listings) — the foundation of the Sengloh booking vertical. **`products.kind`** (`"physical"|"service"|"booking"`, optional, unset = physical — an explicit `"physical"` normalizes to unset so the default has one spelling; **immutable after create**, `update` has no kind arg by design — a kind flip on an ordered product leaves orders whose semantics don't match the row; archive + recreate is the escape hatch) + **`products.booking {capacityPerNight}`** (a plot IS a product; "Standard Plot ×5" = capacity 5; kind ⟷ config pairing enforced BOTH directions in `create`; capacity 1..100 via shared `convex/lib/productKind.ts`, a fat-finger guard not a product limit; lowering never cancels existing bookings) + **`retailers.storeType`** (same enum; ONLY pre-selects the wizard's kind card for NEW products — Settings → Store card under the description, three cards not four since Food & physical share one stored value, tap-again-to-clear). **Wizard goes kind-first**: new step 0 "What are you selling?" (Food/Physical/Service/Booking cards + one-line examples, "Your store type" badge on the pre-answered card; **Food is a router, never stored** — lands as physical and re-words the existing preparation question). Step ids stay stable; the walked route changes: **booking = `[0,1,3,5]`** (no Choices — one implicit `blockWhenOutOfStock:false` variant; no Preparation — request-to-book IS the preparation), whose step 3 renders as "Price and capacity" (per-night price + "Spots available each night" + min notice **promoted from More options**, reusing `products.minNoticeDays` verbatim); review speaks booking words ("RM 80/night", capacity row), More options collapses to just the full-editor handoff (no mockup/custom/minQuantity — one request = one booking). **Edit form**: kind displayed never edited; booking swaps the VariantEditor card for a compact per-night-price + capacity card; summary strip reads "Booking · 5 spots/night · RM 80/night". Handoff carries kind + capacity losslessly both ways. **Deliberately zero buyer-surface change** (booking products render as normal products until S2's calendar — the stacked-merge rule exists for exactly this) and the security-deposit FIELD ships in S5 with its machinery, not here (an input that does nothing is worse than hidden). Build tickets S1–S6: `86eyn4kap`/`kbw`/`kcn`/`kdb`/`kee`/`kf2`. See [`docs/booking.md`](./booking.md).
- **Claim links — seller keys the order, buyer completes address + payment via WhatsApp link** ✅ (ClickUp `86eyq0epn`, dev) — the TikTok Live funnel (Wagyu Walid, Her Moolah): the seller keys each "mine" claim in Counter Checkout (incl. per-line price overrides, `86eyphh8r`) and **sends the rest of checkout to the buyer** — a WhatsApp link to `/claim/<token>`, a pre-filled **price-LOCKED** checkout where the buyer adds address, date/time and (on the order page next) payment. **A claim is an OFFER**: new `orderClaims` table freezes the lines at send (the session draft is a non-authoritative scratchpad whose prices re-resolve at create, so "locked" needs its own snapshot — pinned by test against a catalog re-price), stock is pre-checked at send but **decrements only at buyer commit** (no reservation until 86eybbxhf; sold-out-at-commit names the line, never drops it silently). **The checkout timer (Zaki's window — revised 27 Aug to the Agoda model)**: a **fixed deadline** picked per send (15 min / 1 h / 24 h chips; the choice becomes the store default `retailers.claimLinkWindowMinutes` — the dialog says so, no hidden Settings card) that runs **until real money**: before commit it gates completion; at commit it carries onto the order as **`orders.paymentDueAt`** (floored to a 15-min payment runway) with the buyer's order page showing the continuing countdown, and a 1-min sweep **auto-cancels a due unpaid order** (stock restored via the shared `applyStatusTransition`, `cancelledReason` stamped so the buyer's page explains itself). One predicate (`isAutoCancelDue`) holds every protection: an "I've paid" claim PAUSES the clock (a human verdict must never race a robot — cancelling a true manual-transfer claim was the rejected outcome), fee-pending suspends it (the buyer can't pay), a live HitPay session shields it AND the mint extends the deadline (visible clock jump — a freeze was rejected as exploitable), and a seller who advanced an unpaid order keeps the robot out. **Resend never resets the clock** and is guarded per claim (5-min cooldown, max 3 sends, `claimResendState` — server rule + disabled-with-reason button from one author). Expiry judged live on every read (`effectiveClaimStatus`), flipped by a 5-min cron, dead claims purged after ~30d (PII). **Commit** (`orderClaims.commit`, public token-auth, `claimCommit` + the retailer-keyed `orderCreate` limiter pair) runs the **storefront validation set** (address arm by country, notice floor = store ∨ strictest line, opening hours, live stock; delivery fee via the now-exported `resolveDeliveryForOrder`/`loadCheckoutDeliveryQuote` — one author with `orders.create`) but **skips min-order rules and the mockup gate** (seller keyed the cart, price agreed in the live — counter posture, both pinned); order lands `orders.source: "claim"` (union widened; inbox filter + "Claim link" chip) with the full storefront fan-out incl. the confirmation push. **Idempotent** — a re-commit / reopened link hands back the same order. **One live claim per session**: a re-send supersedes the old claim, and a counter sale or session dismissal cancels it (`cancelOpenClaimsForSession`) — one cart, never two doors. Buyer page = variant-A sticky timer bar ("Price locked for 14:32", shared `formatCountdown` extracted from the Lalamove card into `src/lib/countdown.ts`) over a slimmed storefront checkout reusing `AddressFieldset`/pickup picker/date-time machinery (`claimFormSchemaFor` = checkout schema minus the phone — the claim froze the number the link was sent to); expired/cancelled = calm dead-end + prefilled wa.me to the seller; Clerk-free (`BUYER_ROUTE_IDS`). WhatsApp leg = **`utility_template`, deliberately NOT transactional** (ticket AC: `canSend` applies — an offer, not an order event), env-gated `WHATSAPP_CLAIM_LINK_TEMPLATE` with button base `kedaipal.com/claim/{{1}}` via **Add variable**; a blocked/failed send stamps the claim so the claims list offers **Copy link** — the fallback that always works. Seller surfaces: "Send to buyer to complete" on the counter build screen (identified buyers only, disabled until every line priced) + a "Waiting on buyers" panel (live countdown, Resend/Cancel/Copy, completed → order, expired → fresh link). **Marketing origin**: the send dialog asks where the buyer came from (TikTok Live / Instagram / WhatsApp), frozen on the claim and carried onto the order's `attributionSource` so a live drop's revenue counts against the live rather than "Direct" — **seller-chosen, never derived** (staging's attribution work reserved the `tiktok-live` bucket for claim orders, but a claim link also serves DM quotes and phone orders, so hardcoding it would mislabel most of them), and remembered per store like the window. Window chips gained **10 min**; 5 stays a bound and not a chip, because the payment runway floors payment at commit + 15 min, so at a 5-minute window the runway dominates entirely and the chip would promise urgency the system doesn't deliver. **The counter panel was then rebuilt mode-first** (Zaki's call, from a three-direction design canvas): *"How is this order paid?"* is a segmented control above the blocks it governs — counter sale keeps collection + payment, send-to-buyer **hides both and says so** ("nothing you key here would reach them"; an unexplained absence reads as a bug, and keying them then tapping Send was the original confusion), with the window + origin chips moved **inline** so `SendClaimDialog` could be retired entirely. Exactly ONE primary now, labelled per mode (`Review order · RM 20.00` / `Send link · RM 20.00` — the money is what a send commits, and the total above it reads **"Locked total"** since the buyer adds delivery on their own page). An anonymous cash sale disables the Send segment with its reason rather than hiding it. Mode defaults to counter sale (~90% of traffic, today's zero-tap flow) rather than forcing a pick. The parts that are RULES — which mode shows payment controls, what the primary says, why it's disabled — were extracted to the pure `src/lib/counter-panel.ts` and unit-tested, since the panel itself is a 2,600-line route component; that seam immediately caught a real copy bug (a nameless buyer rendered "They gets the link"). **Zaki's local test then closed six more (27 Aug).** *Panel scroll*: the checkout column is now a **bounded** `lg:max-h-[calc(100dvh-3rem)]` flex column whose body scrolls and whose Total + primary stay pinned — sticky alone meant a panel taller than the screen hung off the bottom, so the wheel dragged the whole catalog before reaching the panel's own footer (the cart list's `max-h-72` drops at `lg`: one scroll region per context). *Mobile gutter*: the mobile grid track is pinned with `grid-cols-[minmax(0,1fr)]` — an implicit `auto` column is floored by its widest child's min-content, so the custom-price `<input>`'s intrinsic ~20ch widened the whole grid and pushed both cards past `main`'s right padding (verified 0px overflow at 375). *Toggles*: both two-up questions became a stacked `ChoiceCard` list — outline + tint + check, the same grammar as the window/origin chips — because a solid accent slab holding two short labels reads as a banner, and `sm:grid-cols-2` went two-up exactly where the 380px panel is tightest. *Order freeze*: `isPaymentWindowLocked` (a `paymentDueAt` on a live order not yet `received`, deliberately time-free; `claimed` still locks) closes seller **Reschedule** server-side and turns its trigger into a tappable **Locked** button that opens onto the reason — the receipt is out and the buyer may be mid-payment, and on a rider booking a date move re-prices the trip; everything that helps payment land (fee, mark-received, cancel) stays open, and Cancel + re-send is the stated escape. *Resend*: now a **retry, not a nudge** — `claimResendVisible` shows it only on `failed`/`blocked`, hides it on `sent` (nothing to retry) and on `opted_out`/`unavailable` (guaranteed to fail identically), because every send is billed from 1 Oct and Copy link is the free, better answer to "I didn't get it"; the cooldown + 3-send ceiling stay as the server rule. The claims row also relaid out — one line on desktop with natural-width actions instead of three buttons stretched across the dashboard. *Sold-out (pre-existing, fixed in passing)*: the counter catalog now **states stock before the cart** — new pure `src/lib/counter-stock.ts` gives Sold out (red, Add disabled + relabelled) / Only N left (amber) / N left / Made to order, a stepper capped at on-hand so the ceiling is found by feel rather than by a create-time error, and a SOLD OUT chip + veil on rows and tiles that stay tappable (the seller still needs to see WHICH size ran out). **The freeze then went pre-commit too (27 Aug, Zaki's second pass)** — the bigger hole was on the other side of commit: reopening a checkout whose link was still out let the seller edit the cart (which **silently diverged**, since the buyer's page renders the frozen snapshot — the seller believes a change landed that didn't) or ring it up at the counter (which **cancelled the buyer's link mid-form**). A counter checkout is now read-only for as long as its claim is live: `liveClaimForSession` judges live with `effectiveClaimStatus` (an unswept expired claim never holds a checkout hostage), `saveSessionDraft` + `createOrderFromSession` refuse server-side, and opening it renders the new **`WaitingOnBuyerScreen`** — the claim's frozen lines, countdown, locked total, Copy link, conditional Retry, and one confirmed way out ("Cancel link & edit this order", which is also what tells the buyer the offer was withdrawn; no navigation, the live subscription flips the screen back). The lock is stated, not just enforced. `listOpenSessions` **parks** such a checkout out of "Open checkouts" — it isn't cashier-workable and it already lives under "Waiting on buyers", so listing it twice put a dismiss button beside a live offer. Copy/Retry/failure-note were extracted to `claim-actions.tsx` so the list and the screen can't drift. **PR review then closed three (28 Aug).** *(HIGH)* `/claim/<token>` was excluded from Clerk (`BUYER_ROUTE_IDS`) but **not from GA/Clarity**, so the buyer's capability token — the stronger of the two, since `commit` writes an order and decrements stock — was being exported to both vendors along with a session replay of the checkout; `isTrackingTokenPath` is now `isCapabilityTokenPath` and covers both `/track` and `/claim`, with a test that a slug merely starting with the word isn't swallowed. *(MEDIUM)* `paymentDueAt` was cleared on `cancelled` only, so a seller advancing an unpaid claim order to Packed — which is allowed, and is why `isAutoCancelDue` has its "seller advanced it on purpose" arm — stranded the row forever: the buyer's page permanently threatened a cancellation that could never happen, and the `by_payment_due` range grew without bound, contradicting the invariant the schema states outright. New shared `paymentDeadlineApplies` is the single author for sweep, freeze, track-page gate and **both** status writers — including `advanceToStage`, which patches `status` on its own path and which the review didn't name, so a custom-stage store would have kept leaking. *(MEDIUM)* the buyer-facing claim surfaces rendered name + phone with no `MASK_PII` (Clarity's Balanced mode records every rendered string verbatim) and none of the four claim files were in the mask-sweep table — both fixed. Also took the non-blocking notes: the `GATEWAY_SESSION_GRACE_MS` docblock now admits the grace is renewable per mint rather than claiming "never an indefinite freeze", and two stale comments claiming a 24h window fallback now point at `DEFAULT_CLAIM_WINDOW_MINUTES` instead of restating a number that had already drifted. See [`docs/claim-links.md`](./claim-links.md) + [`docs/counter-checkout.md`](./counter-checkout.md).
- **One WhatsApp per order — the confirmation is the only automatic buyer send** ✅ (ClickUp [`86eyd63r8`](https://app.clickup.com/t/86eyd63r8), dev) — Meta bills **every delivered message from 1 Oct 2026**, and the lifecycle we'd built (confirm + packed + shipped + delivered + payment-received + mockup + reminders + POD photos + the counter receipt PDF) cost ~**RM0.34/order** ≈ **37% of Starter's RM79** at ICP volume. Gross margin on the wedge plan doesn't survive that. The ticket's per-plan budget table (3 for Starter) was **superseded by one, every plan, every product type**: a budget that varies by tier makes the *buyer's* experience depend on what their seller pays, and "3 messages" still leaves the seller guessing which three they just spent. **The push fires at `orders.create`, always** — no deferral, no timing question. A held total (mockup quote outstanding, `deliveryFeePending`) rides in `{{3}}` as **words**: `PENDING_TOTAL_LABEL` — "to be confirmed" / "akan disahkan", store-locale keyed, chosen at **send** time from the live hold state, so a quote entered between create and send still goes out as a real number. It's a plain text param — the currency code already rides *inside* the value (which is why an SGD store renders "SGD 40.00", not "RM SGD 40.00"), so the body carries a bare `{{3}}`. **Two earlier designs were reversed, and both are worth knowing**: (1) *fire on mockup approval* deadlocked once `notifyMockupSubmitted` was deleted — nothing would tell the buyer a design was waiting, so nobody would approve and the gate would only open on the 48h waiver; (2) *defer, then fire at submit* (86eyfq0w5) fixed that but left the buyer with **no confirmation and no link** from checkout until the seller submitted — possibly days, possibly never — while the order page is the one thing they need, since it's where they approve the mockup. It also **bought nothing** (the price still isn't final when they read it) and cost a state machine: `claimDeferredPush`, five call sites, a `deferred` stamp, a cancel-clears branch in two places, and a buyer-facing card. Saying "to be confirmed" is honest, arrives on time, and **deletes all of it**. **Migration:** `deferred` stays a valid schema literal because rows may still be in it and **nothing claims them any more** — one-shot **`orders:releaseDeferredPushes`** (batched `.take(200)`, idempotent, re-run while `done:false`) must run per deployment or those buyers never get their message; it **sends real WhatsApp**, so it's a deliberate operator action. **Cancel now clears `sending` too**, not just `deferred`: the send action returns early on a cancelled order, so a `sending` stamp would be stuck forever promising a message that can't arrive — reachable far more often now every order starts `sending`. **Removed:** all status updates (packed/shipped/delivered/**cancelled**), custom-stage pings + the per-stage `notify` toggle + `MAX_NOTIFY_STAGES` (this ticket was named as the trigger to re-tune that cap — resolved by deleting it), payment-received, mockup-submitted, the three payment-due prompts, delivery-fee-set, the **day-11 reminder cron**, the counter receipt/invoice **PDF auto-send** (counter 2 → 1), and Lalamove **POD photos** (the only send producing N messages from one event). **The one buyer-side exception is seller-DRIVEN**: the manual payment reminder, hidden days 1–10, live days 11–14 once per 24h (max 4 by geometry), closed forever after — one pure `manualReminderEligibility` drives both the server lock and the button's disabled states. **Seller alerts are a different budget and a different consent** (retailer's own number) and gained a **third** event: a verified HitPay settlement, which previously notified the seller on **no channel at all** — there was no payment-received email either, so the only way to learn money had landed was to open the dashboard. It gets its **own** Meta template, deliberately not a reuse of the claim one whose params otherwise match: a claim asks the seller to check their bank, a settled charge already auto-confirmed the order and needs nothing — reusing it would manufacture work that doesn't exist. Two asymmetries with the claim alert, both deliberate: it is **NOT** fired by `markPaymentReceived` (that *is* the seller, clicking), and it does **NOT** skip cancelled orders (a claim on a dead order is noise; real money landing on one is the urgent case). **The template body names the gateway through `{{4}}`, never as literal text** — Meta freezes body copy at approval, so hardcoding "HitPay" would cost a fresh review cycle when a second gateway lands and, until it got one, would send a Billplz seller to a HitPay account they don't have, on a message whose whole value is *"go look here"*; `receiveGatewayPayment.provider` is **required** (an empty param makes Meta reject the send outright, and requiring it forces a future gateway to identify itself — it caught all seven call sites at compile time). The **env var** stays `WHATSAPP_SELLER_PAYMENT_RECEIVED_TEMPLATE` for the opposite reason: `WHATSAPP_` is a namespace, not a description — none of the nine existing vars name their subject, and there is no `HITPAY_*` namespace to join, since those credentials are BYO per-retailer. This puts the template on the side of the seam the schema already draws (`gatewayPaymentId`, `by_gateway_request` — never `hitpay*`). **Their duplicate emails now self-suppress** via pure `sellerWaAlertWillAttempt`, and the WA action schedules the email back with `force: true` when it gives up — never both, never zero; the WA actions deliberately **don't** force on their own early returns (template unset / toggle off / no number), since the email shares that exact predicate and has already decided not to suppress. **Fixed in passing:** the inbound-`ORD-` **double-send leak** (an old `?send=1` link or a forwarded chat earned a second free-form confirm — now `pushOwnsTheMessage`, with `failed` still replying since that push reached nobody, and `pushRecovered` threaded explicitly because `confirmOrderFromWhatsApp` stamps `recovered` *before* the guard re-reads); the BM `paymentClaimed` subject that read "Pembayaran diterima" (= payment *received*) for an unverified claim, which would have collided head-on with the new sibling; and a **dead config** — sellers could edit "Unknown message reply" in Settings and it was never used in any locale. **`convex/oneMessagePerOrder.test.ts` is the regression gate**: it counts **scheduled** `whatsapp:*` jobs, not fetch calls, because the suite runs on fake timers and a fetch-counting test would read zero and pass vacuously; `NOT_BUYER_SENDS` is a named **subtraction** so an *unknown* new send still trips it, and the seller alerts are asserted **positively** so the exclusion can't rot into a hole hiding their deletion. Note for anyone extending it: "no new send" is asserted as the pending-job count staying **unchanged**, never zero — the create-time job never fires under fake timers. See [`docs/one-message-per-order.md`](./one-message-per-order.md) + [`docs/order-notifications.md`](./order-notifications.md) + [`docs/payment-reminder.md`](./payment-reminder.md).
- **Founder business report — Kedaipal's own weekly numbers** ✅ (dev) — Arif had no way to see the *platform's* health (MRR, who lapsed, signups) without hand-assembling it monthly for the `kedaipal-monthly-review` deck, which needs a manual screenshot of the `retailers` table. New secret-guarded `GET /internal/business-report` (`BUSINESS_REPORT_SECRET`, **prod only**; constant-time compare that **hashes both sides first** — unlike the HMAC verifiers, both operands here are the raw secret, whose length is itself secret) → `admin.businessReport` internalQuery → pure `convex/lib/businessReport.ts`. Fetched weekly by a scheduled routine that files a markdown report; **no in-app page** (founder artefact, not a product surface). **MRR is derived from PAID INVOICES, never `planPrice()`** — `subscriptions` has no currency field (currency is picked at issue time and lives on the invoice), so plan constants would count an SGD store's S$59 as RM59; reported **per currency**, never blended, rounded per-subscription so any figure reconciles against one invoice, and an active sub with no paid invoice surfaces as `activeWithoutPaidInvoice` rather than a silent 0. **Churn reads off `past_due`** (manual billing has NO cancellation flow — nothing writes `status:"cancelled"`, so a seller who stops paying just sits there) **but splits FOUR ways**, because a blended number would report losses that never happened: *lapsed customer* (paid before, invoice past due = real churn) / *awaiting payment* (pending invoice not yet due = healthy) / **awaiting your invoice** (paid before, no pending invoice = **Arif's action item**, the cron's Path C exists so a paid vendor never gets free service while waiting on an invoice) / *trial expired* (never paid). The `awaiting_payment` bucket is load-bearing: `issueInvoice` deliberately doesn't touch the subscription and only `markPaid` clears `past_due`, so issuing a renewal leaves the row `past_due` with a **future-dated** invoice — a 3-way classifier would manufacture phantom churn precisely when the right thing was done. `lapsedThisWeek` works with **no schema change** off an invariant worth knowing: for a row *currently* `past_due`, `updatedAt` IS the flip moment (the only other writers move the status away, and the trial-reminder patch deliberately skips `updatedAt`) — **a fourth `db.patch` on `subscriptions` touching `updatedAt` silently breaks it**. Comped rows are excluded *before* classification (the cron's trial path flips them too, unlike the other two) and surfaced as `compedExcluded`; `cancelled` is reported as a **structural** 0 so the number moves on its own the day a cancellation flow ships. Exclusions are three predicates (owner in `ADMIN_USER_IDS` — self-healing; `notifyEmail` fragment; explicit slug list) and **deliberately NOT "unset email"** — `notifyEmail` is optional and independent of the Clerk auth email, so that rule would drop real paying retailers from MRR/GMV; `excluded.slugs` ships in the payload so a wrong exclusion is detectable. Reads are N+1-free (**one** `by_status` collect per invoice status reduced to per-retailer maps, *not* the daily cron's per-subscription `by_retailer` read) and the order scan rides Convex's **system creation-time index** since every `orders` index is `retailerId`-prefixed; newest-first + bounded means a `capped` week under-reports its EARLIEST days, so `capped` must suppress publishing that week's GMV. **`.mcp.json` now defines TWO Convex MCP servers** so the deployment is legible at the call site: `convex` (dev full + prod readOnly) and `convex-prod` (`--prod --cautiously-allow-production-pii`, prod reads unlocked) — otherwise deployment choice is an opaque base64 `deploymentSelector` argument and a tool-approval prompt can't tell you whether an agent is reading dev or live customer data; now the tool NAME says it (`mcp__convex__data` vs `mcp__convex-prod__data`). This is what closes the spec's verification gap (reconcile the report by independent recomputation against prod). **Tools are pruned via `--disable-tools`, because the two prod flags expose an IDENTICAL tool list** (gating is runtime off each deployment's `readOnly` bit) and `--cautiously-allow-production-pii` flips prod to `readOnly: false` — which the CLI help calls read-only-tools-only but the `status` tool's own description says means *all* tools. Rather than resolve that contradiction by attempting a write against prod, `--disable-tools` makes it moot: **`run` off on `convex-prod`** (it invokes mutations AND actions, and our actions reach WhatsApp/Lalamove/HitPay; `runOneoffQuery` is the sandboxed read path, and `run` stays on `convex` since dev mutations are legitimate), **`envGet`/`envList` off on BOTH** (deployment env vars hold `WHATSAPP_APP_SECRET`, the credential-encryption key, HitPay salts — a worse leak than the buyer PII this guards, and one that would land in a transcript; use `npx convex env list --prod`), `envSet`/`envRemove` off, `--dangerously-enable-production-deployments` never set. Deliberate prod writes go through `npx convex run --prod` in a terminal, attributable to a person not an agent turn. **Committed, so it applies to every session in the repo incl. subagents** — prod holds PDPA-relevant buyer data. Replaced, not extended, when HitPay recurring billing (`86eyb6z4r`) brings real cancellation. See [`docs/founder-business-report.md`](./founder-business-report.md).
- **Lalamove sandbox keys are now visible — and a failed booking can't trap the seller** ✅ (ClickUp `86eypncfy`, dev) — Wagyu Walid reported "I topped up and still can't book"; prod forensics found **nine** `ERR_INSUFFICIENT_BALANCE` failures over 26h/3 sessions on `ORD-W4AH` before he shipped it by hand. Root cause: he was onboarded on **`pk_test_` keys** and nothing ever said so. `inferLalamoveEnv` routed every call to the right host and then **discarded the answer** — so for two weeks a live storefront quoted buyers through Lalamove's **test environment** and charged those prices for real (RM27.40 on that order), his one "successful" booking was a simulated trip, and when the sandbox's play-money wallet drained our copy told him to *"top it up in the Lalamove app"* — a wallet the sandbox can never see. The only stored trace of the environment was the **host in `deliveryJobs.shareLink`** (`share.sandbox.lalamove.com`), and only on bookings that committed, so diagnosing it took a prod dig. **The credential-encryption backfill was ruled out** (encryption landed 17 Aug, the sandbox evidence is 8 Aug; the backfill is CAS-guarded and can't substitute a key; and a 422 with a real error id proves decrypt→sign→auth works — a mangled credential 401s). **(A)** New **`retailers.deliveryBooking.env`**, stamped at save from the **plaintext** key exactly like `apiKeyHint`/HitPay's `mode`, riding `DeliveryBookingSummary` + `getDeliveryJob` to the UI. Queries **never** derive it from a stored value — ciphertext doesn't start with `pk_test_`, so post-encryption inference returns `"production"` for everyone and would suppress the one warning it exists for; `undefined` (un-stamped row) renders as **nothing**, never a green "Live". One-shot **`credentials:backfillLalamoveEnv` must run wherever the encrypt backfill already ran** (those rows are ciphertext, so the encrypt pass can't stamp them) — it decrypts each key solely to read its prefix, never storing/logging/returning plaintext, and leaves `env` unset on an undecryptable key rather than guessing. **(B) Surfaced at every point of spend**, not just Settings — a sandbox booking looks identical to a real one until no rider arrives: `Test keys`/`Live` chip + consequence block in Settings → Fulfilment, amber "Test mode — no real rider" banner on the dispatch card, a compact line **inside the confirm dialog** above the price, and sandbox-aware failure copy that explains the test wallet instead of asking for a top-up. **Deliberately NOT blocked** (Zaki uses sandbox keys on prod for testing — visibility problem, not authorisation). **(C)** `friendlyBookingError` now classifies via **`classifyBookingFailure`** keyed off Lalamove's error **id** (`parseLalamoveErrorCode`, the source `isOutOfServiceAreaError` already used) instead of lowercasing the whole body and matching `"insufficient"|"balance"|"credit"` anywhere in it — their free-text `message` rides beside the id, so any failure merely *worded* that way was reported as a wallet problem; text sniffing survives only for bodies with no parseable id, and a recognised-but-uncopied id stops at `unknown` rather than guessing. Live-wallet copy also now names the **Business account the API keys belong to** (a personal-app top-up credits a different wallet). **(D) The dead quote**: a quotation lives **exactly 5 minutes**, but `handleConfirm` toasted the failure and returned **without clearing `quote`**, leaving the dialog open on it — so pressing "Confirm & dispatch" again re-sent an expired `quotationId`, while the copy pointed at a "Book delivery" button sitting *behind the modal overlay*. Proven in prod: **5 presses on ONE quotationId across 5m23s**. The reason now renders **inside** the dialog (a toast is gone before the seller is back from the Lalamove app) and the primary action swaps to **"Get a fresh price"** — an in-place re-quote — whenever a confirm failed or the quote lapsed; `prepareBooking` returns the `expiresAt` it always parsed and threw away, driving a live `Price locked for 4:32` countdown with a 5-minute client fallback. **(E) Accidental dispatch guard** (`86eypjfuf`, Arif's tablet report, same order): the reported mechanism **doesn't hold** — a Motorcycle/Car tap only *quotes* (no `deliveryJobs` row, no spend; `reserveBooking` is reached only from `confirmBooking`), the nine attempts are **10–23s apart** (scroll-slop taps fire in ms), Radix's `RemoveScroll` + a `fixed`/`overflow-hidden` `DialogContent` mean **neither page nor dialog scrolls while it's open**, and that store had `promptBookOnPacked: false`. What he actually hit was (D). But the underlying concern is real and now fixed: "Confirm & dispatch" spends wallet money on one tap and the dialog *can* open under a finger, so it **arms 600ms after appearing** — keyed on quote *presence* so a vehicle switch never re-disables it, genuinely `disabled` rather than inert, and not applied to the free actions (Book/re-quote/vehicle pills). Rejected: pointer-move tap discrimination (guards a gesture that can't happen) and hold-to-confirm (friction on every happy path). See [`docs/delivery-lalamove.md`](./delivery-lalamove.md#which-environment-am-i-in-21-aug-2026-clickup-86eypncfy).
- **Despatch labels — per-store template, single + bulk + one-click "ready to ship" printing** ✅ (ClickUp `86eyp63mp`, dev) — Kedaipal's **third** PDF document beside the receipt + subscription invoice, on the same pdf-lib pipeline, and the only one printed in stacks. **Explicitly NOT a courier-issued AWB** (that needs courier booking — the deferred Delyva/EasyParcel work): it carries the courier + consignment number the seller already recorded via the manual-courier flow (`86eyehvk4`), said out loud in the settings card, the schema comment and every module header. **Three entry points, two gates:** one label from order detail (`awb.generateAwbPdf`, owner-or-admin by shortId, **all-tier** — a seller shipping a parcel must always be able to put an address on it, the flat-fee posture), the bulk bar's printer button → a sort/count dialog, and a **one-click "Ready to ship" strip** in the inbox (packed + paid + parcel delivery, no selection needed) — both batch paths on `awb.generateAwbBatchPdf`, **Pro via the existing `orderInbox` gate** (they live in the Pro inbox surface; `exportOrders`' exact posture, admin act-as bypassing, no new `PLAN_FEATURES` row so no pricing-table change). **Printing never advances an order** — a label in the tray isn't a parcel in a courier's hands; the seller still marks shipped by hand (pinned by test). The ready count rides `searchOrders`' existing counts seam (`counts.readyToShip`, full-scan like `dueToday`) so it's the real backlog, not the filtered view, and a test asserts the number the strip shows equals what prints; the strip renders **at zero too** (disabled with the reason — the count IS the discoverability affordance) but never on a pickup-only store. **Nothing is dropped silently:** `labelSkipReason` = cancelled | no_address | not_found, counted per reason and named in words ("Skipped 3 for pickup (no delivery address), 1 cancelled"); the real rule is "is there an address", not `deliveryMethod`/`source`, so a counter order with a delivery address prints fine. **The two ceilings differ on purpose** (`AWB_BATCH_MAX = 100`): an over-cap *selection* is refused (dropping orders a seller deliberately ticked is the worst outcome), while the *ready queue* prints its first 100 and reports `remaining` (there's no subset to narrow to, and a refusal would block the despatch run). **`retailers.awbConfig`** (Settings → Fulfilment, under Delivery / above Pickup — the paper that goes on a parcel once it's going by courier): `paperSize` **`a4-4up` default** (every seller owns an A4 printer; A6 is the thermal upgrade) + toggles for logo / payment status / weight / note / contents + a footer line; every field optional and any field equal to its default is DROPPED on save (all-default → unset), the `openingHours` one-spelling posture, zero migration. **Contents default OFF** — a packing list on the outside tells everyone handling the parcel what's inside. **Payment strip is cause-true**: unpaid → big `COLLECT RM 128.00`, `deliveryFeePending` → "confirm amount" (never a figure the buyer would be wrong to pay), settled → `PAID - NOTHING TO COLLECT` (printing COD on a paid order is how a seller gets paid twice). **A fixed-size sheet can't overflow by construction**: header fixed, bottom stack measured first, recipient block takes what's left and truncates — tested with 200-char names, 12 address lines and 40 items on one A6 page. Everything degrades alone (no logo/courier/tracking/weight/date/note). **SG-correct**: `displayAddressState` **moved** into `convex/lib/address.ts` beside `SG_STATE_LABEL` (old `src/lib/address-display.ts` path kept as a re-export — the `src/lib/phone.ts` precedent) so client renderers and the label share ONE author and a label never prints "Singapore, Singapore"; money via `formatMoney` (SGD → S$), nothing hardcodes RM; the "delivery area" sort needs no country branch (SG degrades to postcode = postal sector = geographic). **Collection orders swap the parties** ("Collect from" buyer / "Return to" store) rather than lying about direction, and stay out of the ready queue. Weight reuses `summarizeCartWeight`, so an unweighable cart prints no weight rather than a guess. **Zero new dependencies**: `convex/lib/pdf/qr.ts` (byte mode, level M, v1–10) + `convex/lib/pdf/barcode.ts` (Code 128-B) are in-repo because both must run in a Convex action and `react-qr-code`'s encoder is a transitive dep; the QR is verified by an independent decoder + zero RS syndromes in-test **and was diffed module-for-module against `qr.js` (byte-identical data layer on every ASCII payload; only the mask tie-break differs)**, the Code 128 table by the format's own sum-11 + even-bar-parity invariants plus a decode round-trip. 4-up is drawn straight into A4 quadrants (NOT the ticket's `embedPages`) — one doc, one font/logo embed, one layout path; QR/barcode emitted as merged runs keeps a 100-label batch at ~370 KB. Known limitation (documented, shared with the receipt): pdf-lib's WinAnsi fonts strip non-Latin-1, so a CJK name degrades — a vanished recipient name falls back to the phone, then "Customer"; the real fix is `fontkit` + a CJK font, a v2 item alongside a WYSIWYG template editor and a courier-official AWB. See [`docs/despatch-labels.md`](./despatch-labels.md).
- **Seller reschedule of fulfilment date/time + Lalamove pickup-time picker** ✅ (ClickUp `86eyp5qd1`, dev) — the 3am-advance-order fix: a buyer scheduled a delivery 2 days out at 3:00 AM and nothing could change it (`fulfilmentDate`/`fulfilmentTimeMinutes` were write-once at create; dispatch's only offer was a rider *at 3am*). **(A) `orders.rescheduleFulfilment`** (owner-or-admin, all-tier — a correctness escape hatch, not an upsell): patches date (+ time on delivery orders; omitted time keeps the old one, self-collect stays date-only, a dateless legacy order may be *given* a date) and writes a `fulfilment_rescheduled (from … to …)` orderEvent. Window = pending/confirmed/packed; refused on counter orders, after `collectedAt`, and — the hard guard — **while a Lalamove job is ACTIVE** (the booking is frozen against its `quotationId` and won't follow the order; cancel first, so the flow is *reschedule → book*). The buyer-facing `minFulfilmentNoticeDays` floor deliberately does NOT apply (it protects the seller's lead time; the seller is the one moving the date); `[today,+30d]` still holds. Buyer sees the new moment **instantly with zero new plumbing** (track page reads `orders.get` live; later stage messages/emails render from live fields; inbox buckets are live reads) and **no new WhatsApp send** (one-msg-per-order posture — already-sent messages keep the old time, the chat agreement covers it; the dialog's helper says exactly that). UI = `RescheduleFulfilmentDialog` on order detail's Fulfillment card (two stacked rows — date/time owns a full-width line, mobile-first), rendered only inside the window ("Reschedule" / "Set date"), active-job case opens onto an explanation instead of the form, and on bookable Lalamove orders a debounced **"Lalamove for this slot"** price previews the rider cost at the picked moment (buyer's frozen fee named beside it; never shown for flat/radius/weight — not time-sensitive). **(B) Booking-modal pickup-time picker**: `prepareBooking.scheduleAtOverride` (`number | "now"`) through pure `resolveDispatchSchedule` — default byte-identical (buyer's moment), explicit picks share `resolveScheduleAt`'s clamps EXCEPT beyond-30d, which is refused with a message rather than silently booking now (an immediate rider is the opposite of what the seller asked). Every change re-quotes (quotes are scheduleAt-bound), override state resets on close/confirm, and a **promise-mismatch amber note** (via new `buyerRequestedMoment` on the quote payload) warns when the trip differs from a still-future buyer moment and points at Reschedule *before* booking. **Prevention = the opening-hours bullet below** (`86eyp5rav`, shipped on main just before this): hours gate the BUYER's picker only — the seller's reschedule + dispatch picker are deliberately NOT hours-bound (the vendor is the authority on their own exceptions). Canonical bug tix for this hotfix: `86eyp63xn` (Wagyu Walid rebook) — its ACs added **(C)**: Rebook auto-opens the time editor (a failed booking's schedule is stale by definition), and a **"also update the delivery time the buyer sees" checkbox** runs `rescheduleFulfilment` BEFORE `confirmBooking` (the only order where both can move — an active job blocks the reschedule; a failed sync aborts the dispatch). Default ON when the promise is past or a booking failed, OFF while the buyer's moment is still ahead; never for "now" or counter orders; ticked = the amber mismatch note retires. The ticket's `minNoticeDays` AC was amended off (buyer floor ≠ seller constraint — a 3-day-notice store must still rebook for tomorrow). **Past picks are FE-refused in BOTH editors** (20 Aug follow-up): native min/max only grey the picker, so typed/stepped/stale-seeded values validate live — inline reason + disabled Save (dialog) / no re-quote (modal), prefills clamp to today, fee preview skips invalid moments. See [`docs/fulfilment-date.md`](./fulfilment-date.md#seller-reschedule-19-aug-2026-clickup-86eyp5qd1) + [`docs/delivery-lalamove.md`](./delivery-lalamove.md#dispatch-pickup-time-picker--seller-override-19-aug-2026-clickup-86eyp5qd1).
- **Store opening hours — checkout date/time clamped to when the store operates** ✅ (ClickUp `86eyp5rav`, dev) — born from a real 3:00-AM-delivery order: `retailers.openingHours`, an optional weekly schedule (7 entries **indexed 0 = Sunday**, the same `getUTCDay`-on-MYT read `formatFulfilmentDate` uses; per day `{open, close, closed?}` minutes, `0 ≤ open < close ≤ 1439` — **23:59 is the ceiling** since a native time input can't say "24:00", both boundaries **inclusive**; closed days keep their times so re-opening restores them). **Unset = open 24/7** (every existing store, zero migration), an all-24h save **normalizes back to unset**, an all-closed week is **rejected** (working-method-invariant posture). **All-tier** (correctness, not an upsell). One pure module `convex/lib/openingHours.ts` is the single author: `sanitizeOpeningHours` (updateSettings, `null` clears), `assertWithinOpeningHours` (**`orders.create` + the checkout submit mirror**, same words both sides — closed day rejects BOTH methods, the time window binds only where a time exists, i.e. delivery; **pickup stays date-only**, its point's scheduleNote carries the detail), `selectableTimeWindow` (lead floor raised to opening, capped by closing — hours-unset degrades to exactly the old floor behaviour), `defaultTimeWithinHours`, `openNowStatus`, `openingHoursSpecification`. **Buyers still browse/order 24/7** — only the fulfilment moment is constrained — and **counter checkout is exempt** (seller standing there, min-notice posture, pinned by test). Checkout: day chips **skip closed days and scan forward** (`quickPickDays` grew an `isSelectable` predicate; for delivery a today whose window passed is skipped too), default date = first fulfillable day, TimeField gained `max` + the day's hours in its helper text (the rule is never silent), the 30s repair clamps into the window, and a closed day via the native date input (which can't skip weekdays) gets an inline weekday-naming explanation. Storefront: configured stores get a live **"Open now · closes 9 PM" / "Closed · opens 9 AM tomorrow"** line in the shared `StorefrontHeader` (all four buyer pages, `suppressHydrationWarning` on the clock text + minute tick) opening the weekly schedule dialog; the store home's JSON-LD gains `openingHoursSpecification` (open days only). Settings → Fulfilment **first card** (above notice — more fundamental): summary ⇄ mode-first editor — **"Same every day"** default (ONE range + weekday chips for rest days; the range writes every row so re-opened chips inherit it) vs **"Different per day"** (7 rows, Monday-first display over the Sunday-indexed array), mode derived from whether open days share one range; times picked via the new **themed `ui/time-picker.tsx`** (field-styled trigger → scrollable 30-min list, 11:59 PM terminal option — replaces the unstyled native dropdown on dashboard surfaces; buyer checkout's TimeField deliberately stays native since the phone OS wheel beats any popover); disabled-with-reason Save on all-closed, quiet "Reset to open 24/7". **Deliberately NO hidden ±1h buffer** (the original ask): a hidden offset makes the displayed hours lie ("you open at 9 — why can't I pick 9?"); prep headroom = min notice / the 15-min floor / tighter hours, an explicit buffer setting is a follow-up if a real seller asks. v1 defers: overnight wrap (mamak 6 PM–2 AM), split ranges, holiday dates. See [`docs/fulfilment-date.md`](./fulfilment-date.md#update-2026-08-19-86eyp5rav-store-opening-hours).
- **Landing + pricing funnel — money math, guarantee, one primary CTA, payment strip** ✅ (ClickUp `86eye3p6z`, dev, from the `design_handoff_landing_pricing` bundle) — the 27 Jul funnel audit found the strongest argument we own absent from our highest-traffic surface. **(A) Money math** (`money-math.tsx`, above the pricing teaser; `MoneyMathRow` is the one-line sibling under `/pricing`'s hero, one shared `MARKETPLACE_RATES` array): Shopee up to ~20% / GrabFood 15–22% / foodpanda 12–20% vs a 0% pill, bars proportional to each range's top so the chart can't imply GrabFood is cheaper than Shopee. It is a **positioning claim, never a savings claim** — the seller's WhatsApp orders already cost 0%, so we may say "we never take a cut at any volume" and may NOT say "save RM900 vs Shopee"; `landing-funnel.test.ts` fails on a savings word in any `mm_*` string, and on an un-hedged Shopee rate (per-locale: EN/BM tilde, ZH 最高约). Shopee leads deliberately — GrabFood's 15–22% also buys a rider fleet our BYO Lalamove doesn't. **(B) The guarantee** ("Your first real order lands in the dashboard in our first session, or we set it up until it does") has **one author** — `guarantee_line` rendered by one `GuaranteeLine` in `landing-ui.tsx` across hero / final CTA / both Pro cards / `/pricing`'s closing block — because the WhatsApp outreach ladder makes the same promise word-for-word and a second copy is how the two drift; a test fails if any other key holds the string. Suppressed when the CTA reads "Current plan" (that seller is already onboarded). **(C) One primary button:** the hero's **slug-claim input is deleted, not restyled** (it *was* the competing primary), and "Claim your free store" / "Apply for a founding spot" / "See how it works" are now text links in their own sections; the nav dropped **Cost calculator** (the money-math CTA is a better front door, and 5 links + locale + CTA crowded md–lg) and **Sign in** (recoverable from the sign-up screen's own link, so no path closed). **(D)** `/pricing` printed a hardcoded "10 of 10 spots" and a literal `{spots: 10}`; both now read the live `getSpotsRemaining`, all-open on SSR. **(E)** Calculator stays at the existing `/cost` — same formula, SSR'd, prefill-shareable; a second `/kira` would be two things to sync. **(G) Payment strip** (`src/lib/payment-methods.ts` config + `payment-methods.tsx`, full section on `/` + compact footer repeat): the **LANDING catalogue = HitPay's full MY capability**, deliberately NOT shared with checkout/Settings, which must show only what that BYO seller actually enabled — one shared list would be a lie where a buyer is about to pay. Marks ride a **white pill in both themes** (brand marks can't be recoloured); **one `CARD_MARK` height for all** since every asset we hold is on the same 38×24 artboard (per-mark heights were scaling identical artboards to different sizes = the ransom-note row the brand rules warn about); no brand-approved SVG ⇒ **neutral wordmark chip**, never a raster (ShopeePay's "SVG" is a base64 PNG in a shell); the **brand name IS the accessible name** in every locale (proper nouns don't translate, so they live in the config not the catalog); cross-border wallets sit in the array `visible: false` with a test pinning the deferral. The load-bearing line — "The money goes into your own HitPay account, never ours" + "Gateway fees are billed by HitPay directly — we add nothing on top" — is always adjacent, because the strip sits one section under "0%" and **"0% cut" is Kedaipal's subscription, never HitPay's per-transaction fee**; a test fails on a free-processing word in any `pay_*` string. **Hero visual is now an overflowing inbox** (buried order requests + "dah transfer" among family chats, "Missed order · RM 95" / "47 unread" / "Sis dah bayar ke? 😩") — the old tidy conversation showed the happy path the page spends four sections promising to build. **Real sellers trimmed 4 → 2** (the survivors are one stall + one *service* business, since the broadened non-F&B ICP is that section's claim), and the **Counter checkout section was removed from `/`** (owner call — the capability keeps its bento card + FAQ entry). Copy corrections that rode along: `faq_a_3`/`pricingpage_faq_a4` still said online payment was "coming next" / "on the S5 roadmap" (**HitPay shipped in July**), `handshake_point_1` claimed bank details ride the confirmation message (they ride the buyer's own order page), Lalamove was missing from the delivery/how-it-works copy, `hero_subhead` was F&B-only, and the `/pricing` table gained five shipped Pro rows (online payments, categories, radius, Lalamove, WA seller alerts) while **payment reminders lost their "Coming soon" and became all-tier** (shipped `86ey570am`, no `PLAN_FEATURES` entry). Scale's card allowance was number-free after `86eyb9zwt` while the table already printed 400 — two answers on one page; settled on **400 everywhere**, `pricing-copy.test.ts` re-pinned. Fixed in passing: the "Payments powered by" line was a `<p>` wrapping `AppImage`, whose loading skeleton is a `<div>` — illegal nesting that **failed hydration on every landing render**. Deferred: PostHog funnel instrumentation (not integrated in the repo at all), and official SVGs for Apple/Google Pay, MayBank QRPay, the BNPL trio and ShopeePay (drop the file in + add `src` to the config row; no component change). **Mobile carousels (same branch):** the worst-stacking sections became horizontal snap carousels below `md` (~20k px → ~14k px page): features bento, How-it-works steps (timeline connector is `md`-only), problem strip (slides self-border on mobile, joined `gap-px` grid rebuilt at `md`), pricing-teaser tiers (track `pt-4` so Pro's absolute "Most popular" badge survives the scroller's clip), Real sellers (flattened to one 3-slide track), payment-strip chip rows (one swipeable line per group below `lg`). One shared `carouselTrackClass`/`carouselSlideClass` in `landing-ui.tsx` on the category-rail scroller mechanics; two encoded rules — slides are `position: relative` (absolute `sr-only`/badge descendants otherwise resolve their containing block ABOVE the scroller, escape its clip, and widened the page to 978px) and slides are never individually FadeIn-wrapped (a peeking slide sits in FadeIn's `-80px` inset and holds at opacity 0, hiding the swipe affordance — one FadeIn per track instead). See [`docs/landing-funnel.md`](./landing-funnel.md).
- **Seller WhatsApp order alerts — new order + payment claim to the seller's own number** ✅ (ClickUp `86eyhw9zy`, dev, Sengloh request) — the seller-side WA ping for universal pain #1: opt-in WhatsApp alerts from the shared WABA on **new storefront order** (`orders.create`, beside `notifyRetailerOrderAlert`; **counter excluded** — seller is standing there, min-order-exemption posture, `order.source` re-checked in the action) and **buyer payment claim** (`orders.claimPayment`; **counter pay-later INCLUDED** — a claim lands hours after the sale when nobody's at the counter). Sellers hold no 24h window with the WABA, so both are **Meta utility templates**, env-gated on the confirm-push pattern (`WHATSAPP_SELLER_NEW_ORDER_TEMPLATE` / `WHATSAPP_SELLER_PAYMENT_CLAIM_TEMPLATE`, unset ⇒ silently unavailable, settings card hidden via `waOrderAlertsAvailable`; button URL `https://kedaipal.com/app/orders/{{1}}` ← shortId, registered via **Add variable** per the 86eyheqzv lesson). **EN + BM variants, picked from `retailers.locale`** (Zaki's catch — an EN-only alert would have contradicted the retailer *emails*, which have always rendered in the store locale via `renderRetailerEmail`); resolution moved into one exhaustive `TEMPLATE_LANGUAGE: Record<Locale, "en"|"ms">` in `whatsappCopy.ts` that the buyer confirm push now shares (its own `=== "ms"` ternary deleted) — **zh rides EN** because naming an unapproved language makes Meta reject the send outright, killing a zh store's alerts rather than degrading them. Deliberate carry-overs (identical to the emails): the `{{4}}` fulfilment date stays EN-formatted (`formatFulfilmentDateTime` is locale-blind — cross-surface i18n change, not this ticket) and the /app pages the button opens are EN until the dashboard sweep. `retailers.locale` serves two audiences, so its settings card was relabelled "Message language" with a helper naming BOTH reaches (it drove seller emails all along — stale copy fixed in passing). **Category = `utility_template`, deliberately NOT transactional** — respects kill switch/caps/quality/global STOP opt-outs and shares the buyer-message cap bucket (7 Aug decision: Pro absorbs the ~RM1.47/mo Meta cost, no add-on SKU); an opted-out seller number surfaces on the settings card (`notifyWaPhoneOptedOut` + reply-START fix) instead of the toggle silently no-oping. Retries ride `classifyPushFailure` (30s/2m/8m, ambiguous-2xx never). Schema: `retailers.notifyWaPhone` (MY mobile, normalized; **separate from `waPhone`** — that's the buyer-facing store contact/wa.me fallback/Lalamove sender, the notifyEmail-vs-Clerk-email split) + `retailers.orderWaAlerts` (off by default), dev-only widen. `updateSettings`: `assertValidMyMobile`, rejects the WABA's own number, **enabling Pro-gated** (`PLAN_FEATURES.waOrderAlerts`, act-as bypass) / disable+clear un-gated, clearing the number auto-disables. Settings → Store: notification surfaces now grouped (browser → **WhatsApp** → email), new `wa-order-alerts-card.tsx` prefills from `waPhone`, names both events + the counter exclusion. **Email/browser alerts unchanged this phase** (deliberate double-notify; "WA replaces email + email fallback on WA failure" = follow-up ticket) — so a failed/gated WA alert never means zero notification, and no order-level outcome stamping exists yet. Pricing-table row deferred until the templates are live in prod (never promise what a deployment can't do). Lalamove trip updates deliberately carry no WA alerts (seller's own Lalamove app covers those; failures already email). See [`docs/order-notifications.md`](./order-notifications.md).
- **Weight/zone delivery pricing — the seller's courier rate card at checkout** ✅ (ClickUp `86eyeea1n`, dev) — the fourth `deliveryConfig` mode for frozen/outstation parcel sellers (Lopes Viral JB + Wagyu Walid, both shipping **ambient J&T** + polystyrene/ice; Lalamove is intra-city, radius prices distance not courier zones): seller-defined **zones of MY states × ascending weight bands** (`{mode:"weight", zones:[{name, states, bands:[{maxKg,fee}], freeAbove?}], onOutOfBands, onUnpriceable}`), priced from the buyer's address **state** (already canonical via `assertValidAddress`/`normalizeMyState` — so **manual, pin-less addresses price fine**, unlike radius/lalamove) × the cart's summed `productVariants.parcelWeightG`. **All-tier** (Arif 11 Aug — zero provider cost, correctness for outstation; radius stays the Pro row, pinned by a planGating test). Bands are courier **box tiers** ("up to 5 kg = RM30"), maxKg inclusive compared in integer grams (no float noise); **per-zone `freeAbove`** is deliberately unconditional (waives overweight/unweighable carts — the TikTok-parity free-shipping lever, settings copy says so) but never applies to unserved states. **Never a silent underweigh** (`summarizeCartWeight`): a custom/price-on-quote line → `custom_item`, a weightless non-custom item → `missing_weights`, cart beyond the last band → `over_bands`, state in no zone → `unserved_state` — each block/arrange per policy, frozen onto `deliveryFeePendingReason` with cause-true seller-card copy (missing_weights names the fix) + per-reason checkout blocked copy (an overweight cart never blames the address). Same one-resolver-three-callers seam: public `delivery.quote` grew `state` + `items` (server reads variant weights itself — client can only misprice its own preview; zone internals stripped), `orders.create` weighs in the existing variant loop, `updateDeliveryAddress` **re-weighs + re-zones** on state change; snapshot audits `zoneName`/`chargeableKg`/`bandMaxKg` (seller-only, buyer strip). Settings editor: zone cards w/ state-membership chips (a state lives in ONE zone — claimed chips grey out w/ visible legend), band rows, uncovered-states consequence line, two block/arrange radios, **"Use the J&T template"** seeder (`src/lib/weight-zone-seed.ts`, published Aug-2026 ambient rates seeded slightly ABOVE public so unedited never undercharges; sanitizer-stability pinned by test). **`parcelWeightG` finally got a UI** (was CSV-only): per-choice input under the product editor's Advanced disclosure, **promoted to its own block on weight-mode stores** (`ProductForm.weightMode`); blank = explicit 0 (never undefined — that's `saveVariantGrid`'s preserve path); new grid combos inherit row 0's weight. Dispatch stays manual — pairs with manual courier + tracking (`86eyehvk4`). Deferred: the layered local-rider-bands-inside-the-zone revision (follow-up PR, Pro via `radiusDelivery` since the local layer IS radius pricing), EasyParcel booking (`86eyehvnj`), volumetric weight. See [`docs/fulfilment.md`](./fulfilment.md#delivery-charge--weightzone-courier-rate-card-2026-08-11-clickup-86eyeea1n).
- **Product cap 50 → 200 + the permanent delete that makes it honest** ✅ (ClickUp `86eyjmf4q`, dev) — a cap already existed (`MAX_PRODUCTS_PER_RETAILER = 50`, hard, all-tier) but was wrong three ways: **too low for our own ICP** (Sue Chef Kitchen's scroll-wall menu — the driver for categories — is a >50 catalog), **completely invisible** (no counter, no disabled-with-reason; the seller met it as a raw `ConvexError` at save, and on import *after* preparing the sheet), and **a one-way ratchet** — the count included archived rows and **there was no delete path for a product anywhere in the app**, so a store that filled its slots with test rows + a bad import could never reclaim one. Now **200, every tier** (catalog size is a bad upsell lever for this ICP — a seller's menu *is* their business; `orderCap` is the monetization lever, and >200 SKUs is a by-hand Enterprise conversation). **The cap counts TOTAL rows, active AND archived** — chiefly because it's the only semantic that **can't be breached**: restore is `update({active:true})`, which has no cap check and shouldn't need one, so an active-only cap would let a seller at the ceiling restore straight past it; counting rows keeps `create` + `bulkUpsert` as the only two mutations that can grow the count. That makes **deleting, not archiving, the escape valve** (archive = off-storefront, keeps its slot; delete = frees it), so `products.deletePermanently` shipped with it — **owner-or-admin** like `archive` (not the admin-only posture of the order hard delete — orders are financial records, a never-sold product isn't) and deliberately **not** subscription-gated (reducing is always allowed). It **refuses any product that has ever been ordered**: `orders.items[].productId` is a hard reference beside the frozen snapshot, so erasing a sold product orphans it (Insights groups by productId + resolves the row for its thumbnail) — **a cancelled order still protects it** (the line still names it; a stale stamp only costs an archive, a wrongly-cleared one erases a referenced product). Answered in O(1) by new **`products.orderedAt`** (`orders.items` is an ARRAY — no index answers "which orders reference X", and the scan alternative breaks exactly on the high-volume stores most likely to be pruning), stamped set-if-unset at BOTH order-create sites, never cleared, `updatedAt` deliberately not bumped; **seller-only and stripped from every public read** (PR-review catch — it's the first sales-derived field on the product row, and `productWithVariants` spreads the whole row into the unauthenticated `list`/`getPublicBySlug`/category reads, so left in, anyone could diff a store's catalog for which SKUs have never sold — the same line `popularProducts` draws by staying ids-only; new explicit `forOwner` flag, deliberately NOT keyed off `activeOnly` since `listForCounter` is owner-gated yet passes `activeOnly: true`, and the return is annotated `typeof base` because a Convex query has one return type and inferring the union silently dropped `orderedAt` from the OWNER type too, breaking the `hasSold` check); **one-shot `products:backfillProductOrderedAt` must run per deployment** (paginates ORDERS, converges on the earliest per product) or every pre-existing product looks never-ordered and deletable. **Admin act-as bypasses the cap** (subscription soft-lock precedent) so a white-glove catalog can be stocked past the ceiling — the exemption is the **admin's, not the store's** (that store still can't self-serve a 251st). New pure `convex/lib/productCap.ts` (shared client+server, `exempt` exposed explicitly since "room for N more?" can't be read off `remaining`) + `convex/lib/productDelete.ts` (`deleteProductCascade` — variants+blobs, own blobs, junctions, row — now also used by `retailers.deleteUser`, which open-coded its own; category `productCount` stays caller-side on purpose, since account deletion would just be patching corpses). Discoverability: `N of 200 used` from **80%** only, at-cap amber card naming archived-counts + delete-frees, disabled-with-reason New button (a real `<button>` — an `<a>` can't be disabled), **`/app/products/new` guarded before the wizard renders** (the deep-link path a disabled button can't cover), a single quiet red "Delete permanently" row below the form (not beside Save/Archive — a destructive control next to routine ones invites the misclick; the boxed danger-zone section was cut on Zaki's review, and the sticky bar's archive became a **labelled** outline button since the red icon-only version read as delete), the sold-product state **blocked-but-tappable** — it opens a "why can't I delete this?" dialog that explains the history reason and offers Archive as the action (one affordance both platforms; the shared Button's `disabled:pointer-events-none` means a disabled control can't even show a hover tooltip), and an import preview that blocks Confirm client-side (the sheet is chunked, so an oversized import would otherwise half-apply then throw). Delete confirm is a plain destructive dialog — no type-to-confirm, unlike the order hard delete. Deferred: read-path work on `products.list`/`listForCounter`/`listAll` (they collect every product + N+1 variants + a `storage.getUrl` per image; worth measuring at 4× the old cap, its own ticket). See [`docs/product-cap.md`](./product-cap.md).

- **HitPay online payments — buyer "Pay now" + auto-confirm (BYO seller accounts)** ✅ (ClickUp `86eyb6z3a`, dev) — the buyer side of universal pain #2: **Pay now** on `/track/<token>` → the seller's own HitPay hosted checkout (TnG/DuitNow QR/FPX/card — HitPay owns all bank/wallet app-switching; no public MAE/TNG deep links exist) → webhook + redirect-reconcile flip `paymentStatus → received` through the **shared `applyPaymentReceived` core** (auto-confirm + activation + the existing `notifyPaymentReceived` WhatsApp — zero new send types), killing save-QR/screenshot/manual-claim on gateway orders while the manual handshake stays first-class beside it. **BYO-ONLY** (Lalamove posture; Kedaipal never touches order money): `retailers.hitpay {enabled, apiKey, salt}` pasted in Settings → Payments' new **Online payments card** (bullet-style education: SSM eligibility, T+2/T+3 payout ≠ instant DuitNow, HitPay's ~1.2–2% fee, Kedaipal takes nothing) — sandbox inferred from the `test_` key prefix (verified real), summary-only reads (`hasCredentials`/`apiKeyHint`), **enabling Pro-gated (`PLAN_FEATURES.onlinePayments`), pause/disconnect never** (pause keeps keys). This sidesteps the pending Platform/OAuth enablement (86eyb6z2d) entirely — that layer is additive later. **Requests are minted LAZILY on the buyer's tap** (`hitpay.createCheckout`, token-capability action, rate-limited, 1h expiry, 55-min reuse window = one live payable link), never at order create — so held totals (mockup/`deliveryFeePending`) can't be paid early and every link prices the CURRENT total; **zero WhatsApp/template changes** since the confirm template button + "Make payment" CTA already land on the order page (one door for storefront AND counter pay-later). `POST /webhook/hitpay` = Lalamove posture (resolve order via new `orders.by_gateway_request` → verify v1 `hmac` with the SELLER's salt (sort-concat HMAC-SHA256) → unknown-ack-200 / no-salt-500 / bad-401); **`receiveGatewayPayment` is the single judge**: idempotent by `gatewayPaymentId`, and paid amount+currency must equal the order's current total — mismatch (stale link paid after a re-price) writes a `gateway_amount_mismatch` event + `gatewayMismatch` seller email (EN/MS/ZH) and never auto-receives. Landing back (`?status&reference` — stripped, never trusted) fires ONE `verifyCheckout` against the status API, the lost-webhook safety net. `paymentMethod` gained **`fpx`** + gateway rails stamp truthfully (v1 webhook carries no rail → stamp `other` + `enrichPaymentMethod` upgrades from the status API; never rewrites a seller-picked method) so the inbox Method filter + Insights donut (ramp widened to 8) stay honest; seller order detail reads "Paid online via HitPay · rail · Ref". Buyer page: **one payment door** — Pay-now primary (gateway stores → hosted checkout; manual stores → the `ManualPaymentDialog` sheet: methods w/ copy/QR-save + I've-paid proof, absorbing the old always-visible "How to pay" section and IvePaidDialog) + "Paid by bank transfer instead?" fallback on gateway stores opening the same sheet; `send_sms=false` explicitly (defaults ON upstream). **Sandbox E2E verified live** (MY rails now exist in sandbox — their docs lag; MYR + TnG simulator + redirect contract + status API all confirmed against a real account). Known: HitPay's page requires a buyer email (we don't fabricate one — buyer types it; dashboard toggle TBC); refunds stay HitPay-dashboard manual. **Method truth is probed, never hardcoded**: a connect-time throwaway request (doubling as key validation — 401 renders "HitPay rejected this key") reads the ACCOUNT's `payment_methods[]` echo, every real mint refreshes it, and both the settings chips (official plugin-repo marks) and the buyer's "Pay by …" line render only from that stored list. **v1.x polish (`86eyjmhby`, dev):** the buyer's paid card shows the same copyable "Payment ref …" the seller sees (one number both sides quote; the token read already carried `gatewayPaymentId`), and the settings card links a **print-ready vendor setup guide** `/guides/hitpay-setup.html` (Lalamove-guide precedent: sign-up → KYC → enable methods → keys → connect → RM1 test). **PR #178 review (finding 1) closed the unapplied-payment silence:** a mismatch/pay-after-cancel refusal wrote an event + seller email but had **no UI on either side**, so the buyer kept a plain "unpaid" page with **Pay-now armed** right after their money left — the double-payment path, and the seller's only signal was email (on a product whose thesis is that sellers don't read email). It's now frozen onto the order as **`orders.gatewayPaymentIssue`** (`{kind, paidAmountSen, paidCurrency, paymentId, at}`) — persisted, not derived, because the **webhook** discovers it with no client watching and the surface must survive a reload. Buyer: the notice renders **ahead of** the mockup/fee holds (those say "price isn't final"; here the money already moved) as disabled-button + explanation + copyable ref, with **no Pay-now and no bank-transfer fallback**; a cancelled/`claimed` order gets the same copy as a standalone card (the payment card is hidden or has no room), one `gatewayIssueCopy` author for both. Seller: amber section in the `confirmationPushStatus` family. **Server-enforced** — `createCheckout` refuses while unresolved, so a direct call can't stack a second payment. **Two exits, because blocking Pay-now would otherwise trap a refunded buyer:** accepting clears via `applyPaymentReceived` (the single retirement point every receive path runs through), and refunding uses new `orders.clearGatewayPaymentIssue` (owner-or-admin "Mark as resolved") which retires the notice **without** marking paid, audits it, and **releases the request the odd payment landed on** so the next tap mints fresh rather than reusing a link HitPay may treat as settled. **Root cause closed at source too:** every re-pricing mutation now calls `voidStaleGatewayRequest` (`setDeliveryFee`, `updateDeliveryAddress`, `updatePickupLocation`, `submitMockup`, `updateMockupQuote`, custom-decline) — the id moves to `gatewayPreviousRequestId` (correlation kept, or a payment beating our DELETE becomes an unknown-ack-200 = money moved, nothing recorded), the live link is cleared, and a scheduled `hitpay.voidRequest` DELETEs it; no-ops when the total is unchanged. Chosen over the review's "refuse to re-price while a link is live" — that blocks the seller for up to an hour and ignores buyer-driven re-prices (address/pickup edits). `verifyCheckout` now returns the verdict instead of flattening to `"unpaid"`, and `gone` (order hard-deleted mid-flight) is split from `duplicate` (genuinely settled). Pay-now funnel analytics deferred — PostHog isn't integrated in the repo at all yet. See [`docs/hitpay-gateway.md`](./hitpay-gateway.md).
- **TanStack deps exact-pinned — `latest` dist-tags removed** ✅ (ClickUp `86eyjadx7`, dev) — TanStack shipped a ground-up **lane-scheduler rewrite** (loaders/preload/redirects/SSR status — [PR #7805](https://github.com/TanStack/router/pull/7805), 27 issues closed) as a **patch release tagged "Fix"** on 4 Aug 2026 (`react-router@1.170.19`), while `package.json` spec'd six TanStack packages as the `latest` dist-tag — CI was lockfile-frozen, but any dev-machine `pnpm add`/`pnpm update` would silently re-resolve the whole framework across two minors + the rewrite, untested, into an unrelated PR. Since TanStack ships breaking changes in patches, **no semver range protects us** — the router/start family is now **exact-pinned** to the prod-tested lockfile versions (zero resolution change, byte-identical deps), unused scaffold leftover `@tanstack/react-router-ssr-query` dropped, and `src/lib/dependency-pins.test.ts` guards both rules in the gate (no dist-tag specs anywhere; TanStack family exact — mutation-tested red/green). Audit confirmed the codebase touches **none** of the rewrite's removed APIs (`RouterState.loadedAt/isTransitioning/statusCode/redirect`, `RouteMatch.fetchCount`; `useRouterState` only selects `location.pathname`). The deliberate lockstep **upgrade past the rewrite** (≥1.170.21) + buyer-surface regression pass (ssrRead soft-degrade, RouteErrorCard, sitemap GET, preload dedupe, Clerk-free routes, SSR 404 codes) is ClickUp `86eyjadza`. See [`docs/ci.md`](./ci.md#dependency-pinning--tanstack-is-exact-pinned-2026-08-07-clickup-86eyjadx7).
- **Buyer-page resilience — SSR soft-degrade + branded error boundary + Clerk-free buyer surfaces** ✅ (ClickUp `86eyheqzv`, dev) — the fix for buyers intermittently dead-ending on a raw error page when tapping WhatsApp links (prod blocker; "too many requests/redirects" reports). **ROOT CAUSE (confirmed via buyer screenshot + prod curl repro): the confirmation template's URL button composes to `/track/{{1}}<token>`** — the registered button URL carries `{{1}}` as literal text (not a recognized variable), WhatsApp appends the token after the braces, and TanStack's URL canonicalization 307-loops on the percent-encoded path (decodes `%7B` in Location, browser re-encodes, forever → `ERR_TOO_MANY_REDIRECTS` before any page code runs). "Intermittent" = which link the buyer tapped: the button (broken) vs plain-text URLs in status messages (clean, code-built). Fix in-repo = **pre-router rescue**: custom Worker entry `src/server-entry.ts` (wrangler `main`) 301s polluted `/track` paths to the clean token URL — repairs every ALREADY-SENT button frozen in chats — + `normalizeTrackingToken` defence in `orderByToken` (pure module `convex/lib/trackingToken.ts`, tokens never contain braces). Meta-side correction still required for new sends: re-register the button URL via the **Add variable** control, never hand-typed `{{1}}`. **The rescue is route-agnostic as of PR #178 review (finding 2)** — `rescueTrackUrl` → **`rescuePlaceholderUrl`**, stripping a leading `{{n}}` from *any* path segment: the seller order alerts (`86eyhw9zy`) put the first URL buttons on `https://kedaipal.com/app/orders/{{1}}`, the one path a `/track/`-only rescue didn't cover, so the identical mis-registration would have recurred as a fresh incident — this time 307-looping the **seller** out of their own order. A segment that is a placeholder and nothing else is left to 404 (the parameter never arrived; there's no destination to guess), and braces can't legitimately appear in any Kedaipal path (slugs/shortIds/tokens are generated alphanumerics). Root pattern: no route had an `errorComponent`, so ANY SSR loader throw served TanStack's unstyled fallback with the raw error string (proven live — prod `/sitemap.xml` 500'd with exactly that page), and every buyer loader treated a transient upstream failure as fatal even though the pages' real data is the client's reactive `useQuery`. **(1) `ssrRead`** (`src/lib/ssr-read.ts`): all five buyer-route loaders (store home / category / product / checkout / track) soft-degrade on a THROWN read (return null → generic head meta → shell renders, live query paints) while an ANSWERED null still 404s — the two are deliberately distinct result shapes, since conflating them would 404 a valid order on a blip. **(2) `RouteErrorCard`** as `defaultErrorComponent`: calm mobile-first card, Try-again = `reset()` then `router.invalidate()` (order load-bearing), raw error text dev-only. **(3) Buyer surfaces render Clerk-free** (`BUYER_ROUTE_IDS` + provider branch on matched route ids in `__root.tsx`): storefront/track no longer pay Clerk's 8-request boot incl. two per-IP rate-limited FAPI calls per page view (MY carrier CGNAT = plausible intermittent 429 source) — verified zero external requests on buyer pages, landing/app unchanged; **new buyer surfaces must be added to `BUYER_ROUTE_IDS`** (test pins ids against `routeTree.gen.ts`). **(4)** `defaultPreloadStaleTime` 0 → 30s (the 0 made every hover re-run loaders with zero dedupe — desktop-only storm, touch never preloads at delay 0). **(5) sitemap.xml fixed**: `throw new Response` in a loader = route ERROR in current TanStack (prod served a 500 page, so crawlers never saw store/product URLs) → rewritten as `server.handlers.GET` returning the XML Response, pure builder in `src/lib/sitemap.ts`. Ops follow-ups on the ticket: `www.` NXDOMAIN, CF API token for `wrangler tail` (Convex logs can't see SSR-layer failures), and the confirm template's registered button base must be `https://kedaipal.com/track/{{1}}`. See [`docs/buyer-page-resilience.md`](./buyer-page-resilience.md).
- **Fulfilment TIME at checkout + scheduled Lalamove bookings** ✅ (86eyg0n8e follow-up, dev) — delivery orders (both directions) capture **what time**, not just the day: new `orders.fulfilmentTimeMinutes` (minutes since MYT midnight) kept SEPARATE from `fulfilmentDate` on purpose — the date keeps its whole-midnight invariant, so the inbox sort/due-today/badges/window chips are untouched by construction; legacy/counter/self-collect orders have no time and every consumer treats that as the old behaviour. Checkout: **required but prefilled** (today → next 30-min slot ≥1h out, future day → 10:00 AM; floor ~30 min out when today; an invalid slot bumps to the day's default, a deliberate future one is never touched), native `TimeField` beside the date, delivery-only (pickup hours belong to the point's schedule note). Server validation is range-only by design — "still ahead?" is judged at submit + dispatch, where a past moment books "now". The **checkout quote prices the exact moment** (old noon-heuristic kept byte-identical when no time) and **dispatch defaults to the buyer's moment** via one pure rule `resolveScheduleAt` (≥30 min ahead + ≤30d → `scheduleAt`, else book now): confirm dialog says "Scheduled pickup — … 3:30 PM" vs "the rider comes now", `deliveryJobs.scheduledAt` recorded so the card reads "Scheduled pickup — 3:30 PM" instead of hours of "Finding rider". Time renders on wa.me/track/seller page/email via one `formatFulfilmentDateTime`. **Manually advancing a bookable rider order now opens the dispatch card's OWN booking modal** (live price, Motorcycle/Car, variance) instead of an intermediate "how is this going out?" prompt — one door onto the spend; `MarkShippedDialog` keeps the parcel-courier form and the blocked-rider reason, and its dead `Book a rider` branch is gone. The modal carries a quiet `Mark as {stage} without a rider` **only** on that advance path (the card tracks `fromAdvance`), so the packed prompt and the card's own button stay a plain book-or-not; a quote that fails there (out of service area) hands the seller back to the prompt rather than leaving a toast and no way forward. The modal **opens on the tap** with a "Getting today's price from Lalamove…" line rather than after the quote round-trip (a dismissal mid-quote orphans the result via a generation ref). Fixed the shared `Dialog` primitive in passing — `DialogContent` is a `grid` whose track defaulted to `min-width:auto`, so one wide child pushed the box past `sm:max-w-sm` and `overflow-hidden` **clipped** it (measured: 156px of a 384px modal cut off, Zaki's screenshot); `grid-cols-[minmax(0,1fr)]` + a `min-w-0 sm:flex-wrap` footer fixes it for every dialog in the app. See [`docs/fulfilment-date.md`](./fulfilment-date.md) + [`docs/delivery-lalamove.md`](./delivery-lalamove.md).
- **Collection service (Leg 1) — rider collects FROM the buyer** ✅ (ClickUp `86eyg0n8e`, dev, Bearcamp) — one store-level switch reverses the Lalamove trip for cleaning/repair services: `retailers.deliveryBooking.deliveryDirection: "standard"|"collection"` (**on `deliveryBooking`, not the ticket's `deliveryConfig.lalamove` arm** — pricing-mode switches rebuild that arm wholesale while the booking object merges per-field, promptBookOnPacked precedent, so Flat-and-back can't silently reset it; settings toggle sits above prompt-on-packed). Every branch is `=== "collection"`-gated — standard stores byte-identical. **Dispatch swaps stops AND contacts** (`dispatchContextForOrder`: origin/sender = buyer w/ the same +60 fallback, destination/recipient = outlet) and **`quoteForCheckout` prices the same buyer→store direction** (route prices aren't symmetric; keeps buyer-paid fee vs dispatch re-quote honest). **The webhook moves the JOB only** (frozen `deliveryJobs.deliveryDirection` at reserve): picked_up/completed never advance the order (arriving at the seller ≠ delivered to the customer — seller advances by hand, custom stages tell the story), **no `carrierTrackingUrl` mirror** (a later manual shipped would present the stale Leg-1 trip as shipment tracking) — instead the buyer's track page gets a **live collection-rider strip** while a job is ACTIVE (`orders.get.collectionRider`: trip state + driver name/plate + share link, never phone/cost/ids; transient card, not a status — reads the live job row so it can't go stale, vanishes when the trip ends; verified against a real sandbox booking), **POD photo is seller-only** ("kept for your records" — never WhatsApp'd to the buyer, and `orders.get` hides it from the track page even after manual delivered), and the order page's **manual-advance gate + mark-shipped rider prompt are lifted/skipped** (the gate would lie; the prompt would book ANOTHER collection — Leg 2 return = separate order, out of scope per ticket). **Frozen-status class bugs swept** (two adversarial audits after Zaki caught the first): the fulfilment-date badge showed permanent red **"Overdue"** on every in-flight collection order (its date is the COLLECT day, a start not a deadline, and the only mute was `delivered` — which collection never reaches), suppressing every other contextual badge; fixed with **`orders.collectedAt`**, a webhook *timestamp* (never a status change) that also gives the buyer a persistent "Collected on …" line once the live strip retires. Order-detail's direction gate now reads the **order's frozen** value, not the store's live one (a mode switch was stripping the rider gate off in-flight standard orders). Also: seller card said "Delivery/Deliver on", the confirmed-email next step said "pack and ship", cancel/delete warnings named the wrong card, and the CSV *Fulfilment cell* (never the header — one export mixes directions) said "delivery". **The seller can't move a collection order until the goods arrive** (Zaki's 2nd catch): the stepper let him mark Packed with no rider booked, and prompt-on-packed then offered to dispatch one — backwards, since packing follows arrival. New `isCollectionGateClosed` (beside `isMockupGateClosed`, same shape) blocks packed-or-later anchors while `collectedAt` is unset, **server-enforced on all three write paths** (advance/update throw, bulk SKIPS), escape = "I already have the items" → `advanceToStage({markCollected:true})` stamps arrival so it asks once; prompt-on-packed never fires on collection. **Also tightened the STANDARD rider gate** (a pre-existing hole, not from this branch — proven identical on staging): it required `lastEventAt`, so it was OFF between booking and the first webhook event, exactly when a seller can click through a live trip; now any ACTIVE job gates shipped/delivered, with the same confirm escape. One-shot `orders:backfillCollectionCollectedAt` for jobs completed before the field existed (dev-only; prod has no collection orders). **A completed collection is terminal for the dispatch card** (`collectionDone`, bug caught in local test): since the order never advances it stays `bookable` forever, so the card re-offered "Send rider to collect" — and prompt-on-packed would have AUTO-booked — a second paid trip after the goods already arrived; a *failed* collection still rebooks, standard orders unaffected, and the settled card names the Lalamove-app path for the return leg. **Buyer surfaces read collection truthfully** off frozen `orders.deliveryDirection` (+ public `deliveryCollectsFromCustomer` on the slug payload): checkout ("Collection address", intro line, "When should we collect it?", "Collection" fee rows, failure copy), wa.me ("🚚 Collect from:"), WA confirm method line EN/MS/ZH, tracking page ("Collect from" / "We collect on" / "Collection fee") and the address-edit dialog; seller sees "Lalamove Collection" card copy (pills Heading to customer → Collected → Arrived) + "Collect From" address block. See [`docs/delivery-lalamove.md`](./delivery-lalamove.md#collection-service--leg-1-rider-collects-from-the-buyer-86eyg0n8e).
- **CI — PR gate before merge (typecheck + lint + tests)** ✅ (ClickUp `86eyetzcw`, dev) — the P0 infra-audit safety net: before this, the only workflow ran **after** merge to `main` and auto-deployed to prod, so broken code reached production before any check ran — and its `lint-and-check` job never actually ran lint. New reusable **`.github/workflows/ci.yml`** (one `Typecheck, lint & test` job: install → paraglide compile → `pnpm lint` → `pnpm typecheck` → `pnpm test` → `pnpm build`, 10-min cap, superseded PR runs auto-cancelled) triggered on **every `pull_request`** (any base, so stacked PRs get feedback too), **`push` to `staging`** (catches two-green-PRs semantic conflicts post-merge), and `workflow_dispatch`; **`deploy.yml` now calls it via `workflow_call`** instead of duplicating steps, so the PR gate and the pre-deploy gate can never drift again, and lint finally runs before prod. Toolchain pinned from the repo, not the workflow: Node via `node-version-file: .nvmrc`, pnpm via `packageManager`. **`.nvmrc` is `24` and must not go below 22.12** — the first cut pinned deploy to a stale `.nvmrc` of `20`, silently downgrading prod's toolchain four majors (prod had been on `lts/*` = 24.18.0) onto the one step no gate covered; Node 20 is EOL since 2026-04-30 and `@tanstack/react-start` (a build-time vite plugin) requires `>=22.12.0`. Caught in PR review; `engines.node` tightened `>=20` → `>=22.12.0` to stop claiming support the dep tree doesn't have. **`pnpm build` is in the gate** for the same class of bug: `convex-deploy` runs *before* the Cloudflare build, so a build-only breakage would ship a new backend against the old frontend with no rollback — and the build turns out to need no `VITE_*` values (they're runtime), so the gate stays secret-free. **Branch protection is LIVE** (enabled 2026-08-02): ruleset **`PR gate`** (active, targets `main` + `staging`, requires `Typecheck, lint & test`, strict off, empty bypass list) — one ruleset covers both branches where classic protection needs a rule each, so a red gate now genuinely blocks merge (confirmed on PR #159). UI gotcha recorded in the doc: **Target branches → Include by pattern takes the bare branch name** (`staging`), never `refs/heads/staging` — the UI adds the prefix, the REST API wants it explicit. Also noted there: the pre-existing `Basic safety net` ruleset is active but has an **empty include list**, so it enforces nothing and is fully redundant with `PR gate`. Runnable locally as **`pnpm gate`** (`lint && typecheck && test`, ~30s — the same three commands CI runs as separate steps). Fixed in passing: `biome.json` now excludes `**/.claude/**`, so a leftover Claude Code worktree under `.claude/worktrees/` no longer aborts `pnpm lint` in the main checkout with "Found a nested root configuration" (`.claude/worktrees/` was already gitignored, but Biome has `useIgnoreFile: false` and its `**/src/**/*` pattern matched *inside* the nested trees). Deliberately deferred to the full staging CI/CD ticket: build step in the gate, `pnpm check` format enforcement (21 errors on staging today), Biome coverage for `convex/`, deploy concurrency guard. See [`docs/ci.md`](./ci.md).
- **Storefront landing merchandising — buyer redesign PR3** 🚧 (ClickUp `86eybrhrt`, dev, the final slice — PR1/PR2 now on staging) — the store home's lead is merchandised per the locked direction-B design. **(1) Category rail resized, not removed** (`category-rail.tsx`): the full-size image-tile carousel was first replaced by text chips (it dominated the fold on a ≤3-category store), but losing the photography went too far — for a food seller the picture *is* the menu — so the tiles returned at `h-24 w-36` / `lg:h-28 lg:w-44`, ~a third of the old area, keeping images + product counts without burying the grid (scrim heavier at `from-primary/90` since 13px white names have to survive a pale photo). Tiles are **links to the existing `/c/{slug}` pages** (SEO/deep links intact, one navigation model); **store home only** — a briefly-shipped variant that also rendered the rail (own tile ringed) inside a category page was cut, since there the page already names the category and a row of siblings just competes with the products under it ("← All products" is the way out; no "All" tile for the same reason). **(2) "Popular this week" shelf** (`featured-product.tsx` + public `products.popularProducts` + pure `convex/lib/popularProducts.ts`): a horizontally scrollable row (Grab/Pandamart pattern, same on mobile + desktop) of up to **10** products **real orders** ranked top — rendered with the **shared `ProductCard`** so the shelf inherits every grid state (out-of-stock, Min N, quick-add vs Choose, crawlable `<Link>`s) and the two can't drift; distinct orders (not units, so a 40-pax bulk order can't outrank ten one-cake customers) over the last 7 days, revenue statuses only, **≥2 orders or the row hides** (a quiet store never makes a hollow claim); the 7-day window **rolls, ending now** (MYT-midnight anchor, no upper bound) so it never blanks on a week boundary. The unauthenticated query returns **ids only, and only storefront-listable ones** (sales volume never crosses the public wire) — **visibility is filtered server-side BEFORE the cap**, since ranking reads order lines with no product lookup and counter-only SKUs rank like anything else, so capping first spent slots on products that could never render with no rank-11 to back-fill (a stall seller whose top ten are counter-only got an empty shelf — PR review catch). Live stock/min-quantity stay client-side deliberately (reactive, already in the `products.list` the client holds). Also closed in passing: public `products.get` guarded `hidden`/`hiddenByCategory` but not `active`, so a bare id could pull an **archived** product's payload. Cache discipline = insights precedent: client sends an **MYT-midnight `since`** (`popularSince()`), server rejects unaligned anchors, scan bounded `take(500)` newest-first on `by_retailer`. Add reuses the grid's exact quick-add via extracted **`quickAddProductToCart`** (min-qty top-up + stock clamp, one author). **(3) Desktop grid 6→4 columns** (tiles ~50% larger; skeletons synced) — the old density existed so cards never outweighed the full-size rail tiles. See [`docs/storefront-landing.md`](./storefront-landing.md).
- **Storefront checkout — phone at checkout + WABA confirmation push (kill the wa.me drop-off)** ✅ (ClickUp `86eyf1rck`, dev) — the order **commits the moment the buyer taps "Place order"**; nothing depends on the buyer's wa.me send surviving Meta's in-app-browser interstitial (the drop-off that stranded orders as phone-less `pending`). **Phase 0 (no Meta dependency):** inside WhatsApp's own in-app browser (`isWhatsAppWebview`, UA sniff in `wa-auto-open.ts`) the `?send=1` auto-fire is **skipped** (manual button renders; param still consumed) and the fallback row copies the **order message body** ("Copy order message", EN+BM) instead of the wa.me URL — a wa.me link is exactly what just failed there. **Phase 1:** checkout requires a **MY WhatsApp mobile** (`myWaPhoneCheckoutSchema` client / `assertValidMyWaPhone` server — local `012…` normalizes to the inbound `60…` form; PDPA line EN/BM by store locale); with the approved `order_confirmation_utility` template configured (**env `WHATSAPP_ORDER_CONFIRM_TEMPLATE`** — unset ⇒ exact legacy `pending`+`?send=1` behaviour, code decoupled from Meta review; `getRetailerBySlug.confirmPushEnabled` keeps checkout copy truthful), `orders.create` inserts **`confirmed`** (+ activation stamp, customer linked at create) and schedules `notifyStorefrontOrderCreated` — the order's **one** outbound message (Oct-2026 per-msg billing), a `template` kind new to the `OutboundMessage` union (body params shortId/store/total; URL button = **tracking token**, its own Meta parameter namespace), sent transactional via `makeGuardedSender`, logged as `utility_template` + `templateName`. Send-site + `statuses`-webhook outcomes stamp `orders.confirmationPushStatus` (`sent`/`failed`/`recovered`, wamid correlation via new `by_confirmation_wamid` index — the webhook's `value.statuses` events were previously ignored entirely; adapter gained `parseStatuses`). Buyer sees "Order placed ✓ — confirmation sent to your WhatsApp" (no auto-redirect anywhere on this path); a **failed** push flips the tracking page to the manual Send card (recovery framing) + an amber "Couldn't reach the buyer" note on seller order detail, and the buyer's manual send **re-stamps their real number + relinks the customer record** (`recovered` — narrow: only failed pushes unlock the overwrite; replayed webhooks never regress it). Seller template editor notes the first confirm is the fixed Meta template (custom copy applies from the buyer's first reply). **Checkout echoes the normalized number back** ("We'll send your confirmation to +60 11-5939 9791 — check it's right", new pure `formatMyMobile`) — informs, never blocks, because a gate that can wrongly reject moves the loss from "stranded in chat" to "refused at checkout". **The push retries** (30s → 2m → 8m, pure classifier in `convex/lib/confirmationPush.ts`): transient causes (no response / 5xx / 429 / throttle codes) retry, unreachable-recipient and template codes terminate immediately, and an ambiguous 2xx is **never** retried (Meta has no idempotency key — the double-send guard). Failure is then split by **cause** (`confirmationPushFailureKind`): `unreachable` blames the number and offers the repair, `system` says it's ours and asks nothing — so a Meta outage never sends a buyer chasing a phantom typo, nor a seller chasing a fine customer. **Two repair routes, one shared `customers.moveOrderToPhone`** (moves CRM aggregates off the wrong record): the buyer's own **"Update my number"** on the order page (`orders.updateBuyerPhone`, token capability like `updateDeliveryAddress`, gated to failed pushes, rate-limited since each save costs a send) and the inbound-ORD relink. **Nothing gates the order page** — it stays readable and payable in every failure state, deliberately. **Non-final-total orders (mockup / `deliveryFeePending`) commit at create like everything else but DEFER the push** (86eyfq0w5, superseding the brief exclusion that wrongly returned them to the wa.me handoff): stamped `confirmationPushStatus: "deferred"`, the template fires from the gate-open sites (mockup approve/waive/decline-with-remainder, `setDeliveryFee`) once the price is agreed — always with a true final total — **replacing** the legacy free-form payment prompt there (window-less push-path chats couldn't receive it anyway; legacy orders keep it). De-dup = the **transactional claim** (`claimDeferredPush`): the gate-open mutation itself flips deferred→sending and schedules only when it won the flip with no hold remaining — serializable, so double-hold sends exactly once even under racing gate events; the action keeps hold+cancelled guards as defence. Cancel **clears** a deferred stamp (the promise dies with the order; tracking card also gates on `confirmed`); custom-only decline = cancellation, no confirmation. **The seller's unseen-order signal survives the status change:** confirm-at-create would have made `counts.new` permanently 0, killing the New chip/Home tile/amber-at-4h escalation, so New is now **`pending` OR unseen** (`orders.seenAt`, stamped by `markSeen` when the seller opens the order; gated on `confirmationPushStatus` being set so the historical confirmed backlog needs no backfill and counter orders stay out) — one shared `orderBucket` drives both the counts and the inbox predicate, so a chip can't disagree with its list. Fixed in passing: `assertValidMyWaPhone` accepted a landline (`03-…`) since it only checked 8–15 digits, so the server was weaker than the client — new `assertValidMyMobile` enforces the real MY mobile shape on the storefront paths; `moveOrderToPhone` skips the CRM move on a **cancelled** order (cancel already reversed the aggregates — re-running would eat a different order's count/spend, the trap `deleteOrderCascade` documents). See [`docs/order-lifecycle.md`](./order-lifecycle.md#the-confirmation-push--the-order-commits-at-place-order-86eyf1rck) + [`docs/order-inbox.md`](./order-inbox.md).
- **Storefront product pages — buyer redesign PR2** ✅ (ClickUp `86eybrhrt`, dev) — every visible product gets a URL: **`/{slug}/p/{productSlug}`** (route `$slug_.p.$productSlug.tsx`, pathless-underscore like category/checkout) with per-product SSR/OG (photo → cover → logo) + `Product` JSON-LD (`Offer`/`AggregateOffer`; quote-only products carry no offers block). **`products.slug`** auto-generated from the name at create (never a seller input; `by_retailer_slug` index; collisions suffix `-2`; emoji-only names pad to `item`), **stable across renames** (shared links must keep working — `update` only lazily assigns to legacy slug-less rows), archived/hidden keep their slug (no URL takeover; restore revives it); one-shot `products:backfillProductSlugs` (**run on prod at ship** — dev done 30 Jul). `getPublicBySlug` applies `list`'s exact visibility rules (hidden/archived → null → 404, extending the hidden-products no-leak promise). Frontend: the sheet's buy box extracted into **`product-purchase.tsx`** (`useProductPurchase` + OptionPills/PurchaseHints/CustomOrderCard/stepper/CTA pieces + `addVariantToCart`, the one author of cart-line snapshots) — `product-detail-sheet.tsx` is now a thin composition (behaviour unchanged) and **`product-page.tsx`** composes the same pieces as a real page (shared `StorefrontHeader` + "← All products" back link, variant-aware gallery: mobile snap carousel / desktop hero+thumbs, Copy-link chip via OS share sheet with clipboard fallback, fixed mobile purchase bar w/ measured `--storefront-bar-h` clearance). The hook's "new product opened" reset keys on **`product._id`, not the object** — the page feeds it a LIVE query result, so a stock decrement from someone else's order would otherwise wipe the buyer's options/quantity/custom note/uploaded photo mid-flow (the sheet was immune: it holds a snapshot). **Grid taps navigate to the page on EVERY breakpoint** (30 Jul revision: a mobile sheet hid the URL — the whole point of PR2 — on the platform where the address bar *is* the share affordance, and cost more screen on the smaller device). The sheet survives only as the **seller's draft preview** in the product editor (a draft has no public URL); its buyer-only props incl. the copy-link button were stripped. **Discoverability is wired, not assumed:** `ProductCard`'s photo/name/Choose are real `<Link>`s (a `<button onClick={navigate}>` is invisible to crawlers, ⌘-click and copy-link-address; photo link `aria-hidden` so SRs hear one door; unorderable → disabled button, since an `<a>` can't be disabled; `ProductGrid.storeSlug` now required) and **`/sitemap.xml` lists every visible product** via new `products.listForSitemap` (same visibility rules as `list`; slug-less rows skipped until the backfill, so no soon-to-change URL is published). **Every** storefront bottom bar is `fixed` + measured `--storefront-bar-h` clearance (product page, checkout, `CartBar`) so the powered-by footer is page content floating above it on every page — a `sticky` variant for checkout was tried and rejected on device (welded badge, checkout the odd page out); design-system rule #4 now says `fixed` outright and documents the keyboard question. Un-backfilled rows stay addressable via a derived name-slug fallback (`effectiveSlug`), so there's no un-openable window before the backfill runs. The page carries the **shared `StorefrontHeader`** + an "← All products" back link, mirroring the category page, so every storefront level looks like the same store. Deferred: dashboard copy-link buttons, seller slug editor. See [`docs/storefront-product-pages.md`](./storefront-product-pages.md).
- **Storefront checkout page — buyer redesign PR1** ✅ (ClickUp `86eybrhrt`, dev) — the storefront checkout is now a **real route** (`/{slug}/checkout`, `$slug_.checkout.tsx`), replacing the full-height bottom sheet (`checkout-sheet.tsx` deleted). Desktop finally has a layout: **two columns** (numbered form sections left, **sticky receipt summary** right with the CTA under the money it commits to) instead of a full-width bottom strip with 1,500px inputs; mobile keeps one column — summary first, then sections, with a **fixed bottom bar** (live total + CTA). Browser back returns to the store, refresh keeps the buyer's place, `noindex`. Fixes the audit list: **quantities are editable** (per-line steppers; minus at qty 1 = remove; `+` capped at live on-hand stock for hard-block variants with an "Only N in stock" reason — read from the public `products.list`, `orders.create` still the judge; custom lines stay qty-locked with a Remove chip), the **name field explains itself** ("Appears in your WhatsApp message so {store} knows who this order is for"), the field wall becomes **numbered decision cards** (1 Who's ordering → 2 How do you want to get it → 3 When → an unnumbered note card), the date step gains **one-tap day chips** (Today / Tomorrow / weekday — notice-aware, native `<input type="date">` still the source of truth; pure `src/lib/checkout-dates.ts`), and the **wa.me two-step is announced** ("Opens WhatsApp to confirm with {store} — nothing is paid yet"). The summary card carries the **Order Ticket** skin (dotted leaders, dashed rules, tabular digits, stamped TOTAL) — personality in the receipt, never in the form controls. Also folded in (ClickUp `86eye50qv`, same form, so it isn't redesigned twice): **the Google pin is the single source of truth** — a picked address collapses into a read-only "Location confirmed" card with only unit/floor + notes editable ("Wrong address? Search again" resets it), the search is the sole input before a pick, and manual entry is an explicit escape-hatch disclosure that's **hidden on stores that price from coordinates** (live Lalamove / radius-block — derived from the quote already subscribed to, no new server field). Plus **disabled-with-reason** on the CTA in both render sites. Form logic ported **verbatim** (schema, saved address, Google autocomplete, pickup picker → extracted `pickup-location-options.tsx`, date floors, min-order gates, static + live Lalamove quotes, `orders.create` → `/track/{token}?send=1`); **zero server change**. `CartBar` slimmed to a navigate; the product sheet's "Go to checkout" (86eybhqye) routes to the same page. PR2 = URL-addressable product detail, PR3 = landing merchandising. See [`docs/storefront-checkout-page.md`](./storefront-checkout-page.md).
- **Manual courier + tracking number on mark-shipped** ✅ (ClickUp `86eyehvk4`, dev) — outstation parcel sellers (J&T ambient, DD Cold Chain, Ninja Cold, Celsius) attach courier + consignment number when marking shipped; buyers get TikTok-style tracking through the EXISTING pipeline, no courier API (EasyParcel = separate phase-2 card). **The ticket was ~40% stale** — a full manual `orders.carrierTrackingUrl` pipeline already existed (seller card, WA shipped copy EN/MS/ZH, track-page CTA); the real build was courier NAME + tracking NUMBER as first-class order fields (`orders.courierName`/`trackingNo`, dev-only widen; NOT on `deliverySnapshot` — that's the frozen pricing snapshot) with the URL auto-derived via the new pure registry `convex/lib/couriers.ts` (6 link-pattern couriers + name-only cold-chain entries + Other w/ pasted link; shared `resolveShipmentFields` resolver on every write path). Entry points: an optional skippable **mark-shipped prompt** (delivery orders w/o tracking; remembers last courier per device; rider-managed Lalamove orders never see it) + the order-detail card upgraded to courier/number/copy ("Shipment tracking", `setShipmentTracking` replaces `setCarrierTrackingUrl`). **Follow-up `86eyff02p`: manual courier is parcel-sellers-only** — the two-tab prompt was wrong for a vendor whose delivery method IS Lalamove, so `getDeliveryJob.bookingEnabled` (= `deliveryBooking.enabled`, coupled 1:1 with the Lalamove delivery-charge choice) now drops the courier form entirely for them: the prompt is rider-only (bookable → `Book a rider` + a quiet "ship without a rider" footer button; blocked → the reason via the shared `dispatchBlockCopy`, moved to `src/lib/dispatch-block.ts` as an exhaustive `Record`, + `Mark as … anyway`). The tracking card goes read-only only while a rider is **actually** handling the delivery (`blockReason === null || hasActiveRiderBooking`) — **not** on vendor type alone, since a Pro→Starter downgrade leaves `deliveryBooking.enabled` true + `plan_gated` forever, and `setShipmentTracking` has no other caller, so that would strand the store with no way to record a consignment number at all. Server deliberately stays permissive — the webhook writes `carrierTrackingUrl` itself and a mode switch must never trap an order. **Zero new WhatsApp sends (owner call, Meta bills per-msg from Oct 2026):** tracking entered at ship time rides the already-outgoing shipped update (`📦 courier — tracking no.` line, EN/MS/ZH, also in stage updates + `{courierName}`/`{trackingNo}` template placeholders); tracking added later is track-page-only — no auto-send, no resend button. Track page shows a copyable courier+number card (cold-chain couriers have no link — copy is the path); CSV gains Courier/Tracking-no columns. See [`docs/fulfilment.md`](./fulfilment.md#manual-courier--tracking-number-on-shipped-2026-07-29-clickup-86eyehvk4).
- **Product setup wizard — step-by-step create + question-first edit form** ✅ (ClickUp `86ey9udvz`, dev) — the add-product overhaul for non-tech sellers: **create = a 5-step wizard** (`/app/products/new`, `product-wizard.tsx`) asking plain-language questions that *derive* the config — Name it → "Does the buyer pick anything?" (preset chips, **one axis max**, values as chips) → Price (per-choice or single, "Same price for all", per-choice SKUs behind a "+ Add item codes" link) → "How do you prepare orders?" (Made to order = zero stock UI / From stock = steppers) → Review (buyer-eye preview + per-row Edit + optional publish settings: Visible/Hidden toggle + category picker when the store has categories + a **More options** disclosure: mockup approval (made-to-order only), custom line, order rules, and **"Open in the full editor"** which hands the whole draft to `ProductForm` prefilled — create/edit parity by construction; header ✕ cancels w/ confirm-if-dirty). Maps onto the existing `ProductFormSubmitValues` — **zero backend change**. "Skip — use the full form" → `?form=full`. **Edit = the restructured full form** (steppers are wrong for editing): summary strip in words ("3 choices by Size · Made to order · RM 12–28", `src/lib/product-summary.ts`), card order Basics → Photos → **Pricing & choices** → **Order rules** (min quantity + min notice grouped — both constrain how a buyer may order) → **Where it appears** (Visibility + Categories moved last; `CategoryPicker` gained `embedded`); `VariantEditor` rebuilt question-first — mode segmented control, product-level fulfilment cards w/ **"Vary per choice"** override (auto-open when mixed), stock inputs only for tracking rows, and an **Advanced** disclosure (mockup approval w/ indeterminate, custom line, second axis, per-choice SKU/photo/on-sale) that auto-opens when in use or when a submit issue points inside. Deliberate deltas: desktop variant table removed (one responsive rows list + bulk-fill), made-to-order rows hide stock entirely, SKU lives under Advanced; `rebuildRows` now seeds new combos' flags from row 0 (no silent track-stock rows on made-to-order products). See [`docs/product-setup-wizard.md`](./product-setup-wizard.md).
- **UI cleanup — counter header dropdown + marketplace removal** ✅ (ClickUp `86eyd67y1`, dev) — two UI fixes. (The image-card loader this ticket also scoped was superseded by the parallel `AppImage` primitive that landed on staging — see the AppImage bullet below — so it was dropped from this branch to avoid a duplicate; the merge adopts `AppImage` everywhere.) **(1) Counter-checkout header dropdown:** the two competing pill buttons (`StoreQrChip` "Show QR" + `NoScanControl` "No scan?") collapsed into one accent **`New order`** dropdown (menu items: Show store QR / Enter phone number / Cash sale) — merged into a single `CounterCheckoutActions` component; all behaviour preserved (token auto-provision, `listOpenSessions` scan-watch auto-nav, manual bind, anon sale). New reusable `ui/dropdown-menu.tsx` (radix `DropdownMenu`) primitive — the repo had Popover but no menu. See [`docs/counter-checkout.md`](./counter-checkout.md). **(2) Marketplace integrations removed:** we're no longer building Shopee/Lazada/TikTok Shop/StoreHub connectors — the dashboard "Sales channels" teaser (`app.index.tsx`), Settings "Integrations" tab + `IntegrationCard` (`app.settings.tsx`), and `shopee-icon.tsx` are deleted; `?tab=integrations` deep-links fall back to Store. The landing FAQ (`faq_a_5`) + MS `how_sub` no longer **promise** connectors (rewritten to the real WhatsApp + storefront + counter story); the neutral variant comparison (`bento_variants_*`, "Variants, like the marketplaces") and the TikTok *social* link stay. The structural `channel` field in `convex/schema.ts` is untouched (messaging abstraction, not a marketplace).
- **Mandarin (zh) locale — Phase 1: plumbing + already-localised surfaces** 🚧 (ClickUp `86eybjw5n`, dev, phase 1 of 3) — promotes `Locale` from `"en"|"ms"` to `"en"|"ms"|"zh"` end-to-end for every surface that was already localised (landing/pricing/cost via Paraglide, WhatsApp copy, transactional emails, order-status labels, opt-out keywords). **One canonical `Locale` union** — new `convex/lib/locale.ts` (`Locale`, `LOCALES`, `pickLocale`, `OG_LOCALE`) replaces 5 independent local declarations; every `locale === "ms" ? A : B` ternary in the codebase became an exhaustive `Record<Locale, …>` lookup, so a future 4th locale is a compile error, not a silent English fallback. zh copy (Malaysia-flavoured Simplified Chinese, drafted — **blocked on native-speaker review before merge**) populated across `messages/zh.json` (full Paraglide catalog), `whatsappCopy.ts` (confirm/status/system messages, PDPA notice, powered-by line, pickup/delivery labels), retailer + billing emails, order-status/stage labels, and `wabaLimits.ts` opt-out keywords (停止/退订 → new `zh_stop_keyword`/`zh_unsub_keyword` sources). Schema widened dev-only (optional `zh` fields, no backfill) on `retailers.locale`, `messageTemplates`, `statusLabels`, `orderStages[].label/.description`, plus `optOuts.source`. Settings gets a 中文 language option + a 3-way per-locale WhatsApp template editor; landing nav's EN⇄BM toggle became a 3-way dropdown. CJK renders via a system-font fallback stack (`PingFang SC`/`Microsoft YaHei`/`Noto Sans SC`) — deliberately no bundled webfont yet, revisit if flagged. **Corrected a stale doc claim in passing:** `docs/i18n.md` said `/track/<token>` was Paraglide-localised — it isn't, it runs on the separate backend `retailerLocale` system; doc now documents both systems and the add-a-locale checklist. **Deliberately untouched (Phase 2/3):** storefront/checkout string extraction, the tracking page's own ternaries, PDF receipts/invoices (still English-only, and `sanitize()` still strips non-Latin-1 — needs `fontkit` + an embedded CJK font before zh is safe there), CSV export headers, the `/app` dashboard sweep, and the poster's `PosterLocale` toggle (zh strings exist in the catalog, just not reachable from the toggle yet). **Note:** the landing full-page rewrite below deleted the old `feature_1..9_title/body` keys (replaced by `bento_*`) — this branch's zh translation targeted the old scheme for `feature_2_body` (a "3 languages" mention); that mention survives via `faq_a_4` instead, which the rewrite left untouched. See [`docs/i18n.md`](./i18n.md).
- **Landing — design-faithful How-it-works mockups, K Frozen Food as the demo brand, lockup removed** ✅ (dev; follow-up to the full-page rewrite below) — three changes. **(1) How-it-works mockups rebuilt to the design reference** (`how-it-works-mockups.tsx`): the first pass abstracted the design's mockups into flat token-coloured cards; now they match the reference — floating, slightly-rotated cards with their own shadows and **no shared frame** (the bordered `bg-muted/30` wrapper is gone). Steps 1/3 are literal WhatsApp surfaces (`#ECE5DD` wallpaper + `#DCF8C6` outgoing bubble, hero-phone precedent — a WA chat can't theme-flip): step 1 = status bubble w/ link-preview card + a dashed "Scan to order" QR poster card beside it (side-by-side engages at `lg`, stacks below — the md-width-cramping rule); step 3 = order bubble + confirm reply w/ blue tracking link. Steps 2/4 mock Kedaipal's own UI in theme tokens: step 2 = storefront phone (gradient header, avatar chip, 2-col product-grid tiles w/ striped photo placeholders + "MIN 2" badge, cart bar w/ count-badged 🛍 + navy checkout CTA); step 4 = navy due-today banner pill + two floating inbox cards whose item rows carry per-line prices (keys restructured `how_mockup_4_order_*_item_N`/`_price`, old `_items` blobs dropped) and whose badges reuse the **real `StatusBadge` palette** (blue Confirmed / amber Packed + Buyer-marked-paid, dark-mode variants included). Section chrome per design: `border-y` + `bg-muted/30`, number circles now mint-on-navy `font-heading` (56px at md) with shadows, step tags as neutral white pills, mockup column top-aligned. `QrPattern` extracted from `counter-checkout.tsx` into `landing-ui.tsx` (shared). **(2) Demo brand swap Dapur Nadia → K Frozen Food** (real consented customer): hero chat thread + how-it-works mockups + feature-bento slug pill (`k-frozen-food`) + counter-checkout poster URL (was `lekor-mrganu`) + poster test fixture; avatar initials DN → KF. **(3) K Frozen Food logo lockup + disclaimer removed** from Real sellers (owner call — the archetype grid + stat card stand alone, matching the design, which has no named-customer pill); `proof_live_customer_*`/`proof_disclaimer` keys and both logo assets deleted.
- **Landing — full-page rewrite to the "Landing Rewrite" Claude Design reference** ✅ (dev; extends `86eyc1awz` — the same design file, now implemented across the whole page, not just two sections) — `/` now follows the design's section order exactly: Hero → Real sellers → Problems → **Payment handshake (new)** → How it works → **Counter checkout (new)** → Features (bento rewrite) → Founding 10 → Pricing teaser → FAQ → Final CTA. **Hero**: new positioning "Stop losing orders. **Stop chasing payments.**" (both universal pains, second highlighted), ICP-naming subcopy (cake decorators/kuih/frozen/stall owners), and the raster phone screenshot replaced by a **rendered CSS WhatsApp chat** (`hero-phone.tsx`, Dapur Nadia thread: ask → storefront CTA → order message → auto-confirm; WhatsApp trade-dress colours deliberately literal — a WA chat can't theme-flip) with floating "New order" + "Payment matched ✓ · Receipt sent" badges; marquee re-seeded with the real cohort (keropok lekor, central kitchens, fruit & fish, service bookings). The now-unused `ResponsiveImage` + whatsapp/storefront raster pipeline deleted (optimize-images script kept, empty ASSETS). **Payment handshake** (`payment-handshake.tsx`): pain #2 made concrete — buyer bubble → seller claim-card (Confirm/Not received) → auto-receipt mock, three proof bullets, HitPay-in-flight note. **Counter checkout** (`counter-checkout.tsx`): pain #3 — CSS QR-poster mock ("At the stall") + one-inbox/one-stock story. **Features** rewritten from icon cards to a **bento with per-card CSS mini-mocks** (dark handshake card on `bg-cta-mesh`, pairing-code chip, slug pill, made-to-order rows w/ mockup-approved state, variant pills w/ struck-through sold-out, delivery fee chips, CRM customer card; tiles [2+1]/[1+2]/[1+1+1] — inbox card deliberately span-1 so the grid has no holes; inner 2-col mocks engage at `lg` to avoid md-width cramping). **Problems #3** recopy: "The stall and the chat don't talk". **Pricing teaser**: heading "No cut of your sales", tier feature lists match today's truth (handshake+receipts on Starter, radius fees + insights on Pro, courier-quoted + broadcast on Scale), Scale re-tagged "High-volume kitchens & multi-outlet sellers" (reseller-network positioning + landing band table dropped per ICP update — /pricing untouched), founding line now carries live `{remaining}`, redundant below-grid founding line removed. **FAQ**: primary six per design (new stall question `faq_q_12`; payments/mockup/data/founder answers refreshed), rest behind "See all"; **FAQPage JSON-LD re-mirrored** + SEO description updated. **Founding**: sub parameterized on live `{taken}` ("N sellers already pay monthly…", generic phrasing at 0 so SSR never claims "0 sellers"), perk copy synced. Dropped from the page per design: `SetupStrip`, `CostCta` (the /cost page + nav link stay), dead `SlowYesStrip` — components + ~75 orphaned message keys deleted (EN+BM parity kept, catalog-parity test green). All new copy EN+BM. Original two-section pass (`86eyc1awz`): **How it works** drops the old "customer messages Kedaipal's shared WhatsApp number first" bot framing (factually wrong — buyers never message Kedaipal directly) for the real link-first flow: **Share** (one storefront link everywhere) → **Browse** (cart on the storefront) → **Close** (checkout posts back into WhatsApp with an order ID) → **Run** (one inbox). Renders as a **static vertical timeline** — numbered mint-on-navy circles (checkmark for step 4) connected by a dashed line, all four steps in normal document flow with scroll-reveal (`FadeIn`) — **not** the old click-to-reveal stepper, so the `?step=1..4` URL param and its `validateSearch` schema were removed from `src/routes/index.tsx` entirely (nothing else read it). Steps 1–2 carry a row of pill tag chips under the body copy (WhatsApp status/IG bio/Counter QR poster; Variants/Live stock/Fulfilment date/Delivery or pickup), steps 3–4 don't. The four rendered CSS/HTML mockups (`how-it-works-mockups.tsx`, built from semantic tokens, no raster screenshots) sit beside each step's text on desktop, stacked below on mobile. **Real sellers, real orders** replaces the dead, never-mounted `social-proof.tsx` with a **3-column grid**: a 2×2 block of unnamed seller archetype cards (keropok lekor stalls, central kitchen, fruit & fish sellers, tent-wash service) beside a **tall "why they switch" stat card** that spans both rows (CSS Grid stretch — no explicit row-span hack needed, the sibling 2×2 block sets the row height); header is an asymmetric split (left-aligned eyebrow+heading, right-aligned subhead) instead of centered. Mounted right after `Hero`, before `ProblemStrip`. All copy in EN+BM via Paraglide `m.*` keys (`how_step_*`/`how_mockup_*`/`proof_*`).
- **Shared `AppImage` primitive — skeleton → fade-in → labelled fallback on every image** ✅ (ClickUp `86eybq1uk`, dev) — before this, **zero** of the app's 29 raw-`<img>` render sites had any `onLoad`/`onError` handling; a dead storage URL or a slow connection just showed a blank box. `src/components/ui/app-image.tsx`: `loading` (Skeleton overlay, image `opacity-0`) → `loaded` (fades to `opacity-100` over ~200ms, `motion-reduce:transition-none`) → `error` (muted box + `ImageOff` + the alt text, **terminal** until `src` itself changes — no retry-loop hammering storage); unset `src` renders the same fallback with zero network request. **The cached-image trap**: a callback ref checks `img.complete && naturalWidth > 0` at mount (commit phase, before paint) so a browser-cached photo (SPA back-nav into the storefront) never flashes a skeleton. `blob:`/`data:` upload previews (`URL.createObjectURL`) auto-skip the skeleton (already instant). Two sizing modes via `fill` (default `true` = image crops to fill the `aspect` box via `objectFit`; `false` = `aspect` becomes the image's own intrinsic-ratio classes, e.g. `"h-8 w-auto"` — needed for fixed-height/auto-width brand-mark SVGs, where forcing `w-full` inside an auto-width wrapper is the classic indefinite-percentage CSS trap). `priority` (storefront cover, first product-grid row) skips the lazy hint + requests `fetchPriority="high"`; `$slug.tsx`/`$slug_.c.$categorySlug.tsx` loaders now expose `coverImageUrl` distinctly from the OG-image precedence chain so `head()` can `<link rel="preload">` the actual LCP element. Migrated every call site except two deliberate holdouts: `store-poster.tsx` (print/PDF-export — a lazy or opacity-0 image can print blank; has its own `new Image()` onload/onerror gating) and `landing/responsive-image.tsx` (build-time `<picture>`/srcset for static optimized assets — a different concern from AppImage's runtime Convex-hosted URLs). `ZoomableImage`'s inner thumbnail now wraps `AppImage`, covering 6 downstream call sites (billing QR, product detail, order proof/mockup, tracking POD) for free. See [`docs/design-system.md`](./design-system.md#images-always-render-via-appimage-2026-07-24).
- **Lalamove delivery — checkout quote, one-tap dispatch, auto tracking** 🚧 (ClickUp `86eyb5hrf`, dev, launch week) — end-to-end rider delivery on the Lalamove Open API v3 (Fruit Hut launch). **BYO-ONLY** (revised 21 Jul, supersedes the built-then-removed master fallback/spend-cap/rebill surfaces — Kedaipal has NO Lalamove account, never books/pays on a seller's behalf): seller's own key pair on `retailers.deliveryBooking` is the sole credential source, **required to enable**; sandbox⇄production inferred from the key prefix (`pk_test_` → sandbox), zero env vars. Lalamove pricing option (Settings → Fulfilment → Delivery charge) links the **vendor setup guide** (`/guides/lalamove-setup.html`, print-ready) and surfaces the **webhook URL with one-tap copy** — each vendor registers it in THEIR Partner Portal (v3); skipping it degrades gracefully (booking works, transitions go manual). **Reuses the delivery-charge seam** (ticket's `deliveryOrigin` was stale — `businessAddress` is the origin): `deliveryConfig` gained `mode:"lalamove"` (live buyer-paid quote at checkout, `onUnquotable: arrange→deliveryFeePending | block`) beside flat/radius (kept — the no-Lalamove choice; pricing ⊥ booking). Checkout quote is **tamper-proof**: action records the fee server-side (`deliveryQuotes` row), `orders.create` accepts only the row id (coord-matched, 30-min bound, consumed once). Dispatch = two-tap re-quote → confirm (variance vs buyer-paid shown) → `deliveryJobs` ledger row (one ACTIVE job/order, atomic), **or opt-in prompt-to-book on packed** (`deliveryBooking.promptBookOnPacked` — marking a paid, due-today order packed auto-opens the confirm dialog client-side; never a silent charge); rider contact must be +60 (non-MY buyer → seller-fallback w/ buyer number in rider remarks); `POST /webhook/lalamove` (per-retailer secret via the job row, signature formula **confirmed in sandbox**: HMAC over ts/POST/path/`JSON.stringify(data)`) drives picked-up→`shipped` (live tracking link) and completed→`delivered` via `applyStatusTransition`; job failures email the seller + one-tap rebook, order never regresses. **Proof-of-delivery photo**: every booking requests POD (`isPODEnabled`); on completion the rider's drop-off shot is fetched from `GET /v3/orders`, stored as own blobs (`deliveryJobs.podImageStorageIds`, cascade-deleted), WhatsApp'd to the buyer as a delivered follow-up (EN+BM caption) and shown as thumbnails on the delivered dispatch card — sandbox has no riders, so the photo itself is a first-prod-booking check. Pro-gated (`PLAN_FEATURES.delivery`, clear un-gated). Sandbox E2E passed 21 Jul (quote→book→webhook→cancel). Pre-ship polish (23 Jul): hard-delete/account cascades cover `deliveryJobs`+`deliveryQuotes` (+ daily stale-quote purge cron); **per-product notice** `products.minNoticeDays` (checkout floor = max(store, strictest cart item), server re-validated, "Requested date" copy for custom carts); **browser order alerts** (chime + system notification for new order / booking failed, per-device, Settings → Store — see [`docs/order-notifications.md`](./order-notifications.md)). Remaining: Naim's own account/keys/wallet/webhook (Arif walks him, `86eyb5w24`), first-prod-booking driver-progression check. See [`docs/delivery-lalamove.md`](./delivery-lalamove.md).
- **Minimum order rules — per-product min quantity + store min order value** ✅ (ClickUp `86ey9unyx`, dev) — Sue Chef Kitchen's two asks, one primitive: `products.minQuantity` (judged on the **summed** quantity across the product's cart lines — product-level by design: "min 20 pax" mixes flavours, per-variant would wrongly force 20 per flavour) + `retailers.minOrderValue` (sen, vs the item subtotal before fees, boundary inclusive — the freeAbove posture). **All-tier**, both optional/default-off (0/1 → unset via sanitizers, zero migration). One pure shared module `convex/lib/minOrderRules.ts` imported by BOTH the authoritative gate (`orders.create` throws before totals) and the storefront pre-check, so the two sides can't disagree. **Counter checkout exempt** (seller is standing there — the min-notice posture; pinned by test); **custom/price-on-quote lines exempt** (qty-locked 1, real value settled on the quote — a RM0 custom cake must not bounce off a RM100 floor). Buyer UX so the rule is never a checkout surprise: "Min 20" card chip, detail-sheet hint + stepper that **opens at the remaining amount** toward the minimum (minus floors there on no-options products), quick-add tops up to the min in one tap (clamped to remaining stock), checkout shows per-line "add N more" hints + an alert above the total + disabled-with-reason CTA. When all-hard-block stock erodes below the min (min 20, 15 left), the product goes **unavailable-with-reason** ("Not enough stock" badge + disabled add + sheet explanation via `minQuantityUnreachable`) instead of a stepper pinned at a quantity checkout would reject; a custom line keeps its own CTA live. Seller UX: min-qty input in the product form (amber warning when hard-block stock < min), "Min N" chip on the products list, "Minimum order value" card in Settings → Fulfilment next to the notice card. See [`docs/minimum-order-rules.md`](./minimum-order-rules.md).
- **Delivery charge — flat fee + radius bands** ✅ (ClickUp `86extzdr8`, dev) — delivery orders can finally carry a charge (`total === subtotal` was wrong for every postage/rider seller). Seller picks a mode in Settings → Fulfilment (inside the Delivery card): **Free** (default, unset config, zero migration), **Flat** (+ optional free-above-subtotal threshold, boundary inclusive) — **all-tier**, it's a correctness fix — or **Radius bands** (**Pro**, `radiusDelivery` in `PLAN_FEATURES`, set-gated/clear-un-gated/admin-bypass like chargeablePickup): straight-line (haversine) km from a new owner-only `retailers.businessAddress` (privacy: often the seller's home — never in the public payload, and the public quote strips distances so probes can't trilaterate it; the ticket's original flat/weight_tiers spec was superseded by the 15 Jul radius comment — weight_tiers parked). **One pure resolver, three callers** (`convex/lib/delivery.ts` `resolveDeliveryQuote`): the public `delivery.quote` (live checkout preview — fee visible before "Send order"), `orders.create` (authoritative; frozen `deliverySnapshot {fee, mode: flat|radius|manual, distanceKm?, bandMaxKm?}` + `deliveryFee` mirror, and the create result **echoes the fee** so the wa.me message uses stored numbers), and `updateDeliveryAddress` (**re-prices** — the surface tickets always miss). Rides the `computeOrderTotals` extras seam (now `{quotedAmount, pickupFee, deliveryFee}`); counter checkout + self_collect untouched. **Out-of-range policy per store**: *block* (checkout disabled-with-reason, server enforces) or *arrange* → order lands **`deliveryFeePending`** — a second mockup-style payment hold (confirm copy EN+BM holds the ask, claim/mark-received reject, reminder cron skips) released by seller `orders.setDeliveryFee` (amber order-detail card, 0 = free, locked once payment claimed; `notifyDeliveryFeeSet` sends the held payment ask with charge + final total; double-hold with mockup resolves to exactly one prompt). Fee line on every total surface (checkout breakdown, wa.me, WA confirm, tracking, order detail w/ km audit, PDF + pending note, CSV "Delivery fee" column). Provider-quoted shipping (Lalamove/J&T) later = a new config arm + snapshot mode, same seam. See [`docs/fulfilment.md`](./fulfilment.md#delivery-charge--flat-fee--radius-bands-2026-07-16-clickup-86extzdr8).
- **"Powered by Kedaipal" badge — buyer-facing growth loop** ✅ (ClickUp `86ey8zh3r`, dev) — turns every storefront visit + WA order confirmation into a Kedaipal impression with a click path (the digital twin of the physical Store QR Poster; distinct from the retailer-facing Founding badge). **Always-on, no retailer toggle** (locked, Arif 13 Jul 2026 — universal or the loop doesn't compound). Two surfaces: **(1) Storefront footer** — a quiet centered `StorefrontFooter` reusing the **Store QR Poster's powered-by lockup** (mint "POWERED BY" pill `#B9D9CC`/`#7BA394` over the Kedaipal wordmark `/poster/kedaipal-lockup.svg`; poster `mm`/`pt` units → responsive web units) so poster + storefront share one uniform brand mark, muted so it never competes with the seller's brand — on **every** `kedaipal.com/<slug>` page (mounted on both `$slug.tsx` + `$slug_.c.$categorySlug.tsx`, `mt-auto` to sink below the fixed CartBar), linking to `kedaipal.com?src=storefront_badge` (`?src=` = the repo's reserved PostHog attribution tag) in a new tab. **(2) WA confirmation line** — pure locale-aware `poweredByLine(locale)` (EN "This shop runs on Kedaipal 🛒 kedaipal.com" / BM "Kedai ini guna Kedaipal 🛒 kedaipal.com") appended at the **send site** (new `sendPaymentMessage` `footerLine` param for the normal confirm; direct append for the mockup-pending confirm) — **not** inside the override-able `confirm` template, so a seller's template edit can't strip it. Lands as the message's last block, under payment details. **Scope = storefront online orders only**; counter-checkout confirmations stay clean (physically-present buyer → the printed poster is their growth surface). See [`docs/powered-by-badge.md`](./powered-by-badge.md).
- **Hard delete orders (admin only)** ✅ (ClickUp `86ey8fr8t` built the erase; `86eyaqzpd` restricted it to admin; `86eyhz189` always-audit + own-store UI, dev) — a **permanent** order erase, distinct from cancel: cancel keeps a `cancelled` tombstone and notifies the buyer; hard delete wipes the order and everything derived from it and is **silent** (no WhatsApp/email — it's for test/spam/duplicate orders). **Kedaipal admin only:** both mutations resolve `requireOrderAccess`/`requireRetailerAccess` then throw `Forbidden` unless the caller **is an admin** (`isAdmin` — allow-list membership, **not** `access.actingAsAdmin`), so a plain store owner is rejected server-side (they use Cancel instead — permanent erasure sits with Kedaipal support). The gate is ownership-agnostic on purpose: an admin can erase in **any** store incl. one they own (owner branch → `actingAsAdmin:false`, which the earlier `actingAsAdmin` guard wrongly blocked — the PR-review edge). **Every erase is audited** (`86eyhz189`) — one `adminAuditLog` row **per erased order** (`targetId` = the order id), **including own-store**: new `logDestructiveAdminAction` shares one inserter with `logAdminAction` and drops only its owner-write no-op, because the ordinary policy (owner edits are routine + recoverable, not worth tracing) inverts for an irreversible erase — the deleted row can't be asked who removed it. Bulk went one-row-per-batch → **per order**: a batch row carries a single `retailerId`, so a two-store batch filed the whole erase under whichever store was last, and a bare count answers "how many" when the question is "*which* order is gone". One shared `deleteOrderCascade`: **(1)** reverse create-time effects — restore hard-block stock, decrement customer `orderCount`/`totalSpent`, un-meter usage from the **creation** month — **but only if the order isn't already `cancelled`** (a cancelled order reversed them on cancel; re-running double-counts — the one real trap, so `reverseCancellationEffects` was extracted from `applyStatusTransition` and shared); **(2)** delete owned storage blobs (buyer image, payment proof, mockup image(s), deduped); **(3)** delete the `orderEvents` timeline; **(4)** unlink the counter session that spawned it (new `counterCheckoutSessions.by_order` index → `orderId: undefined`); **(5)** delete the row. Subscription invoices are billing artefacts (not order-tied) — untouched; the payment-reminder cron already no-ops on a missing order. **Both `deleteOrder` and bulk `bulkDeleteOrders` are admin-only, not plan-gated** (erasure is an ops action, not a paid feature — applies equally to Starter/Pro/Scale; the bulk mutation's old Pro `orderInbox` gate was dropped as dead code since a plain owner never reaches it); bulk still caps at 100/batch and re-checks access per order. UI (**hidden, not disabled**): the "Delete permanently" danger action on order detail and the bulk-bar item render for **admins only** via one shared `canHardDeleteOrders({actingAsAdmin, amIAdmin})` (`src/lib/admin-actions.ts` — `api.billing.amIAdmin`, false until both resolve so nothing flashes in mid-load) — a plain seller never sees either. The `amIAdmin` half is `86eyhz189`: gating on `actingAsAdmin` alone made an admin act-as **their own store** just to erase an order in it, a detour the server never required (`actingAsAdmin` is false on the owner branch); `actingAsAdmin` stays as the fast path so another store's order detail still renders with no extra round-trip. The order-detail helper line now names what the act-as banner used to imply — sellers don't get this action, and the erase is logged. Confirm dialog (type-`DELETE` gate) with extra warning when paid/delivered → vanishes from CSV/revenue → back to inbox + toast. See [`docs/order-lifecycle.md`](./order-lifecycle.md#hard-delete--permanent-erase-deleteorder--bulkdeleteorders).
- **Product categories — storefront browse-by-category** ✅ (ClickUp `86ey81n63`, dev) — retailer-defined **categories** (many-to-many, ≤10 per product; "categories" everywhere — no collections/category naming split) driven by Sue Chef Kitchen's scroll-wall menu. New `categories` + `productCategories` junction tables (pure browse layer — never frozen onto orders, never a sellability gate; counter checkout untouched). **Categories can be hidden** (`categories.hidden`, orthogonal to archive, un-gated): a hidden category leaves the rail + 404s its page, and any product whose EVERY category is hidden drops off the storefront (denormalized `products.hiddenByCategory`, still counter-sellable) — a product also in a visible category stays shown. Managed via a ⋯ menu (Edit/Hide/Archive) + "Hidden" badges; picker shows hidden pills + an all-hidden warning. Buyers get a **category rail** on `/$slug` (tiles auto-hide at zero visible products) + nested **`/$slug/c/$categorySlug`** pages (own SSR/SEO head, 404 for unknown/archived slugs, shared cart/grid/checkout — `ProductGrid` grew a `products` override, no fork; the `$slug_` route-file underscore is load-bearing since `$slug.tsx` is a leaf). Sellers manage at **`/app/products/categories`** (under Products; entry via a Products-header action — icon-only on mobile with a back arrow on the page): SortableList reorder, live counts, archive/restore, per-row copy of the shareable deep link, edit dialog w/ auto-slug + image + drag-to-arrange-products; product editor gets a `CategoryPicker` chip section (membership saved LAST so a gate error can't block the core save). **Pro-gated with escape hatches** (`categories` in `PLAN_FEATURES`): building structure = Pro; archive/restore + clearing assignments + all reads stay all-tier (downgrade never traps — chargeablePickup precedent); buyer rendering ungated; admin act-as bypasses + audits. Fixed in passing: `useCart` persist-before-hydrate race (could wipe a stored cart on remount), `getConvexHttpClient` now works in client-side loader runs (SPA navs between storefront routes silently no-oped), `deleteUser` cascade gaps (orphaned `productVariants` + blobs; categories/junction added). See [`docs/product-categories.md`](./product-categories.md).
- **Seller Insights v1 — revenue, top products, charts** ✅ (ClickUp `86ey5tfrz`, dev) — a mobile-first `/app/insights` that turns the order log into a picture: KPI row (**revenue earned** confirmed→delivered **vs collected** `paymentStatus==="received"`, order count, AOV), **top products** (bar list grouped by `productId+variantId` from the order-item snapshots, revenue⇄quantity toggle, thumbnails), a **revenue trend** (hand-rolled bars, MYT day buckets ≤31d / week buckets above) and a **payment-method donut** (hand-rolled SVG, **no chart library** — monochrome mint by opacity; slices weighted by collected revenue so Σ === collected). `pending`/`cancelled` excluded from every figure (so cancel-after-paid drops out of both). **Cache discipline is the spine:** `analytics.getInsightsRange({from,to,bucketing})` is the heavy query over a **closed** range ending ≤ yesterday — MYT-midnight args, **no `Date.now()`**, so it's data-cached and a day-rollover can't stale it — paired with `getTodayStats()`, a tiny **live** query for today; the client merges them onto one trend grid via the shared pure `convex/lib/insights.ts`. Scan is an **indexed `_creationTime` range read** on `by_retailer` (widened by a skew buffer, then filtered on the `createdAt` revenue anchor), bounded by a 10k cap with a `capped` amber banner — no schema change, no rollup (aggregate is the v2 escape hatch). **Pro-gated through the shared `PLAN_FEATURES.insights` seam** (`hasFeature`/`assertPlanFeature`; trial=Pro, comped=full) **server-side** (queries return `{gated:true}` for Starter) — **Starter sees a blurred sample teaser + `wa.me` upgrade CTA**; admin act-as gates on the **retailer's** plan. New primitives: themed `ui/calendar.tsx` (`react-day-picker`, range mode) + `ui/sheet.tsx` (mobile bottom-sheet). Discoverability drove a **mobile bottom-nav restructure to 5 tabs + More**: the bar keeps the daily loop (Home · Orders · Counter · **Insights** w/ Pro chip · More) and the management surfaces (Products, Customers, Store poster, Settings) moved into a floating **More panel** above the bar (radix Dialog, anchored via published `--app-bottomnav-h`; More reads active on its child routes) — the scalable home for future low-frequency surfaces like Broadcast, chosen over a cramped 7th tab. Also a lock-badged **Insights** card on `/app` home + a desktop sidebar link (desktop unchanged). See [`docs/insights.md`](./insights.md) + [`docs/app-redesign.md`](./app-redesign.md#mobile-bottom-nav--5-tabs--more).
- **Poster templates v3 — counter-only / online-only (giant DuitNow-style QR)** ✅ (dev) — `/app/poster` gains a **"Poster type"** toggle (first control: Both QRs / Counter only / Online only, session-only, default Both) rendering two new single-QR templates: shared header/band/footer/phone-mockup, hero badge, one **80mm** `level="H"` QR (72mm after iOS's ~10% shrink; 80mm is the **ceiling** with the step list — the stack ends ~3mm above the pinned 204mm footer) with the **Kedaipal mark** (`QrCenterMark`, `public/logo.svg`) on an 18mm white panel in the centre ((18/80)² ≈ 5% obscured, DuitNow-style), and the template's own numbered steps below (reused `poster_*_step1-3`, **≤~60 chars/step** — one 13pt line per step is load-bearing; no new copy keys). The two-QR sheet also evolved past the strict v2 spec (owner call): **both 56mm QRs get the centre mark** (12.5mm panel, same ~5% area — decode-verified vs the real KPS payload) and the section gap widened 3→6mm (~9mm ceiling before the pinned footer). Print helper text adapts per template (`TEMPLATE_HELP`); counter-only keeps the storefront `?src=counter` fallback so it always prints. `StorePoster` gains `variant` prop + `SingleQrHero` (still the single poster renderer). See [`docs/store-qr-poster.md`](./store-qr-poster.md).
- **Chargeable pickup location — flat per-location fee** ✅ (ClickUp `86ey5tywf`, dev) — a seller can attach an optional **flat fee** (sen) to any pickup location (Bearcamp's paid tent drop-off was the driver). Buyer picks that point → fee becomes its own line and `total = subtotal + fee`, **frozen** at create (`pickupSnapshot.fee` + order-level mirror `orders.pickupFee` — later edits never rewrite a placed order). Rides the `computeOrderTotals` **extras seam** (now `{ quotedAmount, pickupFee }` — quote and fee are independent additive charges); every recompute site carries the fee (mockup submit/re-price, custom-decline) and **`updatePickupLocation` re-prices on a point switch** (paid→free drops it, free→paid adds it — a surface the ticket missed). Validated `sanitizeFee` (integer sen, ≥0, RM10k ceiling; 0 normalizes to unset so "free" has one spelling). **Setting a fee is Pro-gated** (`chargeablePickup` in `PLAN_FEATURES`; clearing is un-gated so a downgraded seller is never trapped; starter sees disabled-with-reason + Pro chip). Fee line on every total surface: storefront picker chip + checkout breakdown + wa.me message, tracking page, order detail, WhatsApp pickup block (EN+BM), receipt/invoice PDF row, CSV "Pickup fee" column (0.00 when free so the identity sums). **Counter checkout deliberately fee-free** — no location exists there; confirmed w/ Arif (ticket AC #8 assumed a picker that doesn't exist). See [`docs/fulfilment.md`](./fulfilment.md).
- **Frontend caching — Convex + TanStack Query adapter** ✅ (ClickUp `86eydh0dy`, dev) — the dashboard felt slow "even navigating to a page I've loaded before" because there was **no frontend query cache**: every screen read via plain `useQuery` from `convex/react`, and Convex drops a query's result the moment the last subscriber unmounts, so back-nav = `undefined` → skeleton → fresh WebSocket round-trip. Fix routes **all reads** through the official **`@convex-dev/react-query`** adapter (`useQuery(convexQuery(api.x, args)).data`) so results + their live subscription survive unmount for `gcTime` (10 min) — revisits paint **instantly from cache** while staying realtime (staleTime is `Infinity`; Convex pushes updates). **Client-side only, no SSR data fetching** (deferred to a separate cold-start ticket). Wiring: two lazy singletons in `src/lib/convex.ts` (`getConvexQueryClient` wraps the same `getConvexClient()` so Clerk's token flows for free; `getQueryClient` sets the bridge's `queryFn`/`queryKeyHashFn` + `gcTime`, then `connect`), `QueryClientProvider` in `__root.tsx` past the `SetupNotice` env guard. **The transform rule that matters:** `"skip"` stays **verbatim** inside `convexQuery(...)` (the adapter accepts it natively — hoisting to `enabled:` would force eager arg construction and reintroduce null-derefs), and reads unwrap into a **named `.data` var** so `undefined`(loading)/`null`(not-found) semantics and `typeof result`-derived types (e.g. the inbox `useRef`) are preserved; type-position `ReturnType<typeof useQuery<…>>` → `FunctionReturnType<…>`. `useDashboardRetailer` unwraps `.data` internally, keeping its `T|null|undefined` contract so ~8 route consumers are untouched. **Stays native:** `useMutation`/`useAction`/`useConvex`, and `usePaginatedQuery` (the whole `app.customers.index.tsx`). Singleton `QueryClient` is safe **only** because nothing fetches Convex during SSR today (`/app` is client-gated by Clerk `<Show>`; loaders populate `<head>` only) — the deferred SSR ticket must move it per-request first. See [`docs/frontend-caching.md`](./frontend-caching.md).
- **Plan-tier gating enforcement + order-usage meter** ✅ (dev) — the pricing table's live ✓/– rows are now **enforced**, closing the audit gap where Starter and Pro were functionally identical. **(1) Plan-feature gates (Pro+):** `PLAN_FEATURES`/`featuresForPlan` in `convex/lib/plans.ts`, resolved onto `AccessState.features` by `resolveAccess` (the ONLY place `plan` is read for gating; missing row fails open to Pro — never a lockout; trial = Pro features). Server gate `assertPlanFeature` on **all CRM surfaces** (`convex/customers.ts` — internal order-linking deliberately un-gated so a Starter store's data is complete on upgrade day) and the **Order Inbox surfaces**: `searchOrders` rejects inbox-only args (buckets/search/filters) while the **plain default-args list stays all-tier** (the "Order pipeline" row), plus `bulkUpdateStatus` + CSV `exportOrders`. Admin act-as bypasses everything (mirrors the soft-lock). UI: `ProFeatureWall` on `/app/customers`(+detail), `ProFeatureTease` strip on the orders page (list keeps working), **Pro chips** in sidebar/bottom-nav + locked dashboard stat tile (`src/components/app/pro-gate.tsx`; client mirror `hasFeature`, fail-open — server is the lock). **(2) Order-usage meter (the promised soft-cap nudge):** new `subscriptionUsage` table keyed per-retailer × **MYT calendar month** (not the billing period — caps are "orders/mo", cycles can be annual); incremented at both create sites (storefront + counter), decremented on first-cancel (creation-month, floored); surfaced as `ordersThisMonth` on the retailer payload → billing-tab progress meter + `SubscriptionBanner` amber nudges (≥80% dismissable-for-month, ≥100% persistent) — **never blocks an order**. **(3) Soft-lock sweep:** `renameSlug` + all 4 `pickupLocations` writes now `assertSubscriptionActive` (had escaped Phase 2). **(4) Pricing-table truth:** date picker un-`comingSoon`'d and marked **all-tier** (it's core checkout for every storefront — buyer flow can't vary by seller plan). Tests `convex/planGating.test.ts` + `convex/lib/usagePeriod.test.ts` + additions. See [`docs/manual-subscription.md`](./manual-subscription.md).
- **Counter checkout — one QR + buyer pairing code** ✅ (ClickUp `86ey5neg6`, dev) — collapsed counter checkout to a **single** QR (the permanent `KPS-` store QR) and **removed the per-session `KP-` flow** it competed with (`createCheckoutSession`, `bindCheckoutSession`, the `checkout_bind` intent + `CHECKOUT_TOKEN_REGEX`, `counterCheckoutBound/Expired/Used` copy, `AwaitingScreen`/`ExpiryCountdown`, `checkoutSessionCreate` rate limit; `awaiting_buyer` status literal kept for migration-safety, just never created). **No "Start checkout" button** (the QR is static) — the Counter page shows the one store QR **compactly in the header** (`StoreQrChip`, tap to enlarge); the walk-in session is created when the buyer scans. **All QR management (rotate) moved to `/app/poster`** (its natural home); the on-page "Store QR card" was removed. **Buyer pairing code — made actionable:** `startSessionFromStoreQr` mints a short `counterCheckoutSessions.pairingCode` (e.g. `K7`, unambiguous letter+digit, unique among open walk-ins), shown in the `storeQrConnected` ack ("Your order code is `*K7*`") **and** as the open-checkouts row avatar — and the list now has a **search box (code/name) with Enter-to-open** so the cashier acts on the code instead of eyeballing. **Downloads/poster:** the counter card's print button + rotate live on `/app/poster` (one A4 renderer; old client-side PNG + `escapeXml` removed); the dashboard **`StorefrontQrDialog` now shows both QRs** (Order online + At the counter), each with its own Download-PNG, centered/width-capped on desktop. Pay-at-scan (`86ey5kq7p`) unchanged. See [`docs/counter-checkout.md`](./counter-checkout.md).
- **Printable store QR poster (EN/BM)** ✅ (ClickUp `86ey5m4m9`, dev; pairs with the static-QR backend `86ey5m35w`) — self-serve replacement for the hand-built Mr Ganu counter poster: `/app/poster` renders a print-ready **A4 sheet** (seller logo on a white panel over the navy header, store name, two `level="H"` QR cards "At the counter" / "Order online", URL pill, powered-by-Kedaipal footer) and downloads via **browser print-to-PDF** — no server rendering. Poster copy is **buyer-facing** with a BM|EN toggle (**default BM**, explicit `m.poster_*({}, { locale })` Paraglide override, independent of any cookie); page chrome stays EN like all `/app` routes. **The left "At the counter" QR encodes Zaki's permanent walk-in `wa.me?text=…KPS-<counterQrToken>…` deep link** (`getStoreQr().waUrl`): scan → connected on WhatsApp → **cashier rings up the order** (copy rewritten from the old self-order flow to match). The route **auto-provisions** the token on first visit (one-shot idempotent `ensureCounterQrToken`, act-as mints for the seller's store), falling back to the storefront `?src=counter` link while it resolves / when `WHATSAPP_CHECKOUT_PHONE` is unset, so the poster always prints. Right QR = storefront `?src=online` (`?src` reserved for PostHog). `StorePoster` is fully presentational (`counterUrl`/`onlineUrl` props); `posterQrUrls` now only builds the storefront pair. **Coexists by design** with the counter-checkout Store QR card (`86ey5m35w`, quick PNG + Generate/Rotate in `/app/checkout`) — both share `retailers.counterQrToken`, so a rotate kills posters from either. First print surface in the repo: shell chrome got global `print:hidden`, the `@page { size: A4; margin: 0 }` rule is **route-scoped** (mounted `<style>`, can't leak into other pages' printing), sheet sized in mm with a `transform: scale()` mobile preview, 56mm QR boxes survive iOS Safari's ignore-`@page`-and-fit (~50mm ≥ 45mm floor). Print stamps `markLinkShared` (skipped in admin act-as). Discoverable via a "Promote your store" card on `/app` home + a cross-link in the storefront QR dialog; storefront URL construction centralised into `src/lib/storefront-url.ts` in passing. See [`docs/store-qr-poster.md`](./store-qr-poster.md).
- **App UI/UX redesign — direction 1a** ✅ (ClickUp `86ey52f55`, dev) — mobile-first visual + interaction pass over every `/app` screen, pulling the landing's character in (Red Hat Display headings, mint highlights, ticket borders). Four global moves: one control surface per page; a **badge diet** (`OrderContextBadge` picks the single contextual badge: mockup → due date → escalated age); one shared **`FilterChip`** primitive (replaces 3 divergent hand-rolled chip styles); every screen leads with its verb. Highlights: Home gets a tappable **today strip** (due today / new / unpaid → pre-filtered inbox) + "needs attention" rows replacing the stat grid; the orders inbox collapses to search + one chip row with a contextual due-today banner, hidden-until-select checkboxes (header button or long-press) and a floating bulk bar leading with the most likely transition; the filter sheet owns the due window + shows a live "Show N orders" count; order detail leads with a **stepper + one big advance button**, an amber payment-claimed state card and CRM context on the customer card; products get stock **colour words** + a stock stepper + sticky save with archive-as-icon; customers get tinted avatars + mint LTV + sticky-note notes; **Settings on mobile is a grouped list index** (Store / Selling, status glances, `?tab=` now optional but all deep links still work; desktop keeps tabs). New `--accent-emphasis` token (readable mint text on tinted surfaces). Backend delta: `searchOrders` counts gained `dueToday`/`unpaid`/`unpaidAmount` (same scan) — Home now reads the same counts seam. The design doc's 1b day-planner inbox is a documented alternative, not built. See [`docs/app-redesign.md`](./app-redesign.md).
- **Counter checkout — pay-at-bind payment info** ✅ (ClickUp `86ey5kq7p`, dev) — right after the buyer scans the seller's `KP-<token>` QR and gets the `counterCheckoutBound` ack, the bot now also pushes the seller's **payment details** (new `counterCheckoutPaymentIntro` EN+MS + `renderPaymentMethods` block + QR images) so the buyer can pay **whenever they're ready** — even while the cashier is still ringing up — instead of waiting for the details at the end. `handleInbound`'s bind branch schedules `notifyCounterCheckoutPayment` (`runAfter(0)`, so the ack lands first; loads methods via the retailerId-keyed `getRetailerPaymentContext` since no order exists yet); **no-ops** when the seller has no methods; sent as a gated `session_message` (retailer known → caps apply). **No double-send:** the pay-later order-create message now omits the repeated bank/QR block (`sendPaymentMessage(..., includePaymentDetails: false)`) — buyer sees the details **once** per session, at scan, plus the invoice PDF; the order-create message keeps the amount + transfer ref + "I've paid" CTA. `bindCheckoutSession` bound-result widened with `retailerId`; dead `paymentMethods`/`paymentInstructions` dropped from `getCounterOrderMeta`. Seller-side discoverability: the counter "Ask the buyer to scan" screen shows a one-line helper (gated on the seller having ≥1 payment method). See [`docs/counter-checkout.md`](./counter-checkout.md).
- **Store cover/banner image** ✅ (ClickUp `86ey391dh`, dev) — a wide cover image (`retailers.coverImageStorageId`, public-safe) that becomes the **storefront header background** (`object-cover` fills the header; Kedaipal mark + store logo + name + blurb overlay on a bottom scrim, white text; falls back to the original light-gradient header when unset) and the **primary** OG/Twitter/JSON-LD image, precedence **cover → logo → first product image** (`twitter:card` stays `summary_large_image` when any resolves). Parallels the logo end-to-end: both reads resolve `coverImageUrl` (`getMyRetailer` + `getRetailerBySlug`); `updateSettings` takes `coverImageStorageId` (blank clears, undefined = no change) and **GCs the replaced/cleared blob** (mirrors the QR-image GC); new `generateCoverImageUploadUrl` (rate-limited like the logo upload); deletion cascade deletes the cover blob. Settings → Store tab gets a **"Cover image"** card (`CoverImageForm`) directly after the logo card (wide 3:1 preview, upload/replace/remove). **Folded in a pre-existing leak fix:** the **logo** replace-GC gap (old logo blob wasn't deleted on swap) — `updateSettings` now GCs the old logo blob too. Schema widened dev-only (no backfill). See [`docs/store-cover-banner.md`](./store-cover-banner.md).
- **Drop-off copy sweep + custom-stage WhatsApp + payment reminder** ✅ (ClickUp `86ey570am`, dev) — three fixes from the first live drop-off test (Bearcamp). **(1) Kind-aware copy everywhere:** checkout date step ("When should we meet?"), WhatsApp packed/shipped/confirm (EN+MS, via new `CopyVars.pickupKind` — `getOrderWithRetailer` now returns the frozen snapshot's kind), tracking page + seller order detail chips ("Drop-off" / "Meet on" / "Meet at"). **(2) Anchor-crossing status messages speak the seller's custom stage vocabulary** (label + description via `renderStageUpdate`, carrier link kept on shipped) when `orderStages` are configured; precedence: authored template override → stage copy → default catalog (default-stage sellers see zero change). **(3) Unpaid-order payment reminder** — NEW standard (no prior default existed): 14-day open-payment window, ONE WhatsApp nudge at day 11 (3 days before close), daily cron + `orders.paymentReminderSentAt` stamp-at-schedule/re-check-at-send, gated `session_message` (kill switch/caps/opt-outs apply), bounded creation-time index scan, nothing auto-cancels at day 14; surfaced to sellers in Settings → Payments. Due-status set is `confirmed`/`packed`/`shipped`/**`delivered`** (only `pending`/`cancelled` excluded) — PR review caught that `delivered` orders were wrongly skipped, but F&B sellers routinely deliver stock on credit and settle at week's end, so goods-arrived ≠ goods-paid-for. See [`docs/payment-reminder.md`](./payment-reminder.md).
- **Counter checkout — scan once, receipt/invoice to buyer** ✅ (ClickUp `86ey4fz3w`, dev) — closing the loop on the counter QR's core promise: the buyer scans **once** to bind their WhatsApp, and everything after (confirmation, receipt, pay-later) rides that same chat — **no rescan**. The ticket's "scan once" headline was **already structurally true** (bind is single-use; `notifyCounterOrderCreated` sends the tracking link post-order), so the real work was copy + Done-screen document tools. **(1) Humanized + localized WA copy:** the inline English bind reply + counter confirmation strings moved into the `whatsappCopy` catalog as system messages (`counterCheckoutBound/Expired/Used`, `counterOrderConfirmed{Paid,Unpaid}`, `orderReceiptCaption/orderInvoiceCaption`), warmed up, and **localized to the store locale** (`bindCheckoutSession` now returns `locale`; `not_found` has no store → stays English). **(2) Receipt/Invoice — one PDF, two faces:** `buildOrderReceiptPdf` titles off a new `OrderReceiptData.paid` flag — settled → **"Receipt"** (green Paid badge), unpaid/claimed → **"Invoice"** (amber Awaiting-payment + "How to pay" + `ORD-XXXX` reference footer); download filename matches. **No** new builder/table — the pay-later case reuses the receipt renderer. **(3) Auto-send to buyer's WhatsApp on checkout:** new outbound **`document`** kind on the channel adapter (`sendDocument` → Meta `type:"document"`). After `createOrderFromSession`, `whatsapp.notifyCounterOrderCreated` fires automatically — **paid now** → "confirmed & paid" text + **receipt** PDF; **pay later** → the **payment ask** (transfer-ref + payment methods + "I've paid" CTA + QR images via the shared `sendPaymentMessage`) + **invoice** PDF. Delivery goes through the shared `deliverOrderDocument` (render → store **transiently** for Meta to fetch → `transactional` send → scheduled `deleteTransientStorage`), reached by `orders.sendOrderDocument` (internal, orderId — the auto path) and `orders.sendOrderDocumentToBuyer` (seller-auth shortId — manual resend). **(4) Done-screen UI** (`src/components/order/send-order-document.tsx`): frames it as **already sent** → Resend / Download / Share (OS share sheet via `navigator.share`, falls back to download), branched receipt vs invoice. Only renders on the fresh-create path (has `shortId` + accurate paid state); **resend-from-order-detail is a noted follow-up**. Schema untouched (`paid` is derived). **(5) Build-screen UX polish** (same ticket, `app.checkout.tsx`): product thumbnails in the catalog (`ProductThumb`), a persisted **list ↔ grid** view toggle (`useCatalogView` → localStorage; grid tiles open variants in a modal, both views share the extracted `ProductVariantRows`), a confirm-gated **Cancel checkout** on the build screen, a **humanized QR prefill** (warm first-person message wrapping the `KP-<token>` ref — only the ref is load-bearing), and the "Start checkout" CTA moved inside the header card for uniform desktop width. See [`docs/counter-checkout.md`](./counter-checkout.md).
- **Hidden-from-storefront product toggle** ✅ (ClickUp `86ey40ze9`, dev) — a product-level **`hidden`** flag (orthogonal to `active`/archive) that keeps a product **off the public storefront** while it stays **fully sellable in counter checkout**, inventory, and the dashboard. The driver: a pre-priced event SKU (Rahman's lekor) rung up at the counter but never listed online, sidestepping the custom-price-approval flow (which can't fire per walk-up sale). **The trap the ticket missed:** counter checkout shared the public `products.list` query (active-only), so naively filtering hidden out of `list` would have removed it from the counter too — so counter now reads a new **owner-OR-admin `products.listForCounter`** (active products, hidden **included**); the public unauthenticated `list` excludes hidden (in-memory filter on the already-active-scoped set, no new index; **no** `includeHidden` param — that would leak to shoppers). Editor gets a top-of-form **Visible/Hidden** control; dashboard + counter picker carry a **Hidden** badge so the state is never silent. Schema widened dev-only (no backfill). Pure list/unlist — no price/approval/variant logic touched. See [`docs/hidden-products.md`](./hidden-products.md).
- **Admin Console — act-as seller** ✅ (ClickUp `86ey25er1`, dev) — the white-glove onboarding tool: an admin opens `/app/admin/sellers`, picks a store, and operates the **normal `/app/*` dashboard as that seller** (products, variants, orders, customers, settings, fulfilment, counter checkout). **No parallel admin UI** — the act-as target is a **persistent client session** (`useActAs` context mirrored to `sessionStorage`, wrapping `/app`), read by one hook (`useDashboardRetailer`); it holds across every navigation/CRUD/refresh until Exit, so nothing has to thread a param (a URL `?actAs` param was tried first and abandoned — too fragile: every link/`navigate` that forgot it dropped act-as). The scattered owner-only checks were centralised into a single **owner-OR-admin** gate `requireRetailerAccess` in `convex/lib/auth.ts` (returns `{retailer, actingAsAdmin, userId}`; throws `Forbidden` for anyone else, so owners see zero change). Admin act-as writes **bypass `assertSubscriptionActive`** (onboarding happens before the seller pays); the seller's own `past_due` writes stay blocked. Every admin-on-behalf write drops an **`adminAuditLog`** row via `logAdminAction` (owner writes never logged); `admin.recentAuditForRetailer` surfaces them. Identity-derived writes got explicit-`retailerId` admin paths (`updateSettings`, `renameSlug`, counter-checkout create/list; session-id ops check `session.retailerId`); `getMyRetailer` stayed a zero-arg query with act-as split into `getRetailerForAdmin` (avoids ~40 test-call churn). Persistent amber **"Acting as {store}"** banner + Exit; sidebar splits seller nav from a labelled **Admin** group (which ends the session); directory gated by `requireAdmin` server-side. **Admins need no store of their own** — a storeless admin runs the console in admin-only chrome (null-retailer `Sidebar`/`MobileHeader`/`BottomNav`) and only ever operates vendors via act-as. `generateLogoUploadUrl` needed no change (a ticket inaccuracy — storage id isn't retailer-scoped until attached via the checked mutation). Admin allowlist stays in `ADMIN_USER_IDS` env. See [`docs/admin-console.md`](./admin-console.md).
- **Activation checklist** ✅ (ClickUp `86exrxj0x`) — the dashboard setup checklist now drives **activation**, and its **completion gate is the first subscription payment** (trial→paid), not config. Two distinct milestones: **(1)** in-system `activatedAt` = first confirmed customer order (new retailer-level one-time stamp + `linkSharedAt`, never un-set, set via shared `stampRetailerActivation` set-if-unset wired into all four confirm sites — WhatsApp confirm, payment auto-confirm, seller status transition, counter checkout) → drives a transient 7-day **first-order celebration** overlay (testimonial CTA → WhatsApp contact). **(2)** UI "onboarding complete" = the checklist's final required **"Subscribe to a plan"** step, done when `hasSubscribed(subscription)` (left `trialing`, or `comped`) — derived from the embedded subscription, no new schema. Checklist stays until subscribed even after first order; trial step shows live days-left. Also: product step recommends **bulk import** first; new required **"Share your store link"** step (`ShareLinkChecklistRow`, copy/QR stamps `linkSharedAt`, hero copy/QR stamp too). `activatedAt`/`linkSharedAt` widened dev-only (no migration). PostHog funnel deferred. See [`docs/activation-checklist.md`](./activation-checklist.md).
- **Invoice & Receipt PDFs + CSV export** ✅ (ClickUp `86ext578n`) — two PDF documents plus a bookkeeping export. **(A) Order receipts** are buyer-facing and **generated on demand** (not stored — deterministic from the order); the buyer self-serves from `/track/<token>`, the seller from order detail, both via `orders.generateReceiptPdf` behind the `resolveSharedOrder` auth seam. **(B) Subscription invoices** (Kedaipal→seller) are **rendered + stored at issue time** (`invoices.pdfStorageId`, frozen so mutable `billingConfig` bank details can't drift); downloadable from the billing tab + admin console via the ownership-checked `getInvoicePdfUrl`. **Bulk export** from the orders inbox is **CSV** (not a PDF bundle — bookkeeping is the real job), honouring the live inbox filter or a ticked selection via the `orders.exportOrders` **action**, which paginates the full result set (not the inbox's 1000-doc reactive cap) with a `capped` backstop so books are never silently truncated. The ticket was **stale** — UC B's spine already existed (`86expn2qg`), so we extended the live `invoices` table rather than building the ticket's colliding generic table. pdf-lib renders in the default Convex runtime; pure logic in `convex/lib/pdf/` + `convex/lib/orderInboxFilter.ts` (extracted from `searchOrders`) + `convex/lib/orderCsv.ts` (with formula-injection defense). See [`docs/invoices-receipts.md`](./invoices-receipts.md).
- **WABA Protection — real-now core** ✅ (ClickUp `86expmgep`, dev) — the gateway every outbound WhatsApp message passes through, protecting the shared number. `canSend(retailerId, toPhone, category)` + `makeGuardedSender` (wraps all 14 send sites). **Category policy:** `transactional` order messages **bypass all gating** (per ticket edge cases); only `session_message`/templates are gated — so the kill switch/caps mainly govern session replies today and become fully load-bearing with Broadcast. Ships: per-retailer **kill switch** (`retailerSendingLimits.pausedAt`, CLI `wabaProtection:pauseRetailer`/`resumeRetailer` + dashboard banner + email), **tiered caps** (50 first-30d / 200 Pro / 500 Scale + 30/5min), **cross-retailer opt-out** (STOP/BERHENTI/START → global `optOuts`), **quality-webhook auto-throttle** (LOW→pause non-transactional), **`outboundMessageLog`** audit. Admin UI relocated → Admin Console `86ey25er1`; spam-detection/Marketing-pause/ramp/health-widget → Broadcast. **Needs Meta dashboard:** subscribe webhook fields `phone_number_quality_update` + `account_update` for auto-throttle (fails open to healthy until then). See [`docs/waba-protection.md`](./waba-protection.md).
- **Customer Database (CRM-lite)** ✅ — `customers` entity keyed by `(retailerId, waPhone)` with denormalized lifetime aggregates, auto-captured WhatsApp pushname, private notes, and a `/app/customers` dashboard (list + detail). Backend + UI. The S1 "Customer DB" roadmap item. See [`docs/customer-database.md`](./customer-database.md). Blocks Automated Reminders + Broadcast.
- **Webhook signature verification** ✅ — inbound `POST /webhook/whatsapp` verifies Meta's `X-Hub-Signature-256`. See [`docs/whatsapp-webhook-security.md`](./whatsapp-webhook-security.md).
- **Channel adapter seam** ✅ (Phases 1–3) — WhatsApp is now one of N messaging channels behind a uniform `ChannelAdapter` (`convex/lib/channels/`). Outbound/inbound/signature all flow through `getAdapter("whatsapp")`; the order orchestration is channel-neutral. Pure refactor, zero behavior change (one delta: signed-but-malformed webhook body → `200`+log, not `400`). Identity migration (`waPhone` → `channelUserId`, Phases 4–6) deferred until a 2nd channel is greenlit. See [`docs/messaging-channels.md`](./messaging-channels.md).
- **Mockup / proof approval** ✅ — made-to-order custom products gate production on buyer sign-off. Per-product `requiresProof` toggle → orders get a third independent dimension `mockupStatus` (`pending → submitted → approved`, + `changes_requested` loop) that blocks `confirmed→packed` until the buyer **approves on the tracking page** or the seller **waives** after a 48h grace. Seller uploads + WhatsApp-sends the mockup; buyer approves/requests-changes; seller emailed on each. Code uses **`mockup`** (not `proof` — that's the payment screenshot). Reminder nudges deferred (waiver is time-based). See [`docs/proof-approval.md`](./proof-approval.md).
- **Product Variants** ✅ — products generalized from flat single-SKU to **option-axes + variant-rows** (Shopify/Shopee/TikTok shape). New `productVariants` table holds per-variant price/stock/SKU/weight/image; `products` keeps **0–2 option axes** + a `blockWhenOutOfStock` (made-to-order) toggle. Every product resolves to ≥1 variant (implicit `optionValues:[]`) — no separate simple-product path. Storefront pill pickers + two-reason grey-out + markdown descriptions; dashboard variant-grid editor (axis presets, bulk-fill, per-row image/deactivate). Caps (2 axes / 50 variants) = TikTok/Shopee/Lazada parity. Schema **widened** (dev only); the production backfill→narrow migration is a separate task. See [`docs/product-variants.md`](./product-variants.md). Mockup requirement is now **per-variant** (not all-items), and payment moved to **after** mockup approval for custom orders.
- **Self-collect pickup locations** ✅ — multi-location pickup with dashboard setup; frugal scope (free-text address + Waze/Google Maps deep links, no Places API). Map URLs patched for Waze mobile/desktop + place details. ClickUp `86exq8ymf`. See [`docs/fulfilment.md`](./fulfilment.md).
- **Optional delivery (symmetric fulfilment)** ✅ — delivery is now a first-class toggle (`retailers.offerDelivery`) like self-collect, so sellers offer delivery-only, pickup-only, or both. A **working-method invariant** (enforced in `retailers.updateSettings` + `pickupLocations.setActive`, mirrored in the UI + storefront) guarantees a storefront never loses its last way to receive an order — "working" self-collect requires ≥1 active pickup location. Legacy default asymmetry: `offerDelivery` undefined → **true** (vs `offerSelfCollect` → false), no migration. Settings "Pickup" tab → **"Fulfilment"** (two toggle cards), checklist step → "Set up delivery & pickup" (shown to all). ClickUp `86exu4grm`. See [`docs/fulfilment.md`](./fulfilment.md).
- **Order note at checkout** ✅ — shopper attaches one free-text instruction to an order. See [`docs/order-note.md`](./order-note.md).
- **Fulfilment date at checkout** ✅ — the lean Date Picker (ClickUp `86expm524`). One required native `<input type="date">` at checkout — _"when do you need this?"_ — for **both** delivery and self-collect (the ICP case, cake delivery, is delivery-led). Stored as `orders.fulfilmentDate` (epoch-ms of MYT midnight; MY is UTC+8/no-DST). Validated to `[today + minNotice, today + 30]`; `retailers.minFulfilmentNoticeDays` (default **0 = same-day allowed**) lives in Settings → **Fulfilment**. **Counter Checkout** defaults the date to **today**, bypasses the buyer notice, and — when paid in person — offers an optional **"Mark as completed"** button on the success screen (one tap → `delivered`; a choice, not automatic). Surfaces end-to-end: buyer's WhatsApp message + new-order email + tracking page, and the **order inbox now defaults to fulfilment-date-ascending sort** with **Today/Tomorrow/This-week** chips + per-card urgency badge. All date logic in the shared `convex/lib/fulfilmentDate.ts` (imported by both sides). Unblocks the Pickup Reminder. See [`docs/fulfilment-date.md`](./fulfilment-date.md).
- **One-tap copy payment details** ✅ — shoppers copy acc no / DuitNow ID from the order details page. ClickUp `86exv7772`.
- **Store description on storefront** ✅ — retailers set a short public blurb (`retailers.storeDescription`, ≤280 chars) that renders under the store name on `/<slug>`, replaces the generic tagline when set, and feeds the SEO/OG description. Plain-text, escaped, line-clamped. ClickUp `86extzdmd`. See [`docs/store-description.md`](./store-description.md).
- **Payment handshake** ✅ in production — the manual two-button payment confirmation flow; canonical doc is [`docs/payment-handshake.md`](./payment-handshake.md) (roadmap doc superseded).
- **Landing + pricing + cost redesign** ✅ (merged Jun 11, PRs #24/#25) — animated mobile-first UI, branded OG image + fresh structured data (SEO), dark-background logo variant, Kris's FROZEN-led assets live. **Founding-spot CTAs open WhatsApp contact, not sign-up.** Setup Wizard + Legal Pack (AUP/Terms/Privacy) + ROI weekly digest also complete in ClickUp.
